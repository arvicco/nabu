# Derived / rebuildable data (regenerated by `nabu rebuild`)
/db/*.sqlite3
/db/*.sqlite3-*
/vectors.sqlite3

# Personal research focus (nabu focus) — owner interest, not a publication
/config/profile.yml

# Local canonical corpus & downloaded sources (large, often licensed)
/canonical/
/tmp/
/log/

# Manual Adapter drop box (P63-1): owner-acquired downloads awaiting ingest
/incoming/

# Ruby
/.bundle/
/vendor/bundle/
*.gem
.ruby-version

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
.claude/worktrees/
.docs/

# P71: the local instance folder — owner rulings, grants, profile, settings;
# part of the BACKUP set, never of the repo (the derivability law).
local/
