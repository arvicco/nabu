# Nabu fixture manifest (P5-4). Schema documented in
# test/fixtures/perseus/manifest.yml.
#
# LOCAL-TRIM PROVENANCE. These five HGV EpiDoc metadata records (P15-2 date/place
# timeline extractor exemplars) were TRIMMED from the locally synced canonical
# snapshot canonical/papyri-ddbdp/HGV_meta_EpiDoc/ on 2026-07-12 — kept to the
# teiHeader idno + history/origin the TimelineBuilder reads, structurally intact
# EpiDoc. There is no raw URL to re-GET (re-acquiring means a full idp.data
# corpus sync), so every entry is refetchable: false — `fixtures:check` SKIPS
# them and `fixtures:refresh` refuses to touch them.
source: timeline
adapter_test:
files:
  - path: papyri-ddbdp/HGV_meta_EpiDoc/HGV1/56.xml
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full idp.data corpus sync, not a raw GET
    canonical: canonical/papyri-ddbdp/HGV_meta_EpiDoc/HGV1/56.xml
    upstream: https://github.com/papyri/idp.data
    copied: 2026-07-12
    whole: false
    trim: teiHeader idno + history/origin only — BCE point (when="-0113"), Pathyris
  - path: papyri-ddbdp/HGV_meta_EpiDoc/HGV1/758.xml
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full idp.data corpus sync, not a raw GET
    canonical: canonical/papyri-ddbdp/HGV_meta_EpiDoc/HGV1/758.xml
    upstream: https://github.com/papyri/idp.data
    copied: 2026-07-12
    whole: false
    trim: teiHeader idno + history/origin only — open-ended notAfter-only date
  - path: papyri-ddbdp/HGV_meta_EpiDoc/HGV1/610.xml
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full idp.data corpus sync, not a raw GET
    canonical: canonical/papyri-ddbdp/HGV_meta_EpiDoc/HGV1/610.xml
    upstream: https://github.com/papyri/idp.data
    copied: 2026-07-12
    whole: false
    trim: teiHeader idno + history/origin only — undated ("unbekannt"), placed
  - path: papyri-ddbdp/HGV_meta_EpiDoc/HGV1/997.xml
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full idp.data corpus sync, not a raw GET
    canonical: canonical/papyri-ddbdp/HGV_meta_EpiDoc/HGV1/997.xml
    upstream: https://github.com/papyri/idp.data
    copied: 2026-07-12
    whole: false
    trim: teiHeader idno + history/origin only — two alternative when-dates (envelope)
  - path: papyri-ddbdp/HGV_meta_EpiDoc/HGV1/9150.xml
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full idp.data corpus sync, not a raw GET
    canonical: canonical/papyri-ddbdp/HGV_meta_EpiDoc/HGV1/9150.xml
    upstream: https://github.com/papyri/idp.data
    copied: 2026-07-12
    whole: false
    trim: teiHeader idno + history/origin only — notBefore/notAfter CE range, precision low
  # -- P16-3 (timeline part 2): ORACC catalogue dates + TOROT annals.
  # Same local-trim provenance, copied 2026-07-13: catalogue.json files kept
  # to a handful of verbatim members (summaries dropped — display HTML the
  # extractor never reads); lav.xml kept to four divs × ≤2 sentences each.
  - path: oracc/saao-saa01/saa01/catalogue.json
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full oracc project sync, not a raw GET
    canonical: canonical/oracc/saao-saa01/saa01/catalogue.json
    upstream: https://oracc.museum.upenn.edu/saao/saa01/
    copied: 2026-07-13
    whole: false
    trim: member P224395 only — regnal formula Sargon2.000.00.00, Nimrud + pleiades_id
  - path: oracc/saao-saa02/saa02/catalogue.json
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full oracc project sync, not a raw GET
    canonical: canonical/oracc/saao-saa02/saa02/catalogue.json
    upstream: https://oracc.museum.upenn.edu/saao/saa02/
    copied: 2026-07-13
    whole: false
    trim: members P500551 (eponym limu formula), P336039 (00.000.00.00 → period fallback), P240211 (Shamshi-Adad5)
  - path: oracc/rinap-rinap1/rinap1/catalogue.json
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full oracc project sync, not a raw GET
    canonical: canonical/oracc/rinap-rinap1/rinap1/catalogue.json
    upstream: https://oracc.museum.upenn.edu/rinap/rinap1/
    copied: 2026-07-13
    whole: false
    trim: member Q003414 only — absolute BCE range date_of_origin "744-727"
  - path: oracc/riao/catalogue.json
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full oracc project sync, not a raw GET
    canonical: canonical/oracc/riao/catalogue.json
    upstream: https://oracc.museum.upenn.edu/riao/
    copied: 2026-07-13
    whole: false
    trim: members Q006693 ("9th-8th century"), Q005837 ("ca. 1233-1197"), Q003700 ("668-ca. 631")
  - path: oracc/dcclt/catalogue.json
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full oracc project sync, not a raw GET
    canonical: canonical/oracc/dcclt/catalogue.json
    upstream: https://oracc.museum.upenn.edu/dcclt/
    copied: 2026-07-13
    whole: false
    trim: members P212382 (period Old Babylonian, Nippur + pleiades_id), P230009 (period "uncertain" — unmapped, skipped+counted)
  - path: torot/lav.xml
    refetchable: false
    provenance: local-trim
    reason: trimmed from local canonical snapshot; re-acquiring = full torot corpus sync, not a raw GET
    canonical: canonical/torot/lav.xml
    upstream: https://github.com/torottreebank/treebank-releases
    copied: 2026-07-13
    whole: false
    trim: annotation+source header intact; divs Introduction (non-annal), 6360, 6361, 6369–6370 at ≤2 sentences each
