# Nabu fixture manifest (P5-4). Schema documented in
# test/fixtures/perseus/manifest.yml.
#
# API-ENVELOPE PROVENANCE (the lexlep precedent). Retrieved 2026-08-18
# from https://ko.wikisource.org/w/api.php in the fetcher's own shape
# (prop=revisions&rvprop=content|ids|timestamp&rvslots=main); the page
# file is the adapter's per-page envelope with the API's wikitext
# byte-verbatim inside. A raw re-GET returns upstream's CURRENT revid —
# a living wiki — so the entry is refetchable: false: `fixtures:check`
# skips it and `fixtures:refresh` refuses to touch it; drift is caught
# by the revid-driven fetch itself at any real re-sync.
source: ko-wikisource-mk
adapter_test: test/adapters/ko_wikisource_mk_test.rb
retrieved: 2026-08-18
files:
  - path: pages/yongbieocheonga.json
    refetchable: false
    provenance: api-envelope
    reason: adapter fetch envelope around the api.php revisions response (revid 454577);
      upstream serves current revids, not this snapshot
    upstream: https://ko.wikisource.org/wiki/용비어천가
    copied: 2026-08-18
    whole: true
