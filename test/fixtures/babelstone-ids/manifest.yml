# Nabu fixture manifest (P5-4). Schema documented in
# test/fixtures/perseus/manifest.yml. Built from this directory's README.md.
#
# A documented TRIM (whole: false) of Andrew West's IDS.TXT (3.1 MB, Unicode
# 16.0, 97,680 entries, File Date 2025-06-27): the real header (metadata +
# the public-domain dedication §2 + format notes §4/§5) kept verbatim, then
# 12 selected data lines (棄 U+68C4 and a coherent transitive-containment
# neighbourhood) byte-verbatim, BOM + CR/LF preserved. A fresh GET returns
# the whole file; re-apply the README recipe after any refresh.
source: babelstone-ids
adapter_test: test/adapters/babelstone_ids_test.rb
retrieved: 2026-07-20
files:
  - path: IDS.TXT
    url: https://www.babelstone.co.uk/CJK/IDS.TXT
    whole: false
    trim: IDS.TXT (3.1 MB, 97,680 entries) -> the real header + 12 selected data lines (60 lines total, BOM+CRLF preserved); recipe in README.md
