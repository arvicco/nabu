# Nabu fixture manifest (P5-4). Schema documented in
# test/fixtures/perseus/manifest.yml. Built from this directory's README.md.
#
# adapter_test is null: unikemet is a feature module (kind: module) —
# discover yields nothing and parse is unreachable; the work is the
# Nabu::Hieroglyphs read seam (test/hieroglyphs_test.rb) and the P65-2
# HieroCard query (test/query/hiero_card_test.rb).
source: unikemet
adapter_test:
retrieved: 2026-08-09
files:
  - path: Unikemet.txt
    refetchable: false
    provenance: local-trim
    reason: header comment block (lines 1-32) + the complete U+13000/U+1305D/U+13143/U+13171/U+13216/U+13460 sign blocks, byte-verbatim from the 1,482,071-byte Unikemet-17.0.0.txt — covers core/legacy/default kEH_Core, a description-less legacy sign, kEH_AltSeq, kEH_FVal phonemogram + logogram, an Extended-A codepoint, and N35 (attested in the aes fixture); re-acquiring = fetch the versioned URL and re-grep the same codepoints
    upstream: https://www.unicode.org/Public/17.0.0/ucd/Unikemet.txt
    copied: 2026-08-09
    whole: false
    trim: 6 of 5,067 codepoints (P65-2, the Nabu::Hieroglyphs seam)
