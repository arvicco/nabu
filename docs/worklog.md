# Worklog

One line per completed packet: date · packet · commit · notes.

---

2026-07-03 · P0-1 · e35a163 · Ruby skeleton: Gemfile (budget-only), Rakefile, RuboCop, minitest+WebMock harness; net-blocked suite green.
2026-07-03 · P0-2 · 35099d1 · Thor CLI skeleton (bin/nabu, version + not-implemented stubs) and Nabu::Config with project-relative defaults + commented config/nabu.yml.
2026-07-03 · P0-4 · 6082c74 · Core primitives: Nabu::Error hierarchy (ParseError/FetchError), Open3-backed Nabu::Shell.run (argv-only, status+stderr on failure), Nabu::Normalize.nfc (UTF-8 NFC, raises on invalid bytes); NFD-Greek regression fixture.
2026-07-03 · P0-3 · a7d0d83 · GitHub Actions CI (.github/workflows/ci.yml): push+pull_request, ubuntu-latest, setup-ruby pinned to 3.3 with bundler-cache, rake test then rake lint as separate steps; concurrency cancels in-progress runs per ref.
2026-07-03 · gate · — · Phase 0 gate: Fable review of full phase diff — pass, no architecture deviations; suite 31/75 green, lint clean; branch pushed, PR opened.
2026-07-03 · P1-1 · 998b2f2 · Domain values in lib/nabu/model/: Passage/DocumentRef/SourceManifest as validating Data.define (keyword ctor, deep-frozen JSON annotations/metadata, non-NFC text rejected), DocumentRef(source_id, id, path, metadata), Nabu::ValidationError, Document plain class with <</append + Enumerable in sequence order, dup urn/sequence rejected; license_class closed enum exposed as SourceManifest::LICENSE_CLASSES.
2026-07-03 · P1-2 · 1ffe988 · Adapter contract: abstract Nabu::Adapter (fetch/discover/parse + self.manifest, instance manifest delegator, NotImplementedError names class+method); AdapterConformance module (hooks conformance_adapter/conformance_workdir, optional conformance_expected_source_id) asserting manifest validity, discover→parse round-trip, cross-set urn uniqueness, two-pass urn stability, NFC/non-empty passages, license class; hand-written TestAdapter rig + meta-tests running broken variants (dup/unstable urns, empty doc, wrong manifest) through anonymous test classes.
2026-07-03 · P1-3 · d924896 · Catalog schema + Sequel models: forward-only migration 001 (sources/documents/passages/provenance/enrichments/runs) with license_class/license_override/run-status CHECKs (enums hardcoded, no app dep), unique+composite indexes, FKs; Nabu::Store.connect/migrate!/setup! (FK pragma on; models bound to Sequel::Model.db and rebound via set_dataset so each store test gets a fresh in-memory db); namespaced Store::* models with associations; store_test_db helper.
2026-07-03 · P1-4 · a9bea34 · Store::Loader + LoadReport: canonical content hashing (netstring field encoding, nil marker, recursively key-sorted JSON) in Store::ContentHash; upsert-on-urn per source with skip/revise(+journaled old_sha)/withdraw/restore provenance semantics; per-document transactions isolate quarantine (ParseError) and constraint failures; full-sync withdrawal sweep in its own transaction; sequence parking makes passage reorders safe against the unique index.
2026-07-03 · P1-6 · 4ccae61 · SourceRegistry (config/sources.yml → validated Entry Data with lazy adapter_class resolution + manifest delegation + sync_source! upsert preserving db-owned enabled/last_sync_*); Store::RunRecorder (running→succeeded/failed runs row, clock seam, LoadReport#withdrawn→withdrawn_count, failure record durable); StatusReport + implemented `nabu status` (aligned per-source enabled/policy/live-counts/last-run, graceful missing-registry/db); Config#sources_path + #catalog_path; ran before P1-5 by design — rebuild consumes the registry.
2026-07-03 · P1-5 · 852d3d8 · Nabu::Rebuild + `nabu rebuild [--dry-run]`: parse-only replay of canonical/ into a fresh catalog db (drop file → migrate → per-source sync_source! + RunRecorder-wrapped Loader#load_from full:true); missing/empty canonical dir skipped (no run row), disabled sources still replayed, errored>0 surfaced as warnings; enrichment replay left as no-op hook (arch §6); Store.connect now accepts a bare file path (fixes status' open_catalog too). Reproducible-modulo-ids acceptance + skip/disabled/warning/dry-run/runs-row tests.
2026-07-03 · gate · — · Phase 1 gate: Fable review pass — 182/679 green, lint clean; decisions recorded: Document#metadata not persisted until an adapter needs it; minitest 6 has no stub/Mock (swap-singleton pattern is the house convention); Store.setup! global model binding is a known constraint for Phase 2's SyncRunner. Drive-by: checkout@v4→v5.
2026-07-03 · P2-1 · 1068532 · Perseus fixtures fetched per owner-approved plan: 4 whole editions (HH 13/14 flat-line grc, 2 John nested chapter/verse grc, Ausonius Genethliacon structural-div lat) + 7 __cts__.xml, CC BY-SA 4.0, strict-XML validated; README records URLs/license/structure notes.
2026-07-03 · P2-2 · 37786ae · Nabu::Adapters::EpidocParser: single-pass Nokogiri::XML::Reader (no DOM); citation scheme from CTS cRefPatterns (most capture groups = deepest, leaf element from replacementPattern xpath, div counts as citation level only when @subtype names a cRefPattern unit — keeps Ausonius' structural section div out of urns); note-dropping, lb/pb/milestone → space, whitespace collapse, NFC + re-NFC after downcase; ParseError on malformed XML/missing cRefPattern/urn mismatch/zero passages; 17 tests against the 4 real fixtures.
2026-07-03 · P2-3 · 71fbe64 · Nabu::Adapters::Perseus composing EpidocParser + PerseusDL repo layout: discover walks data/<tg>/<work>/, matches <tg>.<work>.perseus-grc<ver>.xml (skips perseus-eng* translations), keeps highest version per work, mints urn:cts:greekLit:… + resolves title from work __cts__.xml (tiny targeted DOM, prefer xml:lang=eng, urn-tail fallback when missing), sorted-by-urn Enumerator; parse delegates to EpidocParser; fetch = git clone --depth 1 / pull --ff-only via Nabu::Shell, returns HEAD sha String, Shell::Error→FetchError. Namespace-parameterized class: Perseus IS greekLit (no-arg .new + class .manifest for the registry), latinLit will be a 1-line PerseusLatin subclass overriding NAMESPACE. Registered perseus-greek (enabled:false, sync_policy:live) in config/sources.yml; two CLI empty-registry tests repointed to an isolated empty registry. Conformance + 20 source-specific tests (fetch against local tmpdir git repos, no network).
2026-07-03 · P2-4 · 381dca1 · Nabu::SyncRunner + `nabu sync <slug>|--all [--parse-only] [--force]`: restored the arch §3 fetch contract (new Nabu::FetchReport value; Perseus#fetch now returns it instead of a bare sha String) and wired the arch §8 withdrawal circuit breaker — before any load, predicts withdrawal as (existing non-withdrawn urns − discovered ref ids) and raises Nabu::SyncAborted (writing nothing) when >20% (strict >), --force overrides; RunRecorder now records status "aborted" (not "failed") for SyncAborted. sync <slug> is explicit/unconditional (disabled synced anyway with a note); --all runs only enabled+live sources (manual/frozen excluded), one source's failure isolated. On success updates sources.last_sync_at/last_sync_sha (parse-only skips fetch, keeps prior sha). Fixed latent P1-6 bug exposed by re-sync: SourceRegistry#sync_source! returned nil when Sequel #update no-ops on an unchanged row → now returns the row explicitly.
2026-07-03 · gate-prep · — · Phase 2 gate prep: Fable review pass — 232/1229 green, lint clean; README refreshed (sync commands); notes: breaker prediction assumes ref.id == document.urn (add conformance assertion in Phase 3); P2-4 fixed latent sync_source! nil-return on unchanged rows. PR opens after P2-5 (first real sync) completes the phase.
2026-07-03 · P2-6 · e9c7038 · Live progress for sync/rebuild (owner feedback: first real sync ran ~4 min silent): Shell.stream (popen2e, merged output forwarded per \n-or-\r line so git --progress overwrites forward live, same Shell::Error semantics on nonzero, run() untouched); Adapter#fetch contract gains progress: kwarg (nil-safe), Perseus#fetch streams git clone/pull --progress to it when given and keeps the quiet Shell.run path otherwise; Loader#load/#load_from gain on_document: tick (called per loaded OR quarantined doc with running processed+errored counts); SyncRunner/Rebuild thread an optional Nabu::ProgressReporter (Data of two nil-safe procs) down to fetch+loader while staying print-free; CLI owns all tty/formatting — $stderr tty: raw git stream + \r-updating "loading… N docs (E quarantined)" counter, non-tty: one line per 100 docs, final counts stay on $stdout. Parse-only smoke check on local canonical/perseus-greek: 744 skipped / 25 quarantined in ~22s, per-100 ticks emitted.
2026-07-03 · P2-5 · 42e2af1 · First real sync, owner-run and signed off: perseus-greek cloned (e37eed2), 744 docs / 238,525 passages loaded, 25 quarantined (upstream @n gaps, journaled); spot-checks verified (NFC Greek, multi-level urns); enabled: true flipped, 02-sources.md status LIVE.
2026-07-03 · gate · — · Phase 2 gate complete: all six packets done (incl. owner-feedback P2-6 progress + conventions.md); 245/1267 green, lint clean; perseus-greek LIVE; PR opened.
2026-07-03 · P3-0 · f0254a3 · Promoted the sync breaker's identity assumption into AdapterConformance: new test_conformance_ref_id_is_the_document_urn asserts parse(ref).urn == ref.id for every discovered ref (a diverging urn would let the breaker under-count withdrawals), doc-comment check list updated, + MismatchedUrnAdapter meta-test proving a violating variant fails exactly that assertion while uniqueness/stability/non-empty still pass. Aligned TestAdapter so its DocumentRef id IS the urn (was filename); rippled honest id updates into loader_test/rebuild_test rigs. Perseus already satisfied it by construction.
2026-07-03 · P3-1 · 14dc4fd · Phase 3 fixtures per owner-approved plan: First1KGreek (9 whole XML incl. conditional tlg2959, both probes 200), UD CoNLL-U head-50 for gothic/greek-proiel/sanskrit-vedic/latin-ittb (ITTB +mwt: both MWT sentences already in head, 0 appended), PROIEL cic-off-head15 (4 divs/18 sent) + TOROT peter (whole) & zogr-head40 (2 divs/62 sent) via PROIEL DOM surgery, DDbDP 3 whole EpiDoc; all strict-XML/CoNLL-U validated; 5 READMEs record URLs/sizes/licenses (Gothic/Greek 3.0-prose-vs-4.0-link inconsistency verbatim, ITTB/PROIEL/TOROT nc, Sanskrit BY-SA 4.0, DDbDP BY 3.0); suite 248/1284 green.
2026-07-03 · P3-2 · 9af9955 · First1KGreek adapter as a thin SUBCLASS of Perseus (which was already extension-shaped): generalized Perseus's original-language selection into data-driven #edition_slug_pattern (named :version capture) + #edition_version (slug→token-or-nil) + #version_key (numeric part then letter suffix, so grc2 < grc2a — grc2a wins), replacing the monolithic edition_filename_pattern with a FILENAME split + acceptance hook; Perseus behaviour byte-identical (perseus-grcN only, 22/203 green unchanged). First1kGreek overrides just manifest (id first1k-greek, "Open Greek and Latin — First1KGreek", CC BY-SA 4.0/attribution, OGL repo url, epidoc) + slug rule (accept ANY *-grcN[a-z]? tail regardless of family: 1st1K-/opp-/perseus-; translations …-eng/ger/lat/mul lack -grc, skipped); NAMESPACE stays greekLit so urns + grc language tag inherit free. Tests: AdapterConformance on P3-1 greekLit fixtures + discover finds exactly the 3 editions across two slug families, titles from __cts__.xml (lat-only here), version-preference incl. letter suffix (tmpdir grc1/grc2/grc2a → grc2a), Seikilos parse (Σείκιλος) and subtype="work" refsDecl path (tlg1126, Ἀννουβίων). Registered first1k-greek (enabled:false, live). Suite 269/1384 green, lint clean.
2026-07-04 · P3-3 · 82de3e1 · ConlluParser (second parser family, standalone sibling to EpidocParser) + UniversalDependencies adapter. Parser: streaming line-based CoNLL-U (each_line, never slurps — String source = path, IO source iterated directly), sentence-block = passage terminated by blank line; passage urn = <doc-urn>:<sent_id> (missing `# sent_id` → ParseError, it's mandatory in UD); text = `# text` NFC (else reconstructed from FORM honoring SpaceAfter=No), text_normalized = NFC(downcase); annotations = {"tokens"=>[{id,form,lemma,upos,xpos,feats,head,deprel,misc}], "source"=>`# source`} — DEPS column dropped, `_`→absent key so MWT range lines fall out to just id/form(/misc) naturally, empty nodes tolerated; ≠10 tab columns → ParseError naming file:line. Adapter: TREEBANKS map (gothic-proiel/got, greek-proiel/grc, sanskrit-vedic/san, latin-ittb/lat), one DocumentRef per *.conllu file, urn:nabu:ud:<treebank>:<file-stem> (FROZEN); discover skips unknown subdirs silently; parse delegates; fetch clones/pulls each treebank into <workdir>/<slug> reusing Perseus git pattern — FetchReport.sha = LAST treebank's HEAD (treebanks version independently, no single sha describes the set), notes = full "<slug>=<sha>" per-repo summary. manifest id ud / license_class nc (most-restrictive across the set) / parser_family conllu. Registered ud (enabled:false, sync_policy:manual). Tests: parser unit (gothic 50 passages, first urn …:46928 + real text + 6 tokens + spot lemma/upos/xpos/head/misc, latin MWT 14-15/essetque range line present w/ only id+form, greek NFC, malformed-line + missing-sent_id + empty-source errors via StringIO surgery) + AdapterConformance + discover-4-refs + registry round-trip + local-git fetch. Suite 295/2518 green, lint clean.
2026-07-04 · P3-4 · 5715a01 · ProielParser (third parser family, streaming sibling to Epidoc/Conllu) + PROIEL adapter. Parser: Nokogiri::XML::Reader single pass (sources reach ~29 MB, never DOMed — code-structure test enforces it), reacts only to <source>/<sentence>/<token> so the whole <annotation> dictionary block streams past unslurped (token attrs already carry the terse codes; the dicts only label them). sentence = passage, urn = <doc-urn>:<sentence/@id> verbatim (stable upstream db keys, non-contiguous — 88119/88118 interleaved among 86xxx — never renumbered); sequence = kept order from 0. text = per token presentation-before + form + presentation-after (PROIEL's surface encoding — spacing/punct live in the attrs, not element boundaries), NFC; text_normalized = NFC(downcase). Empty tokens (empty-token-sort, no form) contribute nothing to text but stay in annotations. annotations = {"tokens"=>[{id,form,lemma,part_of_speech,morphology,head_id,relation,citation_part,information_status,empty_token_sort} nils dropped], "citation"=>first token's citation-part, "status"=>sentence/@status}; <slash> secondary edges ignored. Cross-checks → ParseError: <source id> ≠ urn tail, <source language> ≠ caller language (caller wins but disagreement surfaced), missing sentence/@id (names file + document-order position), zero sentences. Adapter: manifest id proiel / name "PROIEL Treebank" / license_class nc / upstream proiel-treebank / parser_family proiel; discover walks <workdir>/*.xml, peeks each <source> header via Reader (id/language/<title>, stops at first <div>), one DocumentRef per file urn:nabu:proiel:<source-id> (== parse urn, conformance identity), skips files w/o <source> defensively; parse delegates; fetch = single-repo Perseus git clone/pull + FetchReport. Registered proiel (enabled:false, sync_policy:frozen — repo frozen at 20180408, successor syntacticus-treebank-data noted). Tests: parser unit against real fixture (18 passages, first urn …:86000, text "Quamquam te, Marce fili…"→"orationis facultate.", presentation-before "(nihil", lemma quamquam/pos G-/citation 1.1, empty-token kept-but-textless, dropped-nil head-id, language/source-id/missing-id/zero-sentence/malformed errors, IO+streaming checks) + AdapterConformance + discover-1-ref + skip-non-treebank + local-git fetch + registry round-trip. Suite 330/2728 green, lint clean. Note for P3-5: TOROT reuses ProielParser unchanged; its adapter overrides manifest (id/license) + language mapping (orv/chu, per-source), and its discover must tolerate <div> w/o @id (parser already ignores div attrs) and sparse <source> headers (title/language may be absent — peek must not assume them); heavy empty tokens are already handled by the shared parser.
2026-07-04 · P3-5 · 9a13204 · TOROT adapter as the thinnest subclass of the phase: Nabu::Adapters::Torot < Proiel overriding ONLY the manifest (id torot, "TOROT — Tromsø Old Russian and OCS Treebank", CC BY-NC-SA 3.0 per-source headers/no LICENSE file → license_class nc, torottreebank/treebank-releases, parser_family proiel). Same PROIEL 2.1 XML as proiel-treebank, so discover/peek/parse/fetch inherit wholesale. DELIBERATE identity decision (documented in the adapter header): TOROT mints under the SHARED urn:nabu:proiel:<source-id> namespace, not urn:nabu:torot: — source ids (peter, zogr) come from the shared PROIEL id-space and the format is byte-identical; cross-source uniqueness holds via distinct source ids, cross-adapter collision is acceptable-impossible (disjoint upstream id-spaces by convention), and if that ever broke the loader's source-scoped (source_id, urn) upsert (P1-4) contains it — peter's source_id is "torot", not "proiel". BASE FIX (Proiel ported all but identity): the base referenced its lexical MANIFEST constant in three identity spots — discover source_id, fetch repo_url, fetch error prefix — so a Torot instance reported source_id "proiel" and would clone proiel-treebank; routed all three through the overridable #manifest (== self.class.manifest) so subclasses carry their own identity. The urn namespace literal stays "proiel" ON PURPOSE (shared id-space) with an inline note. Regression-covered: torot conformance/discover assert source_id "torot"; test_repo_url_targets_the_torot_repo pins the fetch URL. Fixtures reality confirmed: peter whole (11 sentences, orv, title "Correspondence of Peter the Great") + zogr-head40 (62 sentences, chu, no title asserted); real snippets asserted — peter first sentence starts "Вселюбезенейшей", zogr first sentence (id 75108) contains "тъ васъ крьститъ"; empty token 2340186 (empty-token-sort="V", no form) in peter s.219339 retained in annotations, absent from surface text; README.md ignored by the *.xml glob. Registered torot (enabled:false, sync_policy:manual — TOROT still gets occasional releases, unlike frozen proiel). Suite 348/3164 green, lint clean.
2026-07-04 · P3-6 · 9b40218 · DdbdpParser (fourth parser family — Leiden/documentary EpiDoc, NOT CapiTainS) + Papyri.info adapter. Parser: streaming Reader single pass; the Leiden text-extraction policy is FIXED here and documented exhaustively in the file header (conventions.md §5 updated from "deferred" to the print-practice rule): text = what a print edition's main text reads — KEEP lem (rdg drops), reg (orig drops), add (subst's del drops; standalone add too), supplied (restorations ARE readable text), unclear, expan INCLUDING ex, num content; DROP rdg/orig/del (nested del wholesale)/note/figure; handShift → annotation only; <g> glyphs (middots) contribute nothing (a middot inside <reg>de<g/>monstrabit must not tear the regularized word apart — policy decision beyond packet spec, documented); gap → single "[…]" marker regardless of quantity (length data → annotations; a search hit must not match across a lacuna as contiguous), gap children (desc/certainty) drop. Keep/drop handled by a drop-depth guard: a dropped subtree drops EVERYTHING (supplied inside orig counts nothing, gap inside del marks nothing, lb inside a dropped branch is NOT a line boundary — bgu.1.100's triple lb n=3 in reg/orig/del mints line 3 exactly once, from the kept branch). Passage = the LINE (papyrology's citable unit): urn = <doc-urn>[:<textpart-path>]:<lb n> (…:r:3 only when div[type=textpart] exists, else …:3); break=no needs no special text handling in the line model; xml:space=preserve honored during accumulation, house collapse/strip/NFC/downcase-reNFC after (tension documented); empty-after-extraction lines skipped, zero lines → ParseError. Annotations lean: {"leiden"=>{gaps[{reason,quantity,unit}],supplied_chars,unclear_chars(grapheme counts),hands},"languages"=>[inline xml:lang scopes ≠ doc language, mapped]}. Identity: <idno type=ddb-hybrid> mints urn:nabu:ddbdp:<hybrid ;→:> (FROZEN; empty segments survive — "c.epist.lat;;10" → …:c.epist.lat::10), cross-checked against caller urn; missing hybrid/edition-div/lb@n/textpart@n/language mismatch (la→lat map, subtags survive) → ParseError. Adapter: manifest papyri-ddbdp / "Papyri.info — Duke Databank of Documentary Papyri" / CC BY 3.0 attribution / idp.data / family ddbdp; discover globs DDB_EpiDoc_XML both nested (bgu/bgu.1/) and flat (c.epist.lat/) collections, Reader-peeks header (ddb-hybrid/HGV/TM idnos, title, edition xml:lang, stops at edition div), skips non-DDbDP xml defensively, HGV/TM ride in ref metadata for future cross-linking; parse delegates; fetch = single-repo git clone --depth 1/pull --ff-only (idp.data is huge; depth-1 matters) + FetchReport. Registered papyri-ddbdp (enabled:false, sync_policy:manual). Tests: all expected strings derived from fixture bytes and cross-checked against an independent DOM reference extraction (scratch, not committed); bgu.1.102 9 lines w/ line-2 hand-worked in a comment + app/lem, gap marker mid-word fusion "Αἴλιος […]θως", hands m2; bgu.1.100 12 lines, πε/πρακέναι subst split; c.epist.lat.10 lat, 11 lines, textpart urns r/v, expan expansions, standalone add kept, del-with-gap dropped; surgery tests for inline-language annotation, empty-line skip, and all error paths; AdapterConformance + discover 3 refs w/ frozen urns + metadata + local-git fetch + registry round-trip. Suite 393/3466 green, lint clean.
2026-07-04 · gate · — · Phase 3 gate: all 7 packets done; 393/3466 green, lint clean. Gate review notes: Leiden policy fixed + conventions.md updated in-change; :: empty urn segment (c.epist.lat;;10) accepted as frozen-rule consequence, documented; Proiel base identity generalization (P3-5) reviewed — correct; six adapters registered, five awaiting first sync.
2026-07-04 · P4-1 · 8ffadb0 · FTS5 fulltext index + Nabu::Store::Indexer. Separate db/fulltext.sqlite3 (arch §2 one-file-per-concern; catalog stays small/precious, index disposable): Config#fulltext_path + Store.connect_fulltext (reuses connect's scheme-less-path handling, no FK pragma). Indexer.rebuild!(catalog:,fulltext:) drops+recreates ONE FTS5 virtual table passages_fts(text_normalized, urn UNINDEXED, passage_id UNINDEXED, tokenize unicode61 remove_diacritics 2) — created imperatively here, NOT a numbered migration (migrations own the catalog only; the index is derived-of-derived, rebuilt at will; the raw db.run heredoc DDL is the sole sanctioned SQL string — virtual-table DDL has no Sequel dataset API, query paths stay on datasets), bulk-inserts (each_slice 2000 multi_insert, one txn) every passage that is live AND under a live document (P1-4 two-level visibility, joined through documents via raw datasets independent of global model binding), returns count. Auto-wired: SyncRunner reindexes after each successful (non-aborted) sync AFTER the RunRecorder block (corpus-wide, not per-source — an indexing failure is its own error, never a falsified run row; parse-only reindexes too), Rebuild reindexes once after all sources replayed (drops the fulltext file too); both carry the count in their Outcome/Result and the CLI prints "indexed N passages". KEY FINDING/DECISION: SQLite 3.53.2's unicode61 remove_diacritics 2 does NOT fold precomposed polytonic Greek (verified: ά/ῆ/ἀ/ῃ all unfolded; it strips only a few *combining* marks and only in NFD — never perispomeni/breathings/iota-subscript), and the corpus stores text_normalized as NFC, so the tokenizer ALONE cannot deliver Greek diacritic-insensitive search. Resolution (in scope, honoring arch §3 "text_normalized = diacritic-folded search form"): the Indexer folds via new Nabu::Normalize.fold_diacritics (NFD → drop \p{Mn} → NFC) before indexing; the catalog's text_normalized is untouched (adapters still only lowercase — deeper per-language fold at the adapter boundary is a future packet needing a corpus reload). P4-2 MUST fold the query with the same function (MATCH via db[:passages_fts].where(Sequel.lit("passages_fts MATCH ?", Normalize.fold_diacritics(q))); snippet() available as Sequel.function(:snippet, Sequel.lit("passages_fts"), …) but returns the FOLDED form, so display should render the catalog's pristine text joined via passage_id). Tests: indexer unit (live count, withdrawn-passage + withdrawn-document exclusion, idempotent no-dupes, μῆνιν findable by μηνιν, passage_id linkback) + fold_diacritics unit (polytonic/Latin/no-op/NFC) + SyncRunner & Rebuild & CLI integration (index populated, parse-only reindexes, aborted has no count). Suite 407/3503 green, lint clean.
2026-07-04 · P4-2 · 321beec · `nabu search` + Nabu::Query::Search (new lib/nabu/query/ layer, arch §2). Search.new(catalog:,fulltext:).run(query, lang:, license:, limit:20) → array of Result(urn, language, text, snippet, document_title, license_class). Pipeline: fold+downcase the query with the SAME Normalize.fold_diacritics the Indexer used (the fold-both-sides contract — else accented↔unaccented misses) → FTS5 MATCH (Sequel.lit "passages_fts MATCH ?" parameter-bound, the sole user-text-in-SQL point) ordered by bm25(passages_fts) with a generous inner limit (limit*10) → collect passage_ids in rank order → TWO-STEP id join into the catalog (no ATTACH: index is a separate SQLite file) through passages⋈documents⋈sources applying NOT passage.withdrawn AND NOT document.withdrawn + optional language equality + optional license equality on coalesce(documents.license_override, sources.license_class) (P1-3 override wins) → reassemble in FTS rank order, trim to limit, attach snippet. DECISIONS: (1) license filter is EXACT-CLASS in v1 (--license open ≠ "at least as open"); permissiveness ordering deferred, documented in the class header. (2) snippet() is the FTS highlight over the FOLDED index column so its accents are STRIPPED — it marks WHERE the match is, not the source spelling; the pristine accented text rides in Result#text for display, and the CLI footer labels the highlight "(highlights are diacritic-folded)". (3) bm25/snippet ride as literal SQL fragments (FTS auxiliary functions, no Sequel dataset API — same sanctioned-exception class as the Indexer DDL); no user input in them. CLI: search QUERY [--lang X] [--license CLASS] [--limit N] — validates --license against SourceManifest::LICENSE_CLASSES up front (unknown → exit 1 listing the enum), empty query → exit 1; missing/never-built index (fulltext file absent OR passages_fts table absent, via open_fulltext helper) → "no index — run nabu sync or nabu rebuild" exit 1; zero hits → "no matches" exit 0; hits render "urn [lang]" + folded-snippet line + "N hit(s)" footer. Removed search from the not-implemented CLI stub loop. Tests: Query::Search unit (10 — accented match returns pristine text, UNACCENTED-finds-accented fold proof, snippet marks folded token, lang filter, exact-class license filter, license_override demotes out of open + reports "nc", withdrawn-passage exclusion, limit cap, bm25 ranks denser matches first, no-match empty) + CLI (unaccented Greek happy path via real parse-only sync building both dbs, zero-hits exit 0, bad-license exit 1, no-index hint exit 1). Suite 420/3535 green, lint clean. For P4-3/P4-4: reuse Query::Search or its two-step-join + fold pattern; Result shape is (urn, language, text, snippet, document_title, license_class); snippet is FOLDED (accents stripped) so golden-query assertions should match on urn membership, not snippet spelling.
2026-07-04 · P4-3 · 439aebb · `nabu show` + `nabu export` + Nabu::Query::Show/Export (query layer, same construction as Search: catalog handle in, value objects/serialized lines out; raw datasets independent of global model binding; effective-license coalesce(documents.license_override, sources.license_class)). SHOW.run(urn) probes passages FIRST then documents (urns globally unique, a passage urn never equals its document's), returns nil for unknown. Passage urn → PassageResult(urn, language, sequence, revision, withdrawn, text, document_urn, document_title, source_slug, license_class, provenance[ProvenanceEvent(event,tool,at) ordered :at,:id — chronological, loader's "loaded" surfaced). Document urn → DocumentResult(header + passages[PassageLine(urn,text,withdrawn)] in sequence order). DECISION (documented in the class header): Show DELIBERATELY reveals withdrawn passages AND documents, flagged — it is an inspection tool onto what the store actually holds (nothing is hard-deleted), not a corpus view; the two-level visibility filter belongs to Search/Export, not the inspector. SQLite 0/1 booleans normalized to real true/false via truthy? for the value objects. EXPORT.run(format:, lang:, license:) returns a STREAMING Enumerator (Enumerator.new wrapping dataset.each — the corpus is ~238k passages, never materialized; .first(n)/.lazy pull only what's taken) of already-serialized lines over live passages (passage AND document non-withdrawn), same effective-license exact-class + lang filters as Search, ordered by passages.id (index-backed, no buffered sort). plain = text with internal newline runs collapsed to a single space, trimmed (one passage per line); jsonl = JSON.generate {urn,language,text,text_normalized,annotations} where annotations is JSON.parse(annotations_json) so the line carries a real nested object, not a double-encoded string (NULL/blank → {}). CoNLL-U NOT implemented (backlog: deferred to the enrichment phase — needs the token model). CLI: show URN (empty urn / no catalog / urn-not-found → exit 1; passage vs document rendered distinctly, withdrawn tagged "(withdrawn)"); export --format plain|jsonl [--lang X] [--license CLASS] streams line-by-line to $stdout.puts as it iterates (no join), --format required, --format conllu → "deferred until the enrichment phase" exit 1, unknown format → exit 1 listing plain/jsonl. validate_license! generalized (dropped the "search:" prefix) and reused by export; removed the now-empty not-implemented CLI stub loop (show was its last member) and the not_implemented! helper. Tests: Query::Show (5 — passage happy path incl loaded-provenance surfaced, provenance chronological across a later-timestamped event, document with ordered passages, unknown urn nil, withdrawn passage shown+flagged) + Query::Export (7 — plain newline-strip, jsonl keys+parsed-annotations-Hash round-trip, empty-annotations-is-object, lang filter, exact-class license filter w/ override demotion, withdrawn passage+document excluded, streaming: returns Enumerator + first(2) correct over 50-row seed) + CLI (show passage/document/unknown, export plain 3-line, export jsonl valid JSON objects w/ annotations Hash, conllu-deferred, bad-license) — seeded via real parse-only sync so loader provenance is production-shaped. Suite 438/3610 green, lint clean. For P4-4: Query::Show/Export take a catalog handle only (no fulltext); export lines are final serialized strings (plain text / JSONL) suitable for golden-output diffing; Show value objects carry real booleans + chronological provenance.
2026-07-04 · P4-4 · 0a626d4 · Golden-query smoke suite + `nabu verify` (last Phase 4 packet). GOLDEN: test/golden/golden_queries.yml (data: {query, expect_urn, lang?, note}) + test/golden_test.rb iterates it. One full fixture corpus built ONCE at class level (Dir.mktmpdir, Minitest.after_run cleanup) — all SIX adapters loaded via Loader#load_from(full:true) from their test/fixtures workdir into one file-backed catalog + Indexer.rebuild! — so the suite doubles as the cross-adapter integration test (six adapters, one store, one FTS index; a urn collision or indexer/search seam break fails here). One test per YAML entry asserts Query::Search#run(query, lang:) results INCLUDE expect_urn (membership, not rank/snippet — snippet is diacritic-folded per P4-2). 8 queries, every expectation derived from actual fixture bytes: ἠύκομον→tlg0013.tlg013.perseus-grc2:1 (Perseus grc, Homeric Hymn 13), δημητηρ→…tlg013…:1 (THE FOLD: unaccented query finds accented "Δημήτηρ’"), philosophandi→proiel:cic-off:86006 (PROIEL lat Cicero De Off.), þiudangardja→ud:gothic-proiel:…:46938 (UD got, non-Latin þ through CoNLL-U #text), крьститъ→proiel:zogr:75108 (TOROT chu Zographensis), Петрушъка→proiel:peter:219390 (TOROT orv Peter letter — proves peter/zogr don't collide in the shared urn:nabu:proiel: namespace), Αἴλιος→ddbdp:bgu:1:102:7 (DDbDP gap-adjacent: the word sits immediately before a <gap>; finding it as its own token proves Leiden extraction did NOT fuse "Αἴλιος[…]θως" across the lacuna), Σείκιλος→tlg2139.tlg001.1st1K-grc1:1 (First1kGreek, no lang → optional-lang path). Guard tests: ≥6 queries spanning chu/got/grc/lat/orv, all six sources loaded index non-empty. VERIFY (arch §8, cronnable bitrot/tamper): lib/nabu/verify.rb — Nabu::Verify.new(config:,registry:,db:).run(progress:)→Result{outcomes[SourceOutcome{slug,verified,issues[DocumentIssue{urn,canonical_path,kind,detail}]}], skips[Skip{slug,reason}]}, #clean?/#issues. KEY DESIGN: catalog content_sha256 is over the PARSED model (Store::ContentHash.document over urn/language/title/canonical_path + per-passage hashes), NOT raw file bytes — so verify RE-PARSES each file through the source's adapter (discover→parse, reusing the loader's exact DocumentRef incl. metadata — a hand-built ref would lose title/language and false-mismatch) and re-hashes, comparing recomputed vs stored. Mirrors Rebuild's shape (registry+config+db in, result out, streaming per-source), READ-ONLY (only SELECTs the catalog via raw datasets independent of model binding; only reads+parses canonical/). Per non-withdrawn catalog doc of each source with a canonical dir: :ok (hashes equal) | :mismatch (differ; detail{stored,recomputed}) | :missing (file gone) | :unparseable (Nabu::ParseError now, or present-but-no-longer-discoverable). Extra canonical files not in catalog are ignored (sync drift, not verify's business); withdrawn docs skipped; source with no canonical dir → Skip(:no_canonical) with a note (never synced). ref.id == document.urn holds for all six adapters, so ParseError attributes to the right urn. CLI `nabu verify`: per-source "OK slug (N documents verified)" or "FAILED slug" + itemized MISMATCH/MISSING/UNPARSEABLE lines, then skips, then verdict; clean → exit 0 (returns normally), any issue → report to stdout then raise Thor::Error (exit 1) with a summary to stderr; no catalog → hint + exit 1. TestAdapter gained a malformed-input guard (empty/whitespace-only file → ParseError, "the same unparseable signal a real adapter emits") so the verify unparseable path is exercisable — safe (no existing test writes empty .txt). Tests: verify_test.rb (clean pass; corrupted bytes→mismatch on exactly that urn, sibling still ok; deleted file→missing; empty file→unparseable; withdrawn doc's missing file NOT flagged; no-canonical-dir source skipped) + CLI (clean OK/exit 0, corrupted MISMATCH+FAILED/exit 1, no-catalog hint/exit 1). Suite 457/3687 green, lint clean.
2026-07-04 · gate · — · Phase 4 gate: 4 packets done, 457/3687 green, lint clean. Notes: SQLite remove_diacritics doesn't fold precomposed Greek — fold happens index-side + query-side (architecture §3 amended honestly; adapter-boundary per-language folding = future reload packet); golden suite doubles as six-adapter integration test; TestAdapter empty-file guard reviewed OK; Phase 5 (source health) outlined at owner request.
2026-07-04 · syncs · — · First real syncs signed off: first1k-greek (+1054/!37), ud (+12/!0), torot (+40/!0), proiel (+12/!0) → enabled: true, inventory LIVE. papyri-ddbdp (+49060/!21641) held: 12,288 duplicate-urn quarantines from mid-document line-number restarts (P5 fix, frozen-urn-safe) + 9,351 text-less stubs (honest). Corpus index: 1,252,324 passages.
2026-07-04 · P5-1 · b31fbb4 · Restart-aware DdbdpParser urn minting (the 12,288 duplicate-urn quarantine class from the 2026-07-04 first sync). Citation shape extended: urn = <doc-urn>[:<textpart-path>][:b<k>]:<lb n> — the implicit restart block b<k> (k ≥ 2), shaped like a synthetic textpart, sits between textpart path and line number. Blocks open on URN COLLISION, not numeric comparison: when a line's default suffix equals one already minted in this document (within the same textpart context — counter resets at every textpart boundary), the next block opens (b2, b3, …) and the line re-mints inside it, looping until unique; the first block never carries a label. Collision-triggered IS the frozen-urn safety argument: any document that parsed cleanly pre-P5-1 had zero suffix collisions by definition, so the mechanism provably never engages and its urns stay byte-identical (a monotonicity heuristic could fire on loaded docs with odd-but-unique @n like "12/13" and silently re-mint frozen urns); restart docs never entered the catalog, so their urns were free to define. Trade-off documented in the parser header: a restart beginning at a number the previous block never reached opens its block only at the first collision (urns unique+stable, merely blind to that boundary). VERIFIED EMPIRICALLY (read-only vs live catalog): 1,000 random loaded papyri docs re-parsed → 0 urn mismatches; ALL 12,287 duplicate-urn quarantined docs re-parsed → 12,287 recover clean with unique urns, 0 still failing. Exemplar aegyptus.89.240 (flat <ab>, lb n=1 twice, lb n=11 twice) mints :1, :b2:1…:b2:11, :b3:11 (13 passages). Fixtures: aegyptus.89.240.xml + text-less stub chrest.wilck.101.xml copied whole (small; no trimming needed) from local canonical snapshot into test/fixtures/papyri-ddbdp/ + README w/ provenance; pre-existing ddbdp fixtures/conformance workdir untouched. Tests: restart parse w/ full expected urn list + uniqueness, text spot-checks, stability across two parses, GOLDEN regression asserting all three pre-P5-1 fixture urn lists byte-identical, stub still quarantines w/ clear message naming the file. Also: .rubocop.yml now excludes canonical/** — rake lint was ALREADY broken on this box (first torot sync pulled upstream .rb tooling into gitignored canonical/), not this diff. STUB-CLASS AUDIT (9,351 "no citable lines"; all 9,351 machine-classified from the quarantine journal + ~20 hand-inspected): 9,220 literally empty <ab/> + ~40 whitespace-only <ab> + 2 self-closing edition divs — genuine cross-reference stubs (header <ref type="reprint-in"> names the republication), correctly quarantined; 33 note-only + a few figure-only (drawings) — genuinely text-less. RECOVERABLE SUBCLASS REPORTED (future packet, deliberately not fixed here): ~40 docs whose ENTIRE edition is wrapped in <del rend="cross-strokes"|"slashes"> (ancient cancellation — crossed-out but fully legible, edited text; print editions read it in ⟦⟧; e.g. cpr.6.3, bgu.1.179, apf.59.139) plus a few whole-doc <del rend="erasure"> (o.claud.3.457) — the blanket drop-<del> policy erases the entire document; also o.trim.2.783-style oddities (line numbers as plain text "1.", no <lb>). Suite 462/3701 green, lint clean.
2026-07-04 · P5-2 · a565a39 · Retention contract: the canonical attic. Nabu::GitFetch = the ONE shared non-destructive git path (all six adapters; perseus/proiel/papyri via Adapter#git_fetch! one-liner, UD drives the prepare/guard/complete phases itself so ALL repos are guarded before ANY merges): git fetch (objects only) → mass-deletion breaker predicts from `diff --name-only --diff-filter=D --find-renames -z HEAD FETCH_HEAD` ∩ discover-ingestible files (>20% trips SyncAborted with the tree BYTE-UNCHANGED; --force proceeds) → copy doomed files to canonical/<slug>/.attic/<relpath> (first copy wins; per-attic .attic.json manifest records the FETCH_HEAD sha each file vanished at; .attic written to .git/info/exclude) → merge --ff-only. Renames are NOT scrapping (explicit --find-renames → R never D; content survives at its new path). Adapter#discover_with_attic (base class — adapters implement only discover) reruns discover against <workdir>/.attic, flags refs retained + retired_sha from the manifest, live urn beats attic duplicate (superseded, journaled once). Migration 002 documents.retired_upstream (NOT NULL default false); loader: retained ref → retired_upstream=true + provenance "retired" {upstream_sha} (params-less when no manifest — loader stays fetch-independent so rebuilds replay identically, sha rides in canonical via the manifest), reappearing live → "unretired"; retired stays withdrawn=false → search/export/index untouched, rebuild replays the attic (db=f(canonical), attic ⊂ canonical). SyncRunner threads --force into fetch and keeps its db-side guard as second line (now discover_with_attic, covers --parse-only + non-git); verify re-hashes attic docs; status shows retired=N; show labels "(retired upstream)". Docs: architecture §3/§5/§8, conventions §7 licensing (retained docs keep fetch-time license; local use only), CLAUDE.md anti-pattern (destructive fetch). Out of scope per packet: revised-passage old text journaled by sha only; attic guards upstream loss, not disk loss. Tests: GitFetch unit (attic/first-copy-wins/rename/byte-unchanged-abort/exclude/progress), retention E2E (sync→attic→retired→searchable→verify-clean→rebuild-survives; breaker abort tree-unchanged + force; unretire+superseded-once), adapter/loader/schema/show/status/perseus/UD additions. Suite 491/3867 green, lint clean.
2026-07-04 · P5-3 · 1bc2554+f282afd · Upstream probe `nabu health --remote`: no-clone, no-corpus-fetch liveness/drift/license check over EVERY registered source (enabled or not — a disabled source's upstream can still die). Structured as lib/nabu/health/remote_probe.rb (service object returning data; CLI only formats), injectable Shell (tests feed canned ls-remote) with WebMock for license fetches. (1) Liveness = `git ls-remote <url> HEAD` → :alive (+HEAD sha) / :gone (Shell::Error) / :moved (best-effort: git output looks like a 301/redirect — honest that git usually follows GitHub's rename silently so most renames read :alive against the new location); only :gone sets exit 1, :moved is a soft "stored URL stale" signal, detail carries git's first stderr line. (2) Drift = remote HEAD vs catalog last_sync_sha → :current / :behind / :never_synced (no row or no sha) / :unknown (not alive). (3) License drift (best-effort, never an error) = for github.com upstreams fetch raw.githubusercontent.com LICENSE|LICENSE.md|COPYING at the HEAD sha, sha256 vs a per-source baseline; first sight records it (:baseline_recorded, not a false alarm), differ → :changed "review upstream", match → :unchanged; non-github / no file / unreachable → :unchecked. UD MULTI-REPO DECISION (its manifest URL is the GitHub org, un-probeable as a repo → would be a permanent false gone): new Adapter.upstream_repo_urls (base = [manifest.upstream_url]; UD overrides with its 4 treebank repos) — probe EACH repo for liveness (a dead treebank caught individually; source :gone/:moved if ANY repo is, offenders named), but drift → :multi and license → :unchecked("multi-repo") because the catalog stores ONE last_sync_sha + ONE baseline per source (pins only the last treebank by UD's fetch contract); per-repo drift left to a future packet, documented in the probe header. BASELINE STORAGE: migration 003 sources.license_baseline_sha256 — runtime state like last_sync_sha (not derived from canonical), so rebuild-purity holds (rebuild drops it, next probe re-records); health --remote migrates the catalog on open so the column exists on pre-P5-3 dbs. CLI: `nabu health` WITHOUT --remote is the P5-5 stub (clear "local health checks arrive with P5-5" note, exit 0 — P5-5 replaces it); --remote prints one aligned row/source (slug, liveness, drift, license) + summary + trailing detail, exit 1 iff any upstream gone (table on stdout, Thor::Error summary on stderr). Tests: probe unit (alive+current, alive+behind, never-synced×2, gone+exit-flag, moved, license baseline-recorded/unchanged/changed/unchecked incl. COPYING fallthrough, multi-repo all-alive + one-gone, disabled-still-probed, upstream_repo_urls default) with FakeShell + WebMock; CLI (bare stub exit 0, all-alive exit 0, one-gone exit 1) with Shell singleton swap. Live db untouched (probe would write baselines + migrate — smoke-checked only the db-free bare stub). Suite 509/3921 green, lint clean.
2026-07-04 · P5-4 · a9ee370+5dc7cca · Fixture sentinel: per-source manifests + `rake fixtures:check|refresh` (lib/nabu/fixture_sentinel.rb; rake tasks are thin wrappers, network-CAPABLE but never run by the suite). Seven manifests (test/fixtures/<source>/manifest.yml, boring YAML, schema documented in perseus's header) built ONLY from the READMEs' recorded facts — no invented URLs: perseus/first1k/ddbdp/torot-peter are whole:true (byte-identical upstream copies), ud/proiel/torot-zogr are whole:false (documented trims of much larger files), papyri-ddbdp's two P5-1 quarantine exemplars are refetchable:false provenance:local-trim (copied from the local canonical sync; re-acquiring = full idp.data corpus sync, not a raw GET → recorded canonical-relative path + upstream repo, SKIPPED by check with the reason printed). NOTE: the packet named "six sources" but the papyri source owns TWO fixture dirs — ddbdp/ (P3-6 clean-parse, refetchable, drives papyri_test) and papyri-ddbdp/ (P5-1 local-trim) — so seven manifests; the synthetic conformance rig test_adapter/ (no README) is exempt. STANCE: check NEVER overwrites — for each refetchable entry it GETs to a tmp dir, byte-diffs whole files (differs=drift), fetches trimmed files for URL-liveness only (a trim can't byte-match its source), then re-runs the source's adapter test against the FRESH copies (only for all-whole sources: perseus/first1k/ddbdp — a count-based test against untrimmed upstream would false-fail, so trimmed sources leave adapter_test null); a failing fresh-copy adapter test IS the drift report (maintenance §6); exit nonzero on any differ/fetch-fail/test-fail, "clean" per source otherwise. Fixture-dir swap: NABU_FIXTURE_DIR (added to test_helper as Nabu::TestSupport.fixtures(source); replaces the fixtures ROOT, <root>/<source> mirrors the layout) wired into the three all-whole adapter tests' FIXTURES constant (default path byte-identical to before — no assertion weakened); the check shell-out sets it and runs `bundle exec rake test TEST=...` via injected Nabu::Shell. refresh = explicit adoption: re-fetch + OVERWRITE refetchable fixtures, bump manifest `retrieved:` dates by targeted line-rewrite (preserves the documented comment header), reminder warns that trimmed fixtures were replaced by full upstream files and must be re-trimmed; no-arg refresh REFUSES (demand_source raises usage) rather than refreshing the world. Tests (all network-free): manifest presence/validity + completeness both directions over every real-source dir; check-logic with WebMock (identical→clean, changed→drift+nonzero, 404→fetch-fail+adapter-test-skipped, non-refetchable→skipped-with-note-and-never-fetched, trimmed→fetched-not-compared, failing-fresh-adapter-test→drift); refresh in tmp dirs (overwrite, date-bump, trim-reminder, skip non-refetchable, no-arg refusal); the adapter-test shell-out is asserted at the command+env level via an injected FakeShell, never executed. Verified the swap is live end-to-end out of band (perseus_test PASS against a copy, FAIL against an empty dir). Suite 525/4079 green, lint clean.
2026-07-04 · P5-5 · 62d2de4+8257f04 · Post-sync anomaly detection `nabu health` (bare, local, no network) + inline SyncRunner deviation warnings. Two-callers-one-module: lib/nabu/health/trend_rules.rb holds every threshold ONCE (Health::Finding{kind,severity,message}; severity drives both the CLI label and the exit code), consumed by lib/nabu/health/local_check.rb (whole-history health report) and SyncRunner#deviation_warnings (one fresh LoadReport). SOFT/LOUD SPLIT (loud = exit 1 red; soft = exit 0 advisory; info = never affects exit): (1) quarantine_spike LOUD — latest successful run's errored > max(prior ≤5 runs) AND > SPIKE_FLOOR=10, so 0→1/2→7 never scream and a first sync with no prior norm is never a spike; (2) added_collapse SOFT — last COLLAPSE_RUNS=3 successful runs all added:0 AND updated:0 WHILE some earlier run was active (a brand-new all-zero source never trips — a quiet corpus reads the same, maintenance §2's 0,0,0→frozen); (3) withdrawal_creep SOFT >5% / LOUD >15% — cumulative (withdrawn OR retired_upstream)/total documents, catching slow bleed the per-sync 20% breaker misses; (4) stale SOFT — enabled + live-policy source only, last_sync_at older than STALE_AFTER_DAYS=14 (manual/frozen expected to sit still); (5) never_synced INFO; sync_withdrawal reuses the creep thresholds for one sweep. GOLDEN REPLAY: test/golden/golden_queries.yml (the same 8-query smoke set, loaded from the repo path) replayed against the LIVE catalog+fulltext via Query::Search, read-only. SKIP RULE (the packet's think-it-through): a query is LOST (loud) only when its expected passage urn IS present in the catalog but Search fails to return it (the real loader/normalizer/indexer regression); when the urn is absent from the catalog entirely the source was simply never synced into THIS corpus → SKIPPED (informational). Per-urn presence (Store::Passage.first(urn:)) is more precise than a urn-scheme→slug map and needs no coupling to which sources a deployment happens to carry; documented in the class header. Corpus states: no catalog file → :absent ("no corpus — run sync/rebuild", replay skipped, exit 0); catalog but no fulltext table → :no_index (same informational stance). CLI: bare `nabu health` prints per-source trend rows (ok / ANOMALY / warning / note, slug column aligned, continuation findings indented), the golden section (lost lines + "N found, N lost, N skipped"), a verdict, and a hint toward `--remote`; exit 1 on any loud finding (Report#any_loud? = any loud source finding OR any lost golden). The P5-3 `--remote` path is untouched (split into run_remote_health/run_local_health helpers, each owning its db handles). SyncRunner: Outcome gained a `warnings` field (empty on abort); after a successful sync deviation_warnings runs the spike + single-sweep-withdrawal rules against the fresh LoadReport (the just-recorded run is runs.first, prior runs form the norm) — advisory, printed by the CLI in yellow after the counts line (sync_one + sync_all), never failing the sync. The stub bare-health CLI test was REPLACED (as scoped) by real ones: healthy corpus exit 0, seeded quarantine spike exit 1, no-corpus note exit 0. Tests: trend_rules unit (spike fires/floor-suppressed/norm-suppressed/needs-history, collapse fires/needs-prior-activity/streak-broken, creep soft/loud/silent + sync_withdrawal reuse, stale fires/fresh/nil/string-timestamp), local_check unit (never-synced info, healthy, spike loud+exit, no-spike-on-stray, collapse soft, creep soft→loud, stale flagged, manual not stale, golden found/lost/skipped, absent/no_index, repo-yml load), CLI (3 bare-health), sync_runner (inline spike warning + sync still succeeds, clean sync no warnings). Existing P5-3 --remote CLI + probe tests unchanged. Suite 559/4172 green, lint clean.
2026-07-04 · P5-6 · 3e02b3a · Ops wiring: docs/ops.md operating manual + three launchd plist TEMPLATES under ops/launchd/ (nothing auto-installed; never ran launchctl). RUBY/PATH STRATEGY (the tricky bit — launchd jobs get a bare PATH, no shell profile / rbenv shims / Homebrew): every job is `/bin/sh -c` running `bundle exec …` with WorkingDirectory=__NABU_ROOT__ (Bundler finds the Gemfile, adapters find canonical/) and EnvironmentVariables PATH=__RUBY_BIN_DIR__:/usr/bin:/bin:/usr/sbin:/sbin — the one placeholder that pins the real ruby/bundle bindir so `bundle`, bin/nabu's `env ruby` shebang, and `git` all resolve; two placeholders total (__NABU_ROOT__, __RUBY_BIN_DIR__), documented with Homebrew/rbenv/chruby discovery (`dirname "$(which ruby)"`). Templates: com.nabu.verify-nightly (03:15 daily `nabu verify`), com.nabu.weekly-maintenance (Sun 04:00, three sequential commands `sync --all` / `health` / `health --remote`, run regardless of each other's exit — NOT enabled by default, real syncs stay eyeball-verified per CLAUDE.md, doc says so), com.nabu.fixtures-monthly (1st 05:00 optional `rake fixtures:check`); all ProcessType Background, StandardOut/ErrorPath → gitignored log/. ops.md covers install (sed-substitute → ~/Library/LaunchAgents → `launchctl bootstrap gui/$UID`), test-fire (`launchctl kickstart -k`), inspect (`launchctl print`), uninstall (`bootout`), schedule knobs, log/exit-code reading, per-failure-class playbook (verify mismatch→disk/tamper+backups+rebuild; health spike→parser regression via --parse-only; creep→consider frozen; remote gone→check URL/freeze; license changed→review before next sync; breaker→--force only if deletion real), and an OPTIONAL `|| curl ntfy.sh` notification example documented as owner-configured (nothing wired). VALIDITY TESTS (test/ops/launchd_test.rb): substitute placeholders into a tmpdir copy, assert `plutil -lint` OK via Nabu::Shell (skips gracefully when plutil absent — non-macOS CI — documented in the test); no-surviving-placeholder guard; every `bin/nabu <subcommand>` referenced ∈ Nabu::CLI.all_commands.keys (+ bin/nabu executable); every `rake <task>` referenced ∈ `bundle exec rake -T` output (honest source of truth, network-free). Consciously left out: no monthly plist auto-enabled; no launchctl ever invoked; notification endpoints not committed (secrets). Suite 565/4200 green, lint clean.
2026-07-04 · gate · b1dd0ce · Owner UX mid-phase: `show <document>` lists passages as :suffixes relative to the document urn (printed once in the header); --full-urn restores absolutes. Follow-up shas riding in packet lines: f282afd (lowercase license.md probing — live smoke), 5dc7cca (byte-compare fixtures, encoding false-drift — live smoke), 8257f04 (ranking-independent golden replay via Search urn: filter — live smoke). Live papyri --parse-only recovery after P5-1: +12,287 added / 49,060 skipped / 0 revised / 0 withdrawn / 9,354 errored → 61,347 docs, corpus index 1,546,601 passages.
2026-07-04 · flip · — · PR #6 merged; papyri-ddbdp flipped enabled: true (owner sign-off; 61,347 docs / 920,766 passages) — ALL SIX SOURCES LIVE, corpus 1,546,601 indexed passages. Field bug fixed in the same change: owner's `status` showed four signed-off sources as disabled — sync_source! seeded `enabled` only on row creation while sync --all read the yaml (split-brain). SPEC REVISED: the registry yaml is authoritative for `enabled` (the owner's flip-with-sign-off workflow), reconciled into the db row on every sync_source!; db keeps owning sync history. Old preserve-enabled test rewritten to assert the new contract; live db reconciled — status now agrees with sources.yml for all six.
2026-07-04 · P6-1 · 3a59aa3 · Structural-retry citation recovery in EpidocParser (the Iliad quarantine class). ROOT CAUSE: the CapiTainS subtype convention (div is a citation level iff @subtype == a cRefPattern unit name) fails whenever the header's unit labels don't match the body's subtype labels — the Iliad declares units book/line but labels its book divs subtype="Book" (case shift). FIX at the CTS-contract level: when (and ONLY when) the subtype pass raises a citation-shape ParseError (depth mismatch / missing @n / no citable passages / duplicate urn), the file is re-read matching the body against the deepest cRefPattern's replacementPattern XPATH with strict axis semantics ($-bound steps are citation levels, bare steps structural, child vs descendant axes honored, un-@n'd elements unselected) — i.e. exactly what the file's own declared resolver xpath selects; subtype labels stop mattering. Both passes failing → quarantine with a message naming both. FROZEN-URN ARGUMENT (P5-1 standard): retry runs strictly after the unchanged first pass raises, so cleanly-parsing docs never reach it — VERIFIED read-only vs live catalog: ALL 1,798 loaded perseus+first1k docs re-parsed → 0 urn mismatches, 0 text mismatches, 0 new errors; all 62 quarantined docs re-parsed → 58 recover (perseus 24/25 incl. the Iliad at 15,687 book.line passages; first1k 34/37). CENSUS (shape → count → outcome): depth-mismatch-fewer-than-declared P7+F12 → all recovered (case-shifted/renamed/unnamed subtype labels); depth-mismatch-more-than-declared F5 → recovered (extra same-label nesting is content under the strict xpath); no-citable-passages P12+F9 → 20 recovered (case-shifted units e.g. Exordium/Section, unit-less cRefPatterns e.g. Diodorus grc6), 1 STAYS (tlg1389 Harpocration: matchPattern declares 1 group but the xpath binds $1+$2 — self-contradictory header, honest quarantine "replacementPattern binds 2 component(s) but matchPattern declares 1"); missing-@n P6+F7 → recovered (un-@n'd elements are unaddressable by the declared xpath → structural noise, remaining units cite; Pindar tlg0033×4 mint fully); no-edition-div F3 → STAY (tlg0643.grc2/tlg3129/tlg4034 are div[@type="commentary"] files, nothing citable by the edition contract); duplicate-urn F1 → recovered (tlg0627: declared 1-level xpath selects the book divs — coarse but exactly what the declaration resolves to). NOTE for the resync: strict xpath semantics mean misnested/mislabeled files (tlg2178, tlg0627) mint few, coarse passages — faithful to their declarations; upstream fixes would refine them. Fixtures: Iliad trimmed exemplar (books 1–2 boundary, q-wrapped lines) into test/fixtures/perseus/ + Nicomachus (renamed-label exemplar) into test/fixtures/first1k/, READMEs + manifests updated (whole:false trims; adapter_test → null per P5-4 schema since the sources now carry trims). Tests: Iliad book.line spot checks (1.1 μῆνιν ἄειδε…), two-parse stability, golden urn+text regression for all 7 pre-P6-1 fixtures, contradictory-refsDecl surgery still quarantines w/ precise message; conformance green for all adapters. Suite 574/4483 green, lint clean. Expected recovery at the orchestrator's --parse-only resync: perseus-greek +24 (→ 1 errored), first1k-greek +34 (→ 3 errored).
2026-07-04 · P6-2 · eab4bd1 · Cancelled-document ⟦⟧ fallback in DdbdpParser (the whole-document-<del> quarantine class). RULE CHOSEN (fable decision): when — and ONLY when — the standard Leiden pass extracts ZERO citable lines, the document is re-read once with <del> content KEPT, wrapped in Leiden double brackets ⟦…⟧ ("⟦" where the del opens, "⟧" where it closes, gap-marker style: a multi-line cancellation carries one opening and one closing bracket as print sets it; nested dels nest, e.g. bgu.1.179 line 7's ⟦κ⟧ inside the whole-document cancellation); rdg/orig/note/figure still drop and gaps still mark INSIDE the kept del; every touched line carries "leiden":{"cancelled":true}; still-empty documents keep quarantining. ALTERNATIVES REJECTED: "always render <del> in ⟦⟧" — packet-forbidden, rewrites loaded partial-del passages (recorded in conventions.md §5 + parser header as the deferred owner question: a corpus-wide journaled revision, not a parser patch); "del wraps the entire ab/edition" — structurally false for the class (in ALL four exemplars the del opens AFTER the first <lb/>, and o.claud.3.457 has per-line dels, not one wrapper); per-line "keep del iff line would be empty" — NOT inert, would resurrect skipped del-only lines inside loaded documents and shift frozen sequences. INERTNESS: the fallback engages iff pass 1 yields zero lines iff the doc quarantined pre-P6-2; loaded ⇒ ≥1 passage ⇒ fallback provably never runs, standard-pass code path untouched. VERIFIED read-only vs live catalog: 500/500 random loaded papyri docs re-parse to byte-identical urns AND passage text (0 mismatches, 0 errors); all 9,351 distinct "no citable lines" quarantined refs re-parsed → 43 RECOVER (o.claud.3.457, cpr.6.3 19 lines, bgu.1.179 28 lines, apf.59.139 16 lines w/ restart blocks b2/b3 minting inside the del, + stud.pal/p.oxy/o.kellis etc.), 9,308 stay honestly quarantined (empty-<ab/> stubs incl. chrest.wilck.101 and 3 slash-named refs verified individually via their _-encoded filenames). Fixture: o.claud.3.457 copied whole from local canonical into test/fixtures/papyri-ddbdp/ (README + manifest.yml local-trim entry). Tests: exemplar ⟦⟧ texts + cancelled annotations + two-parse stability; golden regression EXTENDED with pre-P6-2 text sha256s for all four papyri fixtures (was urn-only); partial-del doc (c.epist.lat.10) asserted bracket-free; text-less stub still quarantines. conventions.md §5 amended (what ⟦⟧ means, why cancelled ≠ deleted — a crossed-out receipt is still a receipt — and the deferred general-policy question). Suite 579/4521 green, lint clean. Expected recovery at the orchestrator's --parse-only resync: papyri-ddbdp +43 (errored 9,354 → 9,311).
2026-07-04 · P6-3 · e9a9b34 · Per-repo pinning for multi-repo sources — `nabu health --remote` now reports real drift + license per treebank repo instead of :multi/:unchecked (lifting the P5-3 deferral). SCHEMA: migration 004 source_repos (source_id FK, repo_url, last_sync_sha, license_baseline_sha256, unique (source_id, repo_url)); Sequel model lib/nabu/store/source_repo.rb, registered in Store::MODELS + Source one_to_many. Like last_sync_*/license_baseline in sources, every column is RUNTIME state (not derived from canonical/) — `nabu rebuild` drops it, the next sync re-pins, the next probe re-records baselines: rebuild-purity holds. PLUMBING (minimal): FetchReport gained a nullable `repos` field ({ repo_url => head sha }); UD's fetch already drives GitFetch per treebank and knows each HEAD, so it now maps its per-slug shas through repo_url(slug) into repos (keyed by the SAME url the probe reads from Adapter.upstream_repo_urls). Single-repo adapters (Adapter#git_fetch! and the test adapters) leave repos nil — byte-identical, they keep pinning only sources.last_sync_sha. SYNC PATH: SyncRunner#update_source_state, after stamping the source row, upserts one source_repos row per reported repo on (source_id, repo_url) touching ONLY last_sync_sha (so a probe-recorded license baseline survives the sync), then deletes rows for repos no longer in the reported set (stale pins must not linger and read as phantom drift). Chose upsert-on-conflict-key over delete-and-replace precisely to preserve baselines; single-repo sources report no repos → write nothing. PROBE (lib/nabu/health/remote_probe.rb): for a multi-repo source (upstream_repo_urls.size > 1) drift is now computed PER REPO against source_repos (its ls-remote HEAD vs its pin → :current/:behind/:never_synced/:unknown), source-level drift = worst (DRIFT_SEVERITY behind>never_synced>unknown>current), the :behind repos named in a new SourceHealth.drift_detail (drift itself stays a bare symbol so single-repo behavior is byte-identical — no drift Data promotion that would churn every existing assertion); license fetches raw.githubusercontent.com per repo with the baseline stored on that repo's source_repos row (compare_license reused verbatim — Source and SourceRepo both carry the column + #update), source-level license = :changed if ANY repo changed (offenders named) else :baseline_recorded if any newly recorded else :unchanged else :unchecked. NO-PINS FALLBACK: a multi-repo source never synced under P6-3 (zero source_repos rows) still reads drift :multi / license :unchecked("multi-repo") — nothing per-repo to compare until the next sync records rows; single-repo path unchanged (single_repo_drift/single_repo_license reuse the old guards in the old order). CLI health_detail surfaces drift_detail on a multi-repo :behind row. TDD: schema (table + composite-unique + FK), model association; sync_runner (multi-repo records a row per repo, re-sync upserts sha + drops the vanished repo's stale row + preserves its baseline, single-repo writes nothing) via a MultiRepoAdapter returning FetchReport.repos; UD fetch reports per-repo pins keyed by repo_url (fixture git repos); probe (per-repo drift one-behind-one-current names offender, all-current, per-repo license one-changed named, baseline-recorded lands on the right row) + the pre-existing :multi test kept as the honest unpinned case. Suite 592/4549 green, lint clean. Live db untouched.
2026-07-04 · P6-4 · 13f5d8c · Per-language search folding moved from index/query time to the adapter boundary (the P4-1 stopgap retired). text_normalized is now the TRUE search form, minted ONCE in Passage.new (defaults to new Normalize.search_form(text, language:); adapters stopped passing it — epidoc/proiel/conllu/ddbdp parsers + test_adapter all dropped their NFC(downcase) line; conformance suite pins every adapter's output to the minted form). RULE TABLE (Normalize::LANGUAGE_FOLDS, one place; conventions.md NEW §9 with rationale + citations): generic baseline for ALL languages = NFC→downcase→NFD→strip \p{Mn}→NFC (covers polytonic Greek marks incl. iota SUBSCRIPT U+0345, Cyrillic titlo U+0483/palatalization U+0484 — verified against real zogr fixture text дх҃омь→дхомь — and IAST); grc extra: final sigma ς→σ (TLG Beta Code encodes both as one "S"); lat extra: v→u, j→i (PHI: search "does not distinguish i from j or u from v"; Perseus practice same). Conservative/no-rule (open questions documented in §9): Greek ADSCRIPT iota (letter αι) not folded; chu/orv letterforms (ꙇ/і/и, ѡ/о, оу/у) kept distinct — no citable normalization practice, orthographically meaningful; orv й→и noted as a Mn-strip side effect; san IAST stripping conflates phonemic length — accepted price, pristine text intact; got generic only (j is a real letter). INDEX/QUERY SEAM: Indexer indexes text_normalized AS STORED (fold call removed; index_row is a pure column map, tested); Search folds the QUERY ONLY via new Normalize.query_forms = the UNION [generic, grc-variant, lat-variant].uniq ORed in the MATCH (single variant passes through verbatim, preserving user FTS syntax). WHY THE UNION CANNOT MISS: doc in language L is indexed as extra_L(generic(text)); the union always contains extra_L(generic(query)), so source-spelled queries always fold to the indexed form; and variants are ORed so the generic variant keeps no-rule languages findable (Gothic "jah" regression-tested against the lat j→i variant). REBUILD NOT PARSE-ONLY: content_sha256 covers text_normalized, so every passage hash changes BY DESIGN — a parse-only sync would journal a corpus-wide revision storm; the orchestrator runs full `nabu rebuild` (drop + re-derive from canonical, LOCAL) then replays goldens. Goldens grew 8→10: "αοιδησ" (internal-sigma query finds Hymn 13.3's "ἀοιδῆς.") and "uidemur" (u-spelling finds PROIEL cic-off 86001 "videmur"); all 10 green against a fixture corpus built the new way. Tests: search_form/query_forms units incl. the union-covers-every-rule invariant + real fixture samples per language (ς/σ, ᾳ→α, ᾼ→α, titlo, IAST, þ/j); fold-both-sides end-to-end per language in search_test (final sigma both spellings, v/u + j/i both spellings, titlo both spellings, Gothic union regression); Passage minting units; indexer as-stored unit; conformance minted-form pin across all six adapters. Suite 622/5089 green, lint clean. Docs: architecture §3 stopgap note replaced with boundary reality; indexer/search class comments rewritten; conventions §1 final-sigma bullet → §9.
2026-07-04 · gate6 · — · Phase 6 recoveries + reload, all frozen-urn/text guarantees held live: perseus --parse-only +24/=744/~0 (THE ILIAD in: 15,687 book.line passages; show 1.1 → «μῆνιν ἄειδε θεὰ Πηληϊάδεω Ἀχιλῆος»), first1k +34/=1054/~0, papyri +42/=61347/~0 (all four cancelled-doc exemplars verified live with ⟦⟧ text; the 43-vs-42 delta is a journal-history counting artifact across three runs, zero del-bearing files remain quarantined). P6-4 full rebuild: 63,309 docs re-derived, indexed 1,606,538 passages — EXACTLY the pre-reload count (rebuild invariant at corpus scale); honest holdouts 1 perseus (self-contradictory refsDecl) + 3 first1k (commentary-only) + 9,312 papyri (9,309 text-less stubs + 3 textpart-missing-@n). Post-reload health: all sources ok, golden replay 9 found / 0 lost / 1 skipped (incl. new final-sigma + v/u probes); live spot-check: search "videmur" finds "uidemur" in PROIEL Cicero AND a Ravenna papyrus.
2026-07-07 · P7-1 · 460bc96 · Durable history split: db/history.sqlite3 (the LEDGER — never derived, never dropped, backup set) vs catalog+fulltext+vectors = f(canonical). MOVED to the ledger, re-keyed by durable identity because rebuild re-mints every catalog id: runs → runs(source_slug, kind[sync|rebuild], counts…) — RunRecorder now records by slug (kind=rebuild for replays; trend queries in health/sync-warnings filter kind=sync so a replay's added=everything never poisons baselines; status shows latest run of any kind); sources.last_sync_sha+license_baseline_sha256 and source_repos → ONE unified pins(source_slug, repo_url, last_sync_sha, license_baseline_sha256) table (single- and multi-repo alike, keyed by the same url Adapter.upstream_repo_urls declares; sources.last_sync_* kept as display mirrors only; RemoteProbe now reads/writes pins exclusively — needs no catalog at all — closing the rebuild license-drift blindspot). PROVENANCE DECISION (option b): catalog provenance stays derived and resets on rebuild (per-load noise: "loaded" ×60k/replay, quarantined, superseded — it describes the derivation); a compact durable revisions(urn, event[revised|withdrawn|restored|retired|unretired], old_sha, new_sha, at) ledger carries the lasting revision history, written by the Loader ONLY for content transitions of EXISTING rows — inserts (= every rebuild replay into a fresh catalog) write nothing durable, so rebuild leaves the ledger byte-identical (tested). MIGRATION TRACKS per db: db/ledger_migrate/001 (new) + catalog 005 drops runs/source_repos/license_baseline column; each SQLite file carries its own schema_info so the counters can't collide. LIFT-AND-SHIFT: Ledger.open_with_lift! (used by every write path: sync, health --remote, and Rebuild BEFORE rm_f) one-shot copies a legacy catalog's runs (JOIN sources → slug), source_repos rows, and single-repo sources columns (keyed by upstream_url; multi-repo org urls skipped) into the ledger, then migrates the catalog forward; guarded per-table on ledger-not-empty so restores never duplicate history. FRESH BOOTSTRAP: no ledger file = empty history — status says "no run history", health says never-synced (info), first sync creates it. STALE RULE now reads the latest successful sync run's finished_at from the ledger (survives rebuild) instead of sources.last_sync_at (doesn't). Phase-8 contract documented: enrichment journal will live in the ledger urn-keyed like revisions. LocalCheck/StatusReport/SyncRunner/Rebuild/CLI thread the ledger handle alongside catalog/fulltext (house pattern; no ATTACH). architecture §1/§2/§5/§8 updated (invariant now names the ledger exception). Tests: ledger schema/models/open!/lift (incl. legacy-shape catalog fixture at migration target 4, idempotent re-lift, org-url skip); rebuild seeds ledger → intact + kind=rebuild rows + lift-before-drop + slug continuity across id re-minting; loader durable-transition matrix (revise/withdraw/restore/retire/unretire durable; inserts+idempotent reloads+rebuild replays silent; nil-ledger works); probe pins/baselines in ledger; sync pins single+multi+stale-pin removal+parse-only preservation; CLI fresh-bootstrap + no-ledger status honesty. Suite 647/5174 green, lint clean. Live db untouched (orchestrator lift instructions in the packet report).
2026-07-07 · P7-2 · 0bf43b7+8e51208 · Backup & the restore drill: `bin/nabu backup` — file-level rsync (via Nabu::Shell) of the NON-derivable set to a config-driven external-volume target. SET: canonical/ INCLUDING every .attic/ (file-level or nothing — a per-slug git mirror would miss the attic, the one copy of upstream-scrapped docs), db/history.sqlite3 (the P7-1 ledger — only copy), config/ (nabu.yml+sources.yml), and the derived dbs (catalog+fulltext) default-ON with --skip-derived to omit (cheap file copy beats an hour of rebuild). Target from config/nabu.yml `backup: target:` (Config gains backup_target + config_dir readers; commented example shipped), --to PATH overrides. THE MOUNT-POINT GUARD (owner-mandated): before any rsync, ascend from the target to its volume root and refuse loudly ("volume not mounted — refusing to back up onto the boot disk") unless that root's device id differs from its parent's (a real mount) — catches the footgun where an unmounted /Volumes/X is a bare boot-disk dir that rsync silently fills and later shadows the real volume; --allow-unmounted bypasses for deliberately-local targets (drill/tests). Mount detection is injectable-stat (MountGuard.mounted?(target, stat:)) so it's unit-tested with a stubbed device comparison — no hdiutil, no /Volumes, no mounts in the suite. rsync -a --delete scoped to each section's SUBDIR of the target (canonical/, config/), never the volume root (a file beside the target is never swept); single db files copy without --delete (no sibling-sweep). --dry-run passthrough; per-section + summary (files/bytes measured Ruby-side, version-independent — never parse openrsync-vs-GNU output); a failed section → status :failed, Result.ok? false, CLI exit 1 with honest per-section report. `rake ops:drill` (Nabu::Ops::Drill, returns data; task formats) — fresh-machine drill, FULLY LOCAL: back up the live tree to a tmp target (--allow-unmounted) → restore into a fresh tmp "machine" (rsync back) → Rebuild from restored canonical alone → Verify → golden replay (Health::LocalCheck) → report cross-checking restored doc/passage counts vs the source of truth (live catalog, read-only). Reads the live corpus only, writes exclusively under tmp, so the orchestrator runs it against the LIVE corpus at acceptance. Fresh-machine config plumbing: Config.load defaults path/root from NABU_CONFIG/NABU_ROOT (explicit kwargs still win) so a restored install needs no code edit; the drill builds the restored Config directly. docs/ops.md §9 (external-volume + sparsebundle workflow, guard, step-by-step restore, drill, nightly job) + cadence/schedule/log/quick-ref touch-ups; ops/launchd/com.nabu.backup-nightly.plist (02:30, plutil-tested like siblings). Tests: backup full-set incl. attic + --skip-derived + --delete scoping (sibling sentinel survives) + dry-run no-op + guard trips same-device / stubbed-mount passes / --allow-unmounted bypass + no-target error + one-section-failure nonzero; drill end-to-end on a TestAdapter fixture corpus (counts match, verify green, source untouched); CLI backup run/dry-run/guard-refusal/help; config backup_target/config_dir/env-override. Suite 671/5273 green, lint clean. Live db/canonical untouched (drill is the orchestrator's live acceptance step).
2026-07-07 · P7-3 · 86777f5 · Perseus Latin: `PerseusLatin < Perseus` (lib/nabu/adapters/perseus_latin.rb) — the designed one-line sibling shipped VERBATIM as the parent header promised (`NAMESPACE = "latinLit"`, nothing more; zero design-doc drift). Everything is inherited: the perseus-latin manifest, LANGUAGES latinLit→lat, the perseus-lat<n> edition-slug rule, discover/parse/fetch, __cts__.xml title resolution, highest-version selection — all already keyed off `self::NAMESPACE` in Perseus. Wired require into lib/nabu.rb (after perseus, before first1k). Registry: config/sources.yml `perseus-latin`, enabled:false (awaiting owner-initiated first sync), sync_policy:live (same as greek sibling). Tests: test/adapters/perseus_latin_test.rb — full AdapterConformance suite against the P2-1 latinLit fixture (stoa0045, Ausonius' Genethliacon, 28 lines whole:true), plus manifest identity (perseus-latin / canonical-latinLit, distinct from greek parent), discover finds the stoa0045 edition with a lat ref, a synthetic-tree test pinning latinLit urns + perseus-lat<n> slug rule (highest version wins, eng translations AND wrong-namespace grc editions filtered), parse spot-check ("carmina prima tibi eum iam puerilibus annis"), and greek-sibling-unaffected. Fixture manifest.yml: added a comment noting the dir now backs both adapters and why adapter_test stays null (dir-level Iliad trim). docs/02-sources.md row 1: latinLit READY, awaiting first sync. Conformance green for all SEVEN adapters. Suite 690 runs / 5503 assertions / 0F 0E; lint clean (116 files).
2026-07-07 · P7-4 · 2b5485f · Parallel translations: perseus-eng* editions ingested as aligned documents + `show --parallel`. UPSTREAM REALITY (canonical-greekLit, 2026-07-03 snapshot): 786 perseus-eng<n> files over 650 works, version tokens PURE DIGITS (eng1–eng6, no letter suffixes); 785/786 anchor the body in div[@type="translation"] (one outlier, tlg0057.tlg010.perseus-eng2, uses "edition"); 3 grc files embed a translation div NEXT TO their edition div — which is why parser acceptance is PER-PARSE (EpidocParser `division_types:`, default ["edition"] — grc parses byte-identical), not a global widening; translation refs parse with ["translation","edition"]. FLAG PLUMBING: sources.yml `translations: true` (default false, validated boolean) → SourceRegistry::Entry (new member, Entry.new default keeps old call sites) → Entry#build_adapter — THE construction seam sync/rebuild/verify now share (flag off = plain no-arg .new; flag on passes translations: true, ArgumentError from a non-supporting adapter → ValidationError naming source+class). Perseus: initialize(translations: false); classify_edition probes edition_slug_pattern FIRST and only behind the flag translation_slug_pattern (/\Aperseus-eng(?<version>\d+)\z/); Candidate gains :language; best-version grouping keyed [tg, work, language] so the eng pick never competes with the original's; eng refs carry language "eng" (generic fold per P6-4, minted by Passage.new). First1k/PerseusLatin inherit everything, flag defaults off ⇒ untouched. ALIGNMENT (Query::Parallel, `show <urn> --parallel [LANG]`, default eng, works with --full-urn, passage urn scopes to its suffix): sibling = same work prefix (urn:cts:<ns>:<tg>.<work>.) + document language=LANG + urn≠self, highest trailing version wins (numeric-then-letter: eng10>eng2); PAIRING RULE = exact citation-suffix equality, rows follow the queried side's order, right-only suffixes interleave one-sided by their own sequence (two-pointer merge), no fuzzing — unmatched suffixes render honestly one-sided ("—"); withdrawn shown flagged (show-family). Search: eng passages are ordinary hits, --lang separates; golden query "rich haired Demeter" (FTS5 unicode61 splits the corpus's hyphen; a hyphen IN a query is FTS syntax). FIXTURES (whole local-canonical copies): HH13 perseus-eng2 (ONE merged line over the grc's three — the one-sided case) + 2John perseus-eng2 (13/13 verse-aligned); golden corpus loads perseus-greek translations-on; conformance suite ALSO run against a translations-on instance (second include, harness allowed it). FROZEN-URN: structural — flag-off filter is character-identical (translation probe unreachable), grouping key constant per group, parse of originals never constructs a non-default parser call; verified READ-ONLY vs live catalog: flag-off discover = 769 = 768 live docs + the 1 known quarantine, 0 missing; 6/6 random docs re-parse to IDENTICAL passage urns+texts; flag-on = 1419 ids (+650 eng, −0 originals). Owner opt-in recorded: perseus-greek translations: true in sources.yml (strategic review approval); expected recovery = parse-only resync ingesting ~650 eng docs (Iliad eng4 sanity: 190 card passages, :1.1 first — book/card citation, so Iliad alignment pairs only card-initial lines; noted in fixture README).
2026-07-07 · P7-5 · 181be6b · Lemma search over the gold treebanks: `search --lemma FORM [--lang|--license|--limit]` finds every inflected attestation (--lemma λέγω → λέγουσι, λέγοιεν, AND suppletive εἶπας/εἰπεῖν) from the lemmas already stored in annotations_json — zero API, zero enrichment. THE INDEX (first annotation-derived index; the Phase-8 pattern): passage_lemmas in fulltext.sqlite3, built by Indexer.rebuild! alongside the FTS table (same drop-and-rebuild lifecycle, same not-a-migration stance; plain Sequel create_table — the raw-DDL exception stays FTS5-only), extracted from the catalog's STORED annotations_json (never re-parsing canonical; one streaming pass now feeds both tables). SHAPE: a plain table + B-tree index on lemma_folded (exact-equality lookup — FTS adds nothing for an atomic dictionary-form key); ONE ROW PER (passage, folded lemma), not per token — a hit is a passage, so hit-shaped rows need no query-time GROUP BY, and the distinct pristine surface forms aggregate into surface_forms (", "-joined) for readable hits ("λέγω → εἶπας"); lemma_raw keeps the upstream spelling; fixture arithmetic: dedup trims only ~10–14% off per-token rows (7–21 rows/passage across families), so readability, not row count, decided it. FOLDING: index side = Normalize.search_form(lemma, passage language) — a lemma is a dictionary form in language L, so grc λόγος → λογοσ (final ς routine in dictionary forms; consistent because BOTH sides fold), lat video → uideo; query side = the P6-4 query_forms UNION (asserted for lemmas per language rule in tests). EXTRACTION: both parser families store tokens[].lemma/form (ConlluParser: LEMMA/FORM columns, `_`/MWT-ranges lemma-less; ProielParser: token @lemma/@form, empty tokens lemma-less) — one uniform reader; passages without token lemmas contribute NO rows (honest absence: the other ~1.45M passages). QUERY: Query::LemmaSearch — folded IN-list over the lemma table, urn-ordered (no bm25 analogue), then the catalog join for pristine text/lang/license/withdrawn re-check; the join extracted into shared Query::CatalogJoin so Search and LemmaSearch can never drift on visibility/license semantics; urn: probe for ranking-independent golden replay. CLI: --lemma REPLACES the FTS query (simplest honest v1; combining = future), composes with --lang/--limit/--license; hits print urn [lang], lemma → surface form(s), pristine line (truncated 100ch — the surface form already marks the match, readability beats a folded snippet); pre-P7-5 fulltext file (no lemma table) → honest "no lemma index — sync/rebuild" hint; help search teaches it with the fixture-verified λέγω example. GOLDENS: yml entries may carry `lemma` INSTEAD of `query` (xor pinned in tests) — added λέγω→64498 (εἶπας, unreachable by any λεγ- text query); golden_test dispatches per entry + asserts lemma rows come from exactly the three treebank families; health LocalCheck replays lemma entries via LemmaSearch (skipped-not-lost when the fulltext predates the table). Tests: indexer extraction/fold/dedup/withdrawn/idempotence, LemmaSearch end-to-end w/ real fixture lemma-form pairs, query_forms-union invariant, CLI functional over a real synced UD fixture corpus + help pins, goldens, local_check found/lost/skipped. Suite 761/6222 green, lint clean. Live reindex: any `sync <src> --parse-only` or rebuild builds the lemma table (SyncRunner#reindex! calls the same Indexer.rebuild!).
2026-07-07 · P7-6 · 1a85b93 · Show ranges: `nabu show URN:<start>-<end>` prints an INCLUSIVE, sequence-ordered slice of ONE document between two resolved citation suffixes — the concept's own syntax. THE PARSE PRECEDENCE (deliberate, documented, tested): (1) LITERAL FIRST — Show/Parallel resolve the whole string as a real passage or document urn before Range is ever consulted, so a passage urn that itself holds a hyphen stays reachable and is never misparsed as a range; (2) then split on the LAST hyphen (citation suffixes are dots+colons, never hyphens — 1.1, b2:5, r:3 — whereas the version segment perseus-grc2 / slug gothic-proiel carries a hyphen to the LEFT of the citation, so last-hyphen is unambiguous for every real corpus shape); the head is the absolute start passage urn, the tail is the END SUFFIX reconstructed against the SAME document (`<doc>:<end-suffix>`). (3) A range is RECOGNISED only when the head names a real passage OR a real document is a prefix of the head (so a bad START can be named "range start not found"); otherwise → nil → "urn not found", so a genuinely unknown urn is never dressed up as a range. SLICE = every passage whose STORED sequence lies in [start_seq, end_seq] inclusive, whatever citation shapes lie between — a papyri restart block (:b2, P5-1) is sliced straight through (`…:240:1-b2:2`). ERRORS (Query::Range::Error < Nabu::Error → CLI Thor::Error exit 1): endpoint-not-found names the failing endpoint's urn; start-after-end refused, suggesting a swap. New Nabu::Query::Range resolver (shared by Show + Parallel so they parse identically). Show.run gains `|| range(urn)` returning a RangeResult (document header shaped like DocumentResult so passage_label + --full-urn reuse unchanged, plus total M for the honest "[N of M passages]" note and the two endpoint urns). Parallel.locate gains a third return (in-slice suffix set); run filters aligned rows to the slice — pairing applies to the sliced rows only, unmatched suffixes stay one-sided (P7-4). CLI: print_show_range (header + range span + [N of M] + :suffix lines), rescue Range::Error in `show`, help show teaches range syntax with a CTS 1.1-1.10 example AND a papyri :1-b2:2 cross-block example. Tests: slice/inclusive/single/cross-block/endpoint-errors/reversed/literal-first (show_test), range+--parallel composition (parallel_test), CLI range render/full-urn/errors/help pins (cli_test). Suite 778/6305 green, lint clean.
2026-07-07 · gate7 · — · Phase 7 live acceptance, all guarantees held: ledger lift 6 runs/0 pins (P6-4 amnesia now impossible); FIRST REAL BACKUP 310,470 files / 6.0 GB → NabuBackup volume in 71s; restore drill => RESTORABLE (verify clean, golden 9/0/1, restored counts = source to the passage; drill verdict fixed live — faithful quarantines are not a restore failure); English recovery +650 docs / =768 / ~0 → corpus 1,724,012 passages, show --parallel renders Iliad 1.1 grc+eng; lemma index 1,599,485 rows live, search --lemma λέγω finds the suppletive paradigm (ῥηθέντος, εἶπον, εἰπὲ, εἴπω), --lemma tu --lang lat finds tibi/te; ranges live (Iliad 1.1-1.5 proem, papyri cross-block :1-b2:2). Perseus Latin shipped enabled:false — first sync owner-fired at this gate. Two agent session-limit interruptions recovered via resume (P7-5 finished green on resume).
2026-07-07 · P8-1 · 369c357 · MCP tool contract + protocol core (lib/nabu/mcp/): the read-only conversational surface, hand-rolled per owner decision. PROTOCOL (server.rb, researched live against modelcontextprotocol.io + the Claude Code 2.1.x binary): spec revision 2025-11-25 pinned (Server::PROTOCOL_VERSION; Claude Code requests exactly it), stdio framing = newline-delimited JSON-RPC 2.0 (one UTF-8 object/line, NO Content-Length, batching removed in spec 2025-06-18 → arrays rejected -32600); initialize (counter-offer negotiation), notifications swallowed, ping, tools/list, tools/call; unknown tool → -32602 (the spec's own example), semantically bad ARGUMENTS → isError:true tool result (SEP-1303: models self-correct from tool errors); malformed line → -32700, id:null, loop survives. Core driven by injected IO/lines — the real stdin loop + stderr logging is P8-2's bin/nabu mcp. TOOLS (tools.rb, translation-only over Query::Search/LemmaSearch/Show/Parallel): nabu_search (query XOR lemma; default 10, hard cap 50, fetch limit+1 → honest "more than N, showing k"; no-match responses carry a one-line coverage hint), nabu_show (passage/document/range urn; parallel:true + parallel_lang; max_passages default 50 cap 200 with the range-urn escape hatch named), nabu_status (per-source counts + last-sync, languages, license classes, index state, excluded_by_default). CONTRACT: urn+language+license_class+source on every passage everywhere (parallel rows included); research_private/restricted DEFAULT-EXCLUDED from all three tools (search post-filter, show withholds text but names the class honestly, status counts them aggregate-only), include_restricted:true opts in per call with the why in the schema description. DEGRADATION (states, never isError, never a crash): no catalog → "no corpus — sync/rebuild", missing FTS/lemma table incl. the mid-reindex drop race → "index rebuilding — retry shortly", SQLITE_BUSY → 3 attempts w/ backoff then "corpus busy — retry shortly". Store.connect/connect_fulltext gained readonly: (SQLITE_OPEN_READONLY — engine-refused writes, tested). Tool table = one frozen hash; P8-3's nabu_concord is one entry + handler. 50 new tests (protocol round-trips, contract, leak probes w/ seeded research_private row, readonly refusal); architecture §9.
2026-07-07 · P8-1b · fda4b46 · Span-grouped parallel display (owner: card-cited prose was "frankly, not that parallel"). Query::Parallel now groups: each translation suffix present in the original is an ANCHOR owning the original passages from its position up to the next anchor's, computed over the FULL documents' suffix orders (so a range 1.5-1.10 is still covered by the card anchored at 1.1 — the case that used to render all-"—"). Anchors sorted by original position; a 1:1 anchor (one original, suffix == anchor) stays a compact verse pair (byte-identical to pre-P8-1b); a span becomes a :block (originals listed once, then the translation once with a coverage label + clip note when a slice shows only part); pre-first-anchor originals and original-absent translation suffixes stay one-sided; an anchor suffix the original lacks degrades to a one-sided translation row. CLI header gains a truthful "N block(s) covering M lines" clause only when blocks exist. MCP nabu_show parallel rows gain anchor/covers_first/covers_last/clipped (+shown_first/last when clipped); bounds + license contract unchanged. New fixture: Iliad Butler eng4 (tlg0012.tlg001.perseus-eng4), book 1 first two cards, card-cited prose against the existing line-cited grc2 sibling (README + manifest updated, whole:false). Live smoke (read-only): `nabu show urn:cts:greekLit:tlg0012.tlg002.perseus-grc2:1.1-1.10 --parallel` → "1 block covering 10 lines", eng block labeled "covers :1.1–:1.43; range shows :1.1–:1.10". Suite 838 green, lint clean.
2026-07-07 · P8-2 · 75e043a · MCP stdio entrypoint + registration: `bin/nabu mcp` wires P8-1's Server/Tools to real stdin/stdout. STDOUT IS PROTOCOL-ONLY (JSON-RPC, `$stdout.sync`); diagnostics to stderr or `--log FILE` (non-block File.open, closed in ensure). Connection slots are lazy memoizing READ-ONLY openers (Procs, `Store.connect(readonly: true)`): resolved per tool call, cached across calls (no fd churn), and reopened on (dev,inode) change so a mid-session `nabu rebuild` (which rm_f's + recreates the catalog) is picked up, not served stale off a deleted inode. SIGINT/SIGTERM trap → exit 0 (EOF semantics). Shipped `.mcp.json` (project scope, minimal `mcpServers` stdio shape) + docs/mcp.md (three tools, restricted-exclusion stance, project/user-scope + Claude Desktop registration — user scope needs absolute `BUNDLE_GEMFILE` + abs bin path since the client spawns with no repo cwd — example transcript, license/attribution note, nabu.ac rehearsal). Process-level smoke test (test/mcp/entrypoint_test.rb): spawns `bundle exec bin/nabu mcp` against a tmp fixture corpus via NABU_CONFIG, speaks real newline JSON-RPC (initialize → initialized → tools/list → nabu_status), asserts clean exit 0 and stdout carried ONLY 2.0 protocol lines. Suite 839 green, lint clean. LIVE read-only handshake OK: proto 2025-11-25, three tools, nabu_status → 1,988,567 passages / 64,249 docs across 7 sources, index present, exit 0, stderr empty.
2026-07-07 · latin · — · Perseus Latin live (owner sync + sign-off): 290 original docs / 264,555 passages; translations flipped in the same change → +85 English editions / 75,921 passages (Aeneid pairs LINE-BY-LINE — verse-cited eng2, unlike Homer's prose cards); 290/290 originals skipped on resync (frozen guarantee). Census of the 108 eng quarantines: 101 are legacy P4-style TEI (<div1 type="poem"> — pre-edition/translation convention; honest out-of-contract, Phase 9 candidate), 3 empty, 2 malformed XML, 2 citation-depth. Corpus: 64,334 docs / 2,064,488 passages. First1K's ~45 eng files (1st1K-eng1 slug family) queued as a classifier follow-up.
2026-07-07 · P8-3 · af80d96 · Concordance: `nabu concord QUERY|--lemma FORM [--lang/--license/--limit/--width]` + MCP nabu_concord. KWIC formatter over Query::Search/LemmaSearch (Nabu::Query::Concord — a formatter, NOT a new query path): one row per hit, keyword column-aligned, left/right context trimmed to --width chars/side (default 40, … marks clip). DECISIONS: (a) fold-mapping — new Normalize.fold_with_map(text, language:) returns [folded, char-index map]; the keyword is located in the FOLDED passage form (same per-language P6-4 fold as the index) then mapped BACK to the PRISTINE accented text by index (the folds aren't length-preserving, so the map — not a naive index — is what makes ῆ line up under η). Pins byte-equality fold_with_map==search_form (Greek w/ combining marks) so the query, folded via search_form, is always found; prefix* keyword completes to the whole word (stops at space OR punctuation). Chose pristine display over showing the folded line — a concordance is for reading real usage. (b) occurrence policy — FIRST occurrence per passage, one row per hit (row count == hit count keeps the limit + MCP bounds contract clean; classic one-row-per-occurrence noted). (c) width default 40/side. (d) order — corpus (urn) order, re-sorted from the underlying query's page (Search by rank, LemmaSearch by urn), not rank order — a concordance is for scanning. Alignment: left context rendered to EXACTLY --width display chars (String#length; no East-Asian width), so the keyword's left edge is a fixed column; urn+[lang] tag per row. MCP nabu_concord is the 4th tool (tools/list count assertions updated across server/entrypoint/tools tests): same bounded (default 10/max 50, width max 120) + restricted-exclusion + license-fields-per-row + degradation contract as nabu_search, rows as structured left/keyword/right. Suite 870 green, lint clean. LIVE SMOKE (read-only) `bundle exec bin/nabu concord μῆνιν --limit 5`: 5 grc KWIC lines, keyword aligned, corpus (urn) order — tlg0006.tlg004:762, tlg0006.tlg012:1261, tlg0006.tlg014:1355, tlg0011.tlg003:656, tlg0011.tlg007:1328; lemma mode located suppletive ῥηθέντος/εἶπον/εἴπω for --lemma λέγω.
2026-07-07 · gate8 · — · Phase 8 unblocked packets complete, all live-verified: MCP protocol 2025-11-25 (verified against Claude Code's own binary), four read-only tools (search/show/concord/status) with license-on-every-passage + restricted default-exclusion + inode-tracking reopen (rebuild-safe mid-session); .mcp.json ships — open Claude Code in the repo and the corpus answers. Owner-feedback P8-1b: span-grouped parallel display (coverage cited in original numbering + clip notes; a range starting inside a card now finds its block — was all-dashes). Concordance: KWIC in pristine text via fold_with_map index mapping, CLI + MCP. Mid-phase owner events: perseus-latin live (290+85 docs; Aeneid pairs LINE-BY-LINE, verse-cited eng2) — corpus 64,334 docs / 2,064,488 passages, seven sources. P8-4 semantic / P8-5 glossing stay blocked on owner decision menus (embedder; API key+model) — carried forward, not abandoned. First1K eng classifier tweak + P4-TEI legacy class recorded as candidates.
2026-07-08 · P9-1 · d50fdd6 · First1KGreek English translations. First1kGreek#translation_slug_pattern added, mirroring its family-agnostic original rule (`/-eng(?<version>\d+[a-z]?)\z/`) — the base perseus-eng<n> classifier found nothing, since First1K's ~45 eng editions are 1st1K-eng<n> dominant (+ one opp-eng, + letter-suffixed 1st1K-eng1a/1b). registry: translations:true on first1k-greek (owner-directed 2026-07-08). Division anchoring verified across all 45 real eng files: every ingestible one anchors div[@type="translation"]; NO edition-typed eng file exists (base fallback inert). A real parse-only discover finds 43 eng works (highest -eng<n> per work); 41 parse clean, 2 quarantine HONESTLY (not adapter bugs, reported not hidden): tlg0527.tlg048.1st1K-eng1b is a mis-slugged div[@type="commentary"] appendix that wins version selection over the real eng1 translation (zero citable passages); heb0001.heb010.1st1K-eng1 is a Hebrew-namespace work mis-filed in greekLit whose div/@n is urn:cts:hebrewlit:… → inherited edition-urn cross-check rejects the greekLit urn. Fixture: new parallel pair tlg4037.tlg001 (Anonymus, De Incredibilibus) — smallest real eng translation (27 KB) whose grc sibling shares the section citation scheme; both trimmed to matching sections 1–3, aligning SECTION-FOR-SECTION (verse-style, all 1:1 pairs). Tests: flag-off byte-identical discover pin (frozen-urn), flag-on eng discovery/parse, family-agnostic acceptance, parallel-render over the fixture pair (verse pairs, both directions), translations-on conformance, registry build_adapter. Existing four originals' urns unchanged; the fifth grc (tlg4037) is the pair's original side. Suite 888 green, lint clean. Orchestrator recovery at acceptance: +41 eng docs, +2 honest quarantines.
2026-07-08 · P9-2 · a801abd · Legacy P4-TEI acceptance in EpidocParser — the third strategy. CENSUS (current quarantines, all sources, error-shape classification): papyri-ddbdp 9,309 no-citable-lines stubs + 3 textpart-@n (stay, known); perseus-greek 1 (Harpocration self-contradictory header, stays); first1k-greek 5 (3 commentary-only no-edition-div + the P9-1 pair, stay); perseus-latin 186 = 167 P4-class (101 eng "no div[translation|edition]" + 66 lat "no div[edition]") + 19 non-P4 that stay per class: 7 unsupported-replacementPattern-xpath (P5 union leaf steps `*[self::tei:l or tei:p]` — a P6-x extension candidate, not P4), 3 no-citable-both-passes, 3 binds≠matchPattern (Harpocration class), 4 malformed XML (undefined entity in fatal position / tag mismatch / extra content), 1 duplicate-urn-after-retry, 1 missing-@n-unsupported-xpath. THE P4 CLASS (every representative inspected, two sub-shapes): true TEI P4 (<TEI.2> DOCTYPE, unnamespaced, numbered div1/div2/div3 @type/@n containers, undefined ISO entities, <state>-refsDecl) and P5-namespace conversions keeping a <refState>-only refsDecl (body = div[@type=book] containers + milestone unit=chapter/section @n streams — Livy ~64 files dominant; Cicero orations milestone-only; Appendix Vergiliana lb-numbered verse; Boethius tractates bare-<p>). Legacy declarations PROVABLY untrustworthy (phi0692 declares book.chapter.section over lb-only verse; stoa0045 the same over letter divs) ⇒ citation minted from the BODY, never the declaration. FIX: P4 fallback in EpidocParser, gated on the exact no-division ParseError (disjoint from the P6-1 RETRYABLE set — the two retries can never race) AND no usable cRefPattern (a P5 doc without an edition div DECLINES and re-raises the original message byte-identical — verified against tlg0643: the first1k commentary/P9-1 quarantine reports are load-bearing). Three-rung ladder, each rung only if the previous minted zero: (1) container divs (component = @n, else @type+ordinal-among-same-type-siblings, else d+ordinal) + milestone hierarchy in first-appearance order (a milestone clears deeper components, any div boundary clears all; adjacent same-citation segments merge — a boundary that doesn't change the citation didn't create a unit; non-adjacent repeats → honest duplicate-urn quarantine); (2) numbered <lb/> as per-line citation; (3) bare-<p> 1-based ordinals. Same NFC/whitespace/note-drop discipline; P4 additions: <head> subtrees dropped (title apparatus), undefined entities resolved via a fixed table of the ISO names censused across all 167 files (&aelig; &mdash; …, unknown → space so words never fuse); no urn cross-check possible (P4 bodies carry no edition urn — caller's urn authoritative; language from the adapter slug as ever, folding minted by Passage.new). RESULT over the 167 real files: 159 recover (96 eng + 63 lat; Livy praefatio n="pr" mints, Cicero letters' key=value @n labels mint faithfully-ugly), 8 stay quarantined with both failures named — genuine upstream label collisions (Cato's praefatio div labeled chapter n=1 colliding with real chapter 1; Livy eng2 duplicate chapter-1 milestones; repeated letter keys; Valerius chapter-label repeats). FROZEN-URN: structural (fallback runs strictly after the unchanged first pass raised; decline path re-raises the original error object) + verified read-only vs live catalog: 875 loaded docs re-parsed (perseus-latin ALL 375, perseus-greek 250, first1k 250 sampled) → 0 urn mismatches, 0 text mismatches. Fixtures (4, latinLit): Livy AUC I lat3 TRIM (dominant milestone sub-shape, chapters pr/1/2 = 30 sections) + Tacitus Histories eng1 TRIM (TEI.2 numbered divs, &aelig; in passage text, book boundary) + De Institutione Viri Boni lat1 whole 4K (rung 2) + Boethius Quomodo Trinitas eng1 whole 8K (rung 3) — README + manifest updated; two whole-file rung exemplars beyond the packet's "1–2 trims" because untested ladder rungs would be worse than two 4–8 KB real files. Tests: per-rung urns+text spot checks, two-parse stability, entity/head rules, decline byte-identity pin, contradictory-P4 surgery quarantines precisely, ladder-exhausted surgery, golden urn+text digests of all pre-P6-1 fixtures unchanged, conformance green incl. new latinLit discovers. Suite 899/7453 green, lint clean. Expected recovery at the orchestrator's parse-only resync: perseus-latin +159 docs (errored 186 → 27); all other sources unchanged.
2026-07-08 · P9-3 · — · Live-resolvable lemma golden (orchestrator-direct): affero→attulimus (suppletive perfect) at proiel cic-off:86001 — a urn present in BOTH the fixture trim and the live corpus, so live health now exercises the lemma path (golden replay 11 found, up from 10). Also this session's recoveries: first1k eng +41 (P9-1), perseus-latin P4-TEI +159 incl. Livy AUC lat+eng (P9-2) — corpus 2,125,281 passages.
2026-07-08 · P9-4a · 1f947c3 · GRETIL scout + fixture plan. VERDICT: viable as a NEW bespoke parser family (opus), license `nc` — not the feared research_private. Inspected TEI mirror `mmehner/gretil-corpus-tei@master` verbatim: current corpus is mass-converted TEI P5 but NOT EpiDoc (no refsDecl/cRefPattern/CTS) → EpidocParser does not apply. THREE addressability classes: (1) hand-crafted minority (Rgveda-edAufrecht) with real div[@type/@n]→lg[@xml:id]→l[@n] + Vedic accents in choice/orig; (2) the bulk — flat lg/l with verse numbers embedded AS TEXT markers `// BrbUp_1 //`, zero attribute citation; (3) prose — flat <p>, no addressing at all. IAST (sa-Latn), NFC-friendly. License uniform in every header: CC BY-NC-SA 4.0 + good-faith/takedown disclaimer (GRETIL is aggregator, not rights-holder) → `nc`: ingestable local, MCP-excluded by policy, never redistributed. urn sketch urn:nabu:gretil:<slug>:<div-path|marker|seq>, non-addressable prose flagged. Scale ~784 TEI texts / ~240 MB (Sanskrit; Pali/Tibetan still legacy). Overlap with UD Sanskrit-Vedic = complementary (UD 4k annotated sentences sampled from RV/AV/MS/AB/ŚB; GRETIL full texts, no annotation, disjoint urn namespace). Fixture plan (3 texts: brahmabindUpaniSad whole, prajJApAramitAhRdayasUtra whole, Rgveda-edAufrecht trimmed to M1.S1-3) appended to backlog P9-4a for OWNER APPROVAL. docs/02-sources.md row → SCOUTED. No code, no bulk fetch.
2026-07-08 · P9-5a · a52e85f · ORACC scout + fixture plan (the founding dream — Nabu's own tablets). VERDICT: viable, cleanest new source since Perseus — new bespoke parser family (fable, ~DdbdpParser-tier), license `open` (CC0, better than the CC BY-SA the table recorded). Inspected real payloads via curl (upenn TLS fails in WebFetch; curl fine): rimanum.zip (2.9 MB, 337 P-texts) + saao-saa01.zip (5 MB, 265) + etcsri.zip (12.9 MB, 1456 Q-texts) + probes of rinap/riao/dcclt/blms/ribo. Open data = ORACC JSON: each corpusjson/<id>.json is a nested `cdl` tree of c (chunk: text>discourse>sentence) / d (discontinuity: object/surface obverse-reverse/line-start w/ label+n) / l (lemma) nodes. Transliteration line reconstructs by concatenating l-node `f.form` between line-start d-nodes tracking surface — VERIFIED (obv 1 = `2(BARIG) ZI₃ US₂ a-na GEŠBUN`, determinatives {ki}/{d}/{iti} + subscripts ZI₃/E₂/U₄ intact, NFC-clean). Every content l-node carries GOLD LEMMATIZATION: form/norm/cf(lemma)/gw+sense(English gloss)/pos+epos + gdl grapheme array — the annotations_json lemma-search goldmine (P7-5 path). Passage = LINE (line-start label o.1/r.5); text = transliteration; norm/cf/gw ride in annotations. TWO honest corrections to the brief: (1) PROSE TRANSLATIONS ARE NOT IN THE JSON — scanned all 265 SAA texts, node types {c,d,l} only, 0 translation nodes; running English lives in the ATF `#tr.en:` source layer / HTML → future parallel-doc job, out of v1; (2) delivery is per-project ZIP OVER HTTP (`/json/<project>.zip`, subprojects hyphenated saao-saa01, application/zip + Last-Modified), NO git repo (oracc/publicdata empty, oracc/json 404) → ORACC is the FIRST adapter that can't reuse GitFetch — needs a new HTTP-zip fetch path. Ids: P-numbers (artifacts: rimanum/saao) AND Q-numbers (composites: rinap 96, etcsri 1456) — urn:nabu:oracc:<project>:<P/Q>:<line-label>. LICENSE machine-readable per project AND per text (metadata.json + every corpusjson carry `license`/`license-url`): all 8 sampled projects report verbatim "This data is released under the CC0 license" → `open`; website footer still shows the stale 2014 CC BY-SA 3.0 (adapter reads the field, never hardcodes). Language: akk-x-oldbab (valid BCP-47 private-use → base akk) + sux, Sumerian logograms inside Akkadian via gdl.logolang (per-word lang in annotations, per-text primary for Passage#language). Effort: new family fable ≈ DdbdpParser (cdl walk simpler than Leiden XML, but P-vs-Q urn + translit folding rule akk/sux + lang + new HTTP-zip fetcher). Fixture plan: two mini-slices — rimanum (metadata whole + catalogue trimmed + P405432 rich/P405134 short/P405254 empty) + etcsri (metadata + catalogue trimmed + Q004151 + one more Q), <500 KB total, test/fixtures/oracc/, appended to backlog P9-5a for OWNER APPROVAL. RECOMMENDATION: P9-5b belongs as PHASE 10's HEADLINE (two net-new mechanics — bespoke JSON family + first non-git HTTP-zip fetch — over a non-IE family + founding-dream weight; Phase 9 already rich), not a Phase 9 tail; fully unblockable on fixture approval if the owner prefers it in 9. docs/02-sources.md row → SCOUTED. No code, no bulk fetch beyond format-inspection singles.
2026-07-08 · P9-4b · b1fd1bc · GRETIL adapter + `gretil` parser family (new, 5th bespoke family). MIRROR-SCOPE VERDICT: mmehner/gretil-corpus-tei DOES cover the full current TEI corpus despite a 2021 pushed_at — its 784 XML (781 sa_ + 2 xct_ + 1 ta-sa_) == the live corpustei/ count (cross-checked against the actively-maintained INDOLOGY/GRETIL-mirror, pushed 2026-02, whole-site ~1 GB), and the two whole fixtures are byte-identical to the live gretil.sub.uni-goettingen.de bytes; GRETIL's TEI conversion has been stable since 2021. So fetch STAYS on the shared git path (Adapter#git_fetch! → GitFetch, attic+breaker) against the mirror — no per-file HTTP crawler. MARKER CONVENTION (inspected in the real bytes, not guessed): `// BrbUp_N //` sits at the END of the last <l> of each <lg> — it CLOSES the verse it follows; text accumulated across the verse's <l> lines (space-joined), marker stripped, its number the citation; single/double daṇḍa (/ , ||) kept as reading text. ORIG/REG POLICY: the Rgveda has 175,308 BARE <orig> (0 <choice>, 0 <reg>) carrying the Vedic accents (combining U+0331 anudātta / U+030D udātta) — policy KEEPS <orig> as pristine accented text and DROPS any paired <reg> (the de-accented search form) + <note>, the deliberate INVERSE of DdbdpParser's Leiden keep-reg/drop-orig; the generic `san` fold strips the accents for text_normalized downstream regardless. URN SHAPES per rung: (a) attribute-cited pada — citation = l/@n verbatim → urn:nabu:gretil:sa_Rgveda-edAufrecht-m1s1-3:1.001.01a (lg/@xml:id RV_1.001.01 + div maṇḍala/sūkta as annotation context); (b) verse-marker → …:sa_brahmabindUpaniSad:1 (marker stripped); (c) prose ordinal → …:sa_prajJApAramitAhRdayasUtra:p1 (annotation addressing=prose-ordinal, flagged non-canonical). Slug = LITERAL filename stem (prefix kept: sa_brahmabindUpaniSad); language from <text>/@xml:lang, sa-Latn→san-Latn, cross-checked. Fixtures (3, byte/site-verified): brahmabindUpaniSad whole (22 verses), prajJApAramitAhRdayasUtra whole (8 prose paras), Rgveda-edAufrecht trimmed to header+M1.S1-3 (30 lg / 60 l, well-formed, whole:false) + README (both URL sets, verbatim CC BY-NC-SA notice, trim procedure, aggregator note) + manifest.yml. Registry gretil enabled:false / manual (owner-fired first sync). Streaming Nokogiri::Reader (5 MB whole Rgveda never DOM'd). Added gretil to fixture_manifests_test EXPECTED. Tests: conformance green + 20 source-specific (per-rung urns+text incl. accented Rgveda line preserved pristine + fold spot-check, marker-stripping, prose ordinals, two-parse stability, language map, local-git fetch, registry). Suite 920/8110 green, lint clean. 02-sources GRETIL row → READY. First real sync (owner-fired): git clone of ~784 texts / ~240 MB, minutes not hours; expect ~all Sanskrit, addressed by the three rungs.
2026-07-08 · P9-6 · 578d5fe · Slavic sources survey (docs/slavic-survey.md; scouting only, no code, no bulk fetch — page-level WebSearch/WebFetch + gh metadata). Surveyed the owner's OCS/OES/Church-Slavonic axis beyond the two live PROIEL-family treebanks (torot + proiel). RANKED ≤2 FOR PHASE 10: #1 UD SLAVIC TREEBANK EXPANSION — the `ud` adapter's TREEBANKS map (verified in-tree) lists only gothic/greek-proiel, sanskrit-vedic, latin-ittb; adding `old-east-slavic-birchbark` (RNC Corpus of Birchbark Letters, vernacular OES 1025–1500, letters/household records/charms, ~27k tokens) + `old-east-slavic-rnc` (Middle Russian 1300–1700, legal/correspondence/narrative/folklore) — BOTH README-verified `CC BY-SA 4.0` (= `attribution`, MCP-safe), CoNLL-U, reusing ConlluParser + UD multi-repo fetch/probe/dedup, urn:nabu:ud:<tb>:<sent_id> — is a config+fixture change with ZERO new parser family, and the data is genuinely new (absent from TOROT/PROIEL). DEDUP HAZARD flagged: deliberately EXCLUDE UD's chu-PROIEL/orv-TOROT conversions (they re-load the native sync). #2 CCMH (Corpus Cyrillo-Methodianum Helsingiense, Kielipankki/CLARIN) — 7 canonical OCS texts (Assemanianus, Marianus, Suprasliensis, Zographensis, Savvina kniga + Vitae Constantini/Methodii) as transliteration + SIMPLE structured XML (NOT TEI/CTS/CoNLL-U → new small bespoke family), openly downloadable, catalogue marks `Open`; real gain = Codex Assemanianus + Savvina kniga (absent from current holdings) + alt editions (distinct versions, never dedupe); adapter reads the exact CC at ingestion, catalogue warns "encoding … very simple, not all texts properly checked." HONORABLE MENTION: obdurodon Codex Suprasliensis critical edition (Miltenova & Birnbaum) — richest single OCS ms + parallel Greek/glossary, license quoted verbatim "Creative Commons BY-NC-SA 3.0 Unported" → `nc` (local-ingestable, MCP-excluded), citation text·folio·side·line, but XML is per-text on the website with no located bulk repo (gh shows only djbpitt/repertorium metadata + normalization tooling) → per-text HTTP crawl, and overlaps TOROT's Suprasliensis as a fuller alt-edition; behind #1/#2. NOT-INGESTABLE, SURVEYED-BLOCKED (unblock paths in the survey): TITUS-Frankfurt (quoted "used freely for scholarly purposes … must not be used for any kind of commercial usage," some texts members-only — no redistribution grant, no standard license, legacy HTML/custom encodings, dir index 403s bots → `research_private` at best; OCS covered more cleanly by CCMH/obdurodon); RNC full historical corpora at ruscorpora.ru (query-only, "texts cannot be distributed" — but its `CC BY-SA 4.0` UD releases ARE pick #1); "Манускриптъ"/Manuscript (manuscripts.ru, Udmurt State Univ., 130+ mss / >3.5M word occurrences, retrieval system with no export — write for a research_private grant, high potential); Sreznevsky Materialy (page scans only on HathiTrust/PrLib, no machine-readable TEI — HTR/OCR future); SEENET/eSlavistik (no distinct open machine-readable corpus located under those names). PHASE-10 SHAPE: ORACC stays headline (P9-5b); pick #1 rides alongside as the smallest-possible companion (TREEBANKS-map + fixtures, no fixture-approval ceremony beyond confirming two repos), pick #2 as the follow-on scout→plan→adapter track; both top picks `attribution`/`Open` so they improve shareable+MCP Slavic coverage, not just local. Overlap-dedup discipline (the OCS canon recurs across TOROT/UD-chu/CCMH/obdurodon) called out as a per-packet hazard. docs updated: new docs/slavic-survey.md; 02-sources.md rows — TITUS #13 → SURVEYED-BLOCKED, CCMH #19 → SURVEYED (rewritten, format/license corrected), UD #4 note, new #30 obdurodon-Suprasliensis (SURVEYED) / #31 RNC / #32 Manuscript / #33 Sreznevsky (SURVEYED-BLOCKED); backlog P9-6 → done + Findings block.
2026-07-08 · gate9 · — · Phase 9 complete: First1K eng +41 (section-aligned De Incredibilibus); P4-TEI ladder +159 perseus-latin (Livy AUC lat+eng, book.chapter.section citations; census across all sources, 8 remaining latin quarantine classes named); live lemma golden (affero→attulimus, health 11 found); GRETIL scouted (CC BY-NC-SA → nc) AND shipped (new parser family: 3 addressability rungs, keep-orig Vedic accents — inverse of Leiden; mirror verified 784 files, GitFetch path; enabled:false awaiting owner sync); ORACC scouted (CC0! gold lemmas in JSON; deferred to Phase 10 headline, fixture plan pre-approved); Slavic survey ranked: #1 UD Birchbark+RNC-MiddleRussian (config-only, CC BY-SA), #2 CCMH (Assemanianus+Savvina, small new family); TITUS/RNC-full/Manuscript/Sreznevsky honestly BLOCKED. Corpus: 64,534 docs / 2,125,281 passages, 872 aligned English editions.
2026-07-08 · P9-4c · 7627a14 · GRETIL quarantine recovery (118 → 8). Census-first (orchestrator's 2026-07-08 census: 663 clean / 118 quarantined of 781 = 45 collision + 73 no-citable); three fixes, all frozen-urn-safe by construction, VERIFIED by a read-only two-parse census over every canonical file (663 clean docs byte-identical, 0 violations) AND the live parse-only sync (+110 added, ~0 updated, =663 SKIPPED, !8 errored — the loader's content_sha256 matched every previously-loaded doc, the standing frozen-urn proof). FIX 1 — the xml:id rung (~60 no-citable files: Rāmāyaṇa, Buddhacarita, Gītagovinda, Kirātārjunīya, Paippalāda Saṃhitā): `<lg xml:id="RgV_1.1.1">` with markerless `<l>` children → citation = the id, `RgV_` prefix stripped, dotted path kept (`1.1.1`); the lg IS the passage (its `<l>` are pada rows). CRITICAL DESIGN (the census caught it): rung d is NOT emitted in-line — it is a SECOND streaming pass (XmlIdExtraction) run ONLY when the primary rungs leave the doc empty. An in-line emit rewrote 144 already-loaded docs that carry `<lg xml:id>`/`<p xml:id>` AND prose and were loaded via the prose rung (their group text mashed into `pN` ordinals) — a frozen-urn violation. p-level xml:id is DELIBERATELY NOT recovered: a `<p xml:id>` carrying text already emits as a prose ordinal in the primary pass (clean-as-prose, frozen); recovering it would break the guard — flagged for a future packet. FIX 2 — single-pipe markers `| BBs_1,1.1 |` (~13 files, sa_bAdarAyaNa-brahmasUtra): the recognizer accepts `|`-delimited markers (matched-delimiter regex, `_<digit>` requirement rules out lone daṇḍas) but ALSO only as a fallback pass — keeping the primary MARKER byte-identical to P9-4b (`//`-only). Applying pipe in the primary pass broke 45 clean docs that merely contain `| … |` reading-text spans (read as prose before). FIX 3 — collision tolerance (45 files): a duplicate citation no longer quarantines; instead deterministic `:b2`/`:b3` suffixing in document order (ddbdp precedent — the real upstream dup Valc_1.70×2), PLUS a multi-prefix join for interleaved kārikā/commentary — when marker verses carry ≥2 distinct prefixes AND their bare numbers collide, the prefix joins the citation (DhvK_1.1/DhvA_1.1 → `DhvK.1.1`/`DhvA.1.1`). Both run in the primary pass but are provable no-ops on clean docs: collision-suffix fires only on duplicates (a clean doc has none, else it'd have quarantined), and multi-prefix is GATED on a real bare-number collision (the census found 26 files with ≥2 `//`-prefixes but only a handful collide — the rest were loaded cleanly with bare citations; unconditional prefixing would rewrite them). Single-prefix files never reach the join (guard test pins this). PERF: fallback order is xml:id BEFORE pipe — a markerless xml:id doc accumulates its whole body into one buffer, and running the `(//|\|)` recognizer over that megabyte buffer re-scans at every daṇḍa (quadratic, minutes on a purān_a); draining xml:id docs first keeps the pipe pass linear (pipe docs flush at every marker). RESIDUE — 8 no-citable stay quarantined, censused and REPORTED not force-fixed (packet scope): 4 genuinely unaddressable (paramArthasAra, bIjanighaNTu, lagadha-RgvedavedāṅgajyotiSa, vāmadeva — flat `<lg>`/`<l>`, zero `@n`/xml:id/marker/prose) + 4 recoverable marker-format variants OUT OF P9-4c SCOPE (vimalamitra-abhidharmadīpa `// Abhidh-d_N //` = hyphenated prefix the regex rejects; sātvatatantra/somānanda/puruSottamadeva `// Abbr_N</l>` = leading-`//`-only markers with the `</l>` as terminator, no closing delimiter) — a clean follow-up packet. Fixtures (4, trimmed real slices from local canonical, README + manifest updated): sa_RgvidhAna-a1 (xml:id), sa_bAdarAyaNa-brahmasUtra (pipe, whole), sa_AnandabhaTTa-vallAlacarita-c1 (real dup 1.70 → 1.70:b2), sa_Anandavardhana-dhvanyAloka-comm-u1 (DhvK/DhvA multi-prefix). Tests: per-fix urns+text, two-parse stability (all 7 fixtures), single-prefix guard (Brahmabindu stays bare `1`…`22`, never `BrbUp.N`), conformance green. Suite 925 green, lint clean. 02-sources GRETIL row updated (four rungs + recovery classes).
2026-07-09 · P10-1 · 93ca5ba · ORACC adapter + parser family (the founding dream ships). Executed the owner-approved P9-5a fixture plan exactly: rimanum.zip + etcsri.zip downloaded once to scratch, EXACT slices extracted to test/fixtures/oracc/ (rimanum metadata whole + catalogue trimmed to 3 P-ids + P405432/P405134 whole + P405254 kept as its true 0 bytes; etcsri metadata whole + catalogue trimmed to 2 Q-ids + Q004151 whole + SECOND Q = Q001299, the smallest non-empty Q at extraction, 2,980 B "Anonymous Nippur 45" — single-line + no-surface-labels case), zips deleted after; README (retrieval 2026-07-09, URLs, verbatim CC0 notice, trim procedure, translations-not-in-JSON + HTTP-zip notes) + manifest.yml (all entries refetchable:false provenance zip-extract — no raw per-file URLs exist) + fixture_manifests EXPECTED. HONEST size note: etcsri metadata.json is 377 KB (plan estimated ~30 KB; big formats/witnesses blocks) — kept whole per plan rationale, fixture total 536 KB vs the <500 KB estimate. NEW PARSER FAMILY OraccJsonParser (6th, first JSON): recursive c/d/l cdl walk (node-less linkbase hashes skipped); passage = line-start d-node, text = l-node f.form joined with spaces (nonw/nonx fragments never reading text); UPSTREAM REALITY the scout didn't cover — cof-head/cof-tails continuation pairs (542 in rimanum: one written form NIG₂.ŠU = two lemma words ša+qātu) → a cof continuation contributes NO text but keeps its token (else the sign doubles); tokens carry form/lemma(=cf)/norm/gw/sense/pos/epos/lang + per-grapheme logolang (recursed through gdl group/seq — EN.ZU inside {d}EN.ZU-še-mi is sux), sentence labels ride per-passage ("sentences"); morph/morph2 deliberately not ingested (enrichment-shaped follow-up). Language: per-text primary = majority base subtag over l-nodes (akk-x-oldbab→akk; P405432 is akk with sux year-name lines, honest per-word lang in tokens), no language: param — it lives in the data. URNs FROZEN: urn:nabu:oracc:<project ("/"→"-")>:<textid>:<label spaces→dots> (o.1, r.5, seal.1.1’ — primes kept; labels verified unique per text across BOTH whole projects); parser mints from project/textid and cross-checks caller urn. Empty corpusjson (40 of 378 in rimanum) = catalog-only upstream norm → discover SKIPS (not quarantine), fetch notes count them ("338 texts, 40 catalog-only (empty)"). LICENSE READ per project from metadata.json, mapped CC0→open / CC BY-SA→attribution; unmapped OR class-diverging → FetchError STOP (never mislabel); gate skipped where metadata.json absent (the attic — texts were gated live). LEMMAS: tokens use the treebank "tokens"/"lemma"/"form" contract → passage_lemmas rows via the UNCHANGED Indexer (fixture load: 4 docs / 20 passages -> 41 lemma rows, 29 akk + 12 sux; `search lemma:qemu` → P405432:o.1 attested as ZI₃, end-to-end test). FOLDING: new shared akk/sux rule in Normalize.LANGUAGE_FOLDS — sign-join ./- and {} → space (bare sign readings as tokens), subscript ₀-₉ₓ → ASCII (ZI₃→zi3); per-codepoint (tr only) so fold_with_map equality holds; query_forms picks it up automatically; conventions §9 row documents the mid-word-determinative phrase trade-off. FIRST NON-GIT FETCH: Nabu::ZipFetch (new, lib/nabu/zip_fetch.rb) mirrors GitFetch phase-for-phase — prepare! (GET + unpack to staging, live tree untouched, doomed = live minus fresh) / guard / complete! (attic-before-delete, SAME .attic.json format with zip-sha256 as the FETCH_HEAD analog → base-class attic rediscovery + RETIRED_SHA_KEY work unchanged, then tree swap); Last-Modified/If-Modified-Since via .zip-fetch.json state (304 = untouched); system unzip via Shell.run (NO new gem); adapter drives UD's all-prepare-then-guard-then-complete choreography across projects, FetchReport.repos keyed by zip URLs. Registry: oracc enabled:false (owner-fired first sync pending) / manual / translations:false. Scope: rimanum + etcsri (Oracc::PROJECTS). Tests: conformance + 16 parser + 22 adapter (incl. WebMock-stubbed REAL-fixture zips, 304 path, attic, breaker trip + force-attic-rediscover) + 8 ZipFetch + folding; suite 973/12,012 green, lint clean. FLAGGED for P10 gate: `nabu health --remote` git-ls-remotes the manifest URL → oracc reads gone until the probe learns non-git upstreams (left out of scope deliberately). docs: 02-sources row → READY; architecture §8 ZipFetch bullet; conventions §9 row. NO real sync run (owner-fired).
2026-07-09 · P10-2 · aada0c5 · UD Slavic treebank expansion (survey pick #1): added old-east-slavic-birchbark + old-east-slavic-rnc to the `ud` adapter's TREEBANKS map (both language `orv`, repo/lang shape identical to the existing four, no new parser family — pure ConlluParser + UD multi-repo fetch/discover reuse). LICENSE GATE PASSED before committing fixtures: each repo's LICENSE.txt AND README machine-readable metadata verbatim `CC BY-SA 4.0` (Attribution-ShareAlike 4.0 International) — the packet's stop-if-different condition never fired. Fixtures: one trimmed real slice per treebank (first 50 complete sentence blocks of orv_birchbark-ud-test.conllu 1,446,600→63,865 B and orv_rnc-ud-test.conllu 2,483,580→365,977 B; neither split has any MWT `n-m` or empty-node `n.m` line so head-50 is representative as-is; the RNC slice is large because Middle-Russian chancery sentences run 2–396 tokens, mean ~86 — honest real data, kept at 50 for parity); README (retrieval 2026-07-09, repo URLs, verbatim license quotes, trim procedure) + manifest.yml + no fixture_manifests EXPECTED change (ud already listed). DEDUP GUARD (survey hazard): new test asserts TREEBANKS excludes UD_Church_Slavonic-PROIEL and UD_Old_East_Slavic-TOROT (repo-name pins) and carries no `chu`-language treebank — those conversions would double-load the native proiel/torot syncs; the two orv treebanks added are RNC-scheme, no PROIEL/TOROT overlap. URNs FROZEN urn:nabu:ud:<treebank>:<sent_id> (e.g. urn:nabu:ud:old-east-slavic-birchbark:orv_birchbark-ud-test-head50:002-1); conformance two-parse stability + idempotency inherited unchanged. Lemma acceptance: fixture load → passage_lemmas orv rows via the UNCHANGED Indexer plumbing (asserted: birchbark росомуха NOUN @002-1 attested by surface росомꙋха, and both treebanks contribute rows). Adapted the multi-repo mass-deletion breaker test to the grown 6-treebank set (delete 2 of 6 = 33% > 20% to still trip). Registry unchanged (ud already registered, enabled untouched). 02-sources UD row → 6 treebanks. Suite 975/12,660 green, lint clean. NO real `bin/nabu sync ud` (owner-fired at gate).
2026-07-09 · P10-3 · fe2d2bc · GRETIL residue micro-packet (quarantines 8 → 4). Census-first: inspected all 4 recoverable files in canonical/gretil/ before coding — (a) sa_vimalamitra-abhidharmadIpa carries CLOSED hyphenated markers `// Abhidh-d_N //` (597, ALL line-final, one per <l>, never mid-line — the primary MARKER's prefix charset rejects the hyphen); (b) sa_sAtvatatantra (701) / sa_somAnanda-zAktavijJAna (30) / sa_puruSottamadeva-ekAkSarakoza (27) carry leading-`//`-only markers `// Abbr_N</l>` (no closing delimiter; the `</l>` boundary terminates; verified 100% line-final in all three, and NONE of the three ever has a closed `// … //` marker — no shape mixing). FIX — one new recognizer covers both shapes: LINE_MARKER (`//` + hyphen-admitting prefix + `_num` + OPTIONAL closing `//`, anchored `\z`) applied PER <l> line (never scanned over the running buffer — a lone daṇḍa can't anchor to \z, and per-line testing keeps a large markerless file linear) in a new LineMarkerExtraction streaming pass, run as the LAST fallback rung (primary → xml:id → pipe → line-marker), firing ONLY on docs the earlier passes leave empty. The P9-4c discipline held: MARKER and MARKER_FALLBACK stay BYTE-IDENTICAL; emitted addressing is "verse-marker" with the captured prefix, so multi-prefix join + :b<k> collision suffix apply unchanged (none of the four recovered files turned out to carry a duplicate citation — all 1,355 recovered passages minted bare unique citations, verified in the live db). FROZEN-URN PROOF: read-only two-parse census over canonical/gretil/ before/after (781 censused docs each; scratch scripts p103_census/p103_diff) — before 773 OK / 8 quarantined, after 777 OK / 4 quarantined, 0 byte-identity violations among the 773, 0 regressions, 0 two-parse instabilities; live `bin/nabu sync gretil --parse-only`: +4 added ~0 updated =773 skipped !4 errored (content_sha256 matched every previously-loaded doc). RESIDUE (4, genuinely unaddressable flat <lg>/<l> with zero @n/xml:id/marker/prose, stay quarantined by design): sa_abhinavagupta-paramArthasAra, sa_bIjanighaNTu, sa_lagadha-RgvedavedAGgajyotiSa, sa_vAmadeva-janmamaraNavicAra (NB the P9-4c worklog's ambiguous "paramArthasAra" is the abhinavagupta one, pinned by this census). URNs minted: …:sa_vimalamitra-abhidharmadIpa:1 (hyphenated closed), …:sa_sAtvatatantra:1.1 (leading-only). Fixtures (2, trimmed real slices cut from LOCAL canonical — no network; README + manifest updated, retrieval noted, CC BY-NC-SA 4.0 `nc`): sa_vimalamitra-abhidharmadIpa-h1.xml (teiHeader + first 4 lg, closed hyphenated markers) + sa_somAnanda-zAktavijJAna-l1.xml (teiHeader + first 4 lg, leading-only markers); trim suffixes -h1/-l1 keep fixture urns off the real slugs. Tests: per-shape urns+text+marker-stripping, two-parse stability over all 9 fixtures, guard that the line-marker pass does NOT fire on a primary-marker doc (Brahmabindu keeps bare 1…22). Suite 978 green, lint clean (138 files). 02-sources GRETIL note updated (P10-3 clause + corrected residue names).
2026-07-09 · gate10 · 3a6bc8b · Phase 10 complete: ORACC shipped (new OraccJsonParser family — cdl tree walk, line-grain passages, cof continuation pairs, gold cf/gw lemmas → passage_lemmas for akk/sux; new ZipFetch HTTP path with GitFetch-parity attic + Last-Modified detection; license READ per-project CC0→open, never hardcoded; enabled:false awaiting owner sync + starter-project pick); UD Slavic expansion (birchbark + RNC Middle Russian, both verified CC BY-SA 4.0 = attribution/MCP-safe, dedup guard pinned excluding chu-PROIEL/orv-TOROT conversions; awaiting owner sync); GRETIL residue recovered (line-terminated marker rung: hyphenated `// Abhidh-d_N //` + leading-//-only; quarantines 8 → 4, +4 docs/+1,355 passages, frozen census 773/773 byte-identical; remaining 4 genuinely unaddressable flat lists). Known gap flagged: remote health probe is git-shaped, reads ORACC HTTP upstream as gone — probe follow-up packet for Phase 11. Corpus: 65,311 docs / 2,822,794 passages. Suite 978/12,776, lint clean.
2026-07-09 · P10-4 · e2c47b4 · Per-treebank license override plumbing (fix: the two P10-2 Slavic treebanks are CC BY-SA 4.0 but `show`/MCP reported them `nc` because `documents.license_override` — honored by the whole query layer since P1-3 — had no write path; no adapter had ever set it). SEAM: the override rides `Nabu::Document#license_override` (nil default, the value object the loader already consumes on every persist path — both `load` and `load_from`, create AND update — so no new plumbing seam is invented; the alternative, DocumentRef, only reaches `load_from` and would need re-plumbing for the `load([doc])` path the loader tests use). Validated at construction via new `Model::Validation.license_class_or_nil!` (fail-fast at the domain boundary; the db CHECK on documents.license_override stays the backstop, not the UX). Adapter→Document flow: `UniversalDependencies::TREEBANKS` gains optional `:license`/`:license_class` (the two `old-east-slavic-*` entries set `attribution` / "CC BY-SA 4.0"; the four legacy entries omit them → override NULL → inherit the source's nc); `document_refs` carries `license_class` in the discover→parse `DocumentRef.metadata` hint channel ONLY when declared (legacy refs stay byte-identical); `ConlluParser#parse` gains a nil-default `license_override:` kw forwarded to Document (parser stays license-agnostic). REVISION SEMANTICS (the crux): `license_override` is deliberately kept OUT of `Store::ContentHash.document`, so a relabel cannot change content_sha256; the loader writes it on insert (`insert_document`) and on content-revise (`revise_document`), and — for the same-content case (override newly declared, changed, or removed upstream) — reconciles it on the SKIPPED path via new `reconcile_license_override?`, a pure metadata update modeled on `reconcile_retirement?`: no revision bump, no passage work, content_sha256 untouched, not journaled (matches how a title ride-along isn't its own event). An override removed from the map reverts the column to NULL on next load. TESTS: loader — `test_load_persists_license_override_on_create`, `test_relabel_on_reload_is_metadata_only_no_revision_bump_no_content_change` (pins revision==1 + content_sha256 unchanged across a nil→attribution relabel), `test_reload_with_same_override_is_idempotent` (skipped, no drift), `test_override_removed_upstream_reverts_to_null_on_next_load`, `test_content_revision_carries_the_current_override`, `test_invalid_license_override_is_rejected_before_any_write`; UD — `test_treebanks_map_sets_attribution_only_on_the_two_slavic_entries`, `test_parse_surfaces_license_override_for_slavic_and_nil_for_legacy`, `test_fixture_load_writes_attribution_override_only_for_the_slavic_treebanks` (end-to-end: two Slavic → attribution, four legacy → NULL, source class still nc). Suite 990/12,825 green, lint clean (138 files). Live relabel of the six docs is the orchestrator's post-review step (not run here; db untouched). 02-sources UD row gains the split-licensing sentence; backlog P10-4 → done.
2026-07-09 · P11-1 · 0ce5505 · Old English / Anglo-Saxon sources survey (docs/oe-survey.md; scouting only, no code, no bulk fetch — page-level WebSearch/WebFetch + gh api metadata + raw-header peeks + one 2.2 MB sample TEI inspected in scratch and discarded). The corpus has ZERO OE today. RANKED ≤2: #1 ISWOC TREEBANK (Oslo, Bech & Eide) — PROIEL XML 2.1, the exact schema proiel/torot already parse (verified in raw wscp.xml: schema-version="2.1", same proiel.xsd, <source id="wscp" language="ang">); 5 OE texts ≈ 29,406 gold-annotated tokens (Ælfric's LIVES OF SAINTS — packet lead said Catholic Homilies, corrected — Apollonius of Tyre, Anglo-Saxon Chronicles, Orosius, West-Saxon Gospels) + 10 medieval Romance texts to language-filter out; license verbatim (repo README): "freely available under a Creative Commons Attribution-NonCommercial-ShareAlike 3.0 License", per-source headers agree → nc, same class as its PROIEL siblings; adapter = TOROT-pattern subclass + ang filter, near-config-only; repos: iswoc/iswoc-treebank (frozen, README redirects) → successor syntacticus/syntacticus-treebank-data (MUST scope to iswoc/ subdir — its proiel/+torot/ dirs are the already-synced data, the UD-conversion double-load trap again); urn: inherit urn:nabu:proiel:<source-id> (NB non-ASCII id æls). BIBLICAL-AXIS ANSWER (the owner's ask): ISWOC wscp IS the best machine-readable open OE Gospel edition and it is the GOSPEL OF MARK COMPLETE (chs 1–16, 671 verse citations) + fragments Matt 7/John 1 — NOT four Gospels despite the "West-Saxon Gospels" title; native citation-part="MARK 1.1" refs are already lifted by ProielParser (proiel_parser.rb) → OE Mark = drop-in sixth P11-3 hub witness (vs greek-nt/latin-nt/gothic-nt/armenian-nt/marianus) with zero citation plumbing; the honest demo verse is a Mark verse, not John 1:1. Full-tetraevangelion paths, all costly: YCOE cowsgosp.o3 conversion (Penn bracketed + OTA noncommercial) or PD reconstruction from Skeat 1871–1900 (WS+Latin+Lindisfarne+Rushworth parallel, scans only) / Bosworth & Waring 1874 (Gothic+WS parallel — scaffold vs our gothic-nt); NO open TEI OE Gospels edition exists. Provenance note: ISWOC wscp text imported from YCOE (header verbatim) — distinct editions if both ever ingested, never dedupe. #2 ASPR via OTA 3009 — the complete six-volume Krapp & Dobbie Anglo-Saxon Poetic Records as ONE 2.2 MB TEI-P5 file, fetched WITHOUT auth and verified in-file: 374 texts / ~30.5k verse lines spanning Beowulf ("Hwæt! We Gardena…" confirmed), Junius, Vercelli, Exeter, Paris Psalter, Minor Poems (Cædmon's Hymn in Northumbrian AND West-Saxon versions, Bede's Death Song ×3); license verbatim FROM THE TEI HEADER: "Distributed by the University of Oxford under a Creative Commons Attribution-ShareAlike 3.0 Unported License" → attribution, the ONLY fully-open structured OE found, MCP-safe; format TEI P5 but NOT EpiDoc/CTS (no refsDecl; div→head+l*, <caesura/>, <unclear>; NO l/@n anywhere → ordinal line citations, urn:nabu:aspr:<poem-slug>:<line-ordinal>) → new small bespoke family (First1K-sized) + first single-file HTTP fetch; early-90s e-text, fixture archaeology expected. DICTIONARY SHELF (P11-4 pattern): Bosworth-Toller official LINDAT dump (Tichý/Charles Univ, hdl 11234/1-3532, v0.1 2021) — license verbatim on the record: "Attribution 4.0 International (CC BY 4.0)"; SQL + lemma-keyed CSV id;headword;body (body project-XML, not TEI; id → bosworthtoller.com/<id>); record caveat "Not all entries have been checked and/or tagged"; GLP digitization (effectively PD) as fallback. SURVEYED (possible later): YCOE+YCOEP (OTA 2462/2425, ~1.5M words OE prose canon incl. the FULL four WS Gospels + 71,490 words poetry; Penn-Helsinki .psd/.pos = NEW parser family; OTA "ACA Academic Use" verbatim "only for purposes of non-commercial research or teaching" + York layered-copyright caveat, no text-layer redistribution grant → nc…research_private posture); PerseusDL/canonical-angLit (stray CC BY-SA TEI Beowulf + ENGLISH TRANSLATION, stale 2015 — translation maybe useful when ASPR ships); Helsinki Corpus OE slice (OTA 1477, dominated by YCOE). SURVEYED-BLOCKED (unblock paths in the survey): DOEC (~3M words, the complete OE record, subscription product $75/$200/yr + 20 free logins; terms verbatim §3 "Recompiling, copying, publication, or republication of the data, or any portion thereof … only with specific written permission"; TDM prohibited; unblock = written permission, or verify the 2000 release sitting on OTA as record 2488 academic-use ~26 MB); OE Poetry in Facsimile (~350 works transcribed, Digital Mappa web-app, "open-access" = access not a reuse grant; ask Foys/UW); Electronic Beowulf (© Kiernan + British Library); Jebson ASC (fullest free ASC A–E, verbatim "Copyright © 1996-2006, Tony Jebson … all rights reserved", XHTML only, TEI-P4 source unpublished; email for a grant); Digital Ælfric (commercial); CoNE/PASE/LangScape (non-commercial-only/metadata/post-project). UD: NO Old English treebank exists (verified against the org repo list — the hoped-for config-only add is unavailable). MENOTA: confirmed NO OE (Norse/Nordic only). PHASE SHAPE: #1 feeds P11-3 directly (OE Mark as a registry entry proving "sixth version = registry entry, not code" on a NEW language); #2 is the first shareable OE (attribution) → scout→plan→adapter track; B-T slots into P11-4 if the shelf schema stays language-agnostic; the treebanked axis is nc-or-stricter while the open material is poetry+dictionary — flag in MCP-coverage claims. docs: new docs/oe-survey.md; 02-sources rows #34 ISWOC / #35 ASPR / #36 Bosworth-Toller / #37 YCOE+YCOEP (Tier 2) + #38 DOEC / #39 OE web-app editions (Tier 3) + UD #4 no-OE note + Menota #21 no-OE note; backlog P11-1 → done + Findings. No code, no fixtures, suite untouched.
2026-07-09 · P11-2 · 00ff07d · HTTP remote-health probe: `nabu health --remote` no longer reads ORACC as gone. Strategy is now keyed per source off the adapter (new `Adapter.remote_probe_strategy`, default `:git`; `Adapter.http_probe_targets` → HttpProbeTarget(label, zip_url, metadata_url, state_subdir)). The git path (ls-remote liveness, HEAD-vs-`last_sync_sha` drift, raw.githubusercontent license baseline) is UNTOUCHED — all 18 pre-existing git tests stay green. ORACC overrides to `:http_zip`: per-project HEAD on the zip URL (200 = alive; 301-308 = moved; else — incl. ORACC's live 500-for-unknown — = gone) carrying `Last-Modified`, diffed against the on-disk `.zip-fetch.json` pin (`<canonical>/<slug>/<project>/`) for drift (`current`/`behind`/`never_synced`/`unknown`); license drift = GET the small standalone `metadata.json`, hash its `license` field, compare/record on the SAME ledger pin + compare_license baseline mechanism the git sources use (pins keyed by zip URL, which is how the sync path already pins ORACC). Aggregation reuses the multi-repo shape verbatim (worst-wins liveness/drift with offenders named, any-changed license). Never-synced project → reported `never_synced`, not gone; probe never mints pins. Both HTTP halves go through `ZipFetch.default_http` (system trust store + vendored InCommon intermediate) — a bare Faraday would fail TLS on oracc.museum.upenn.edu exactly as sync did. RemoteProbe gains `canonical_dir:` (CLI passes `config.canonical_dir`) to locate the state files. LIVE SHAPES recorded 2026-07-09 (a few HEADs + one metadata GET, no sync): rimanum.zip/etcsri.zip HEAD 200 + `Last-Modified` (`Fri, 28 Jun 2024 12:46:37 GMT` / `Wed, 14 Aug 2024 14:47:17 GMT`) + `content-type: application/zip`; unknown project HEAD **500** (not 404); standalone `metadata.json` lives at `/<project>/metadata.json` (NOT `/json/...`, which 500s) and GETs 200 `application/json` but with an **EMPTY body** over HTTP on both oracc.museum.upenn.edu and oracc.org — so the license row reads `unchecked` live (best-effort; the metadata field-compare code is implemented and stubbed-tested for when upstream serves it, and drift via zip `Last-Modified` is unaffected). GAP FLAGS REMOVED (the P10-1 "reads as gone" flags): oracc adapter class header note, architecture §8 ZipFetch bullet, 02-sources ORACC row; ops.md `--remote` bullet + troubleshooting rewritten. Tests: +10 WebMock-stubbed http-zip cases (reachable/current, behind+offender, never-synced-not-gone, gone-on-500, license baseline-recorded/unchanged/changed/empty-body-unchecked, real-Oracc-adapter-not-gone). Suite 999/12,852 green, lint clean. NO real sync run.
2026-07-09 · P11-3 · 27789c7 · Cross-source alignment hub (improvements §1.2; architecture §10 written FIRST as the design record). Verified citation reality in the live catalog before designing: PROIEL passage urns are sentence ids, verse identity lives in per-token citation_part ("MARK 2.3") in annotations_json, sentence↔verse many-to-many (846 greek-nt spanners), and the packet's John 1:1 is NOT five-way (absent from gothic-nt + marianus) — demo verse is MARK 2.3. Shipped: config/alignments.yml (Nabu::AlignmentRegistry, loud-fail conformance loader, five-way NT flagship + prepared OE-Mark line, per-witness books: aliases, closed extractor set proiel-citation) + derived alignment_refs in fulltext.sqlite3 (Store::AlignmentIndexer, the P7-5 annotation-derived-index pattern — one row per work/normalized-ref/passage, rebuilt inside Indexer.rebuild! at both call sites, NO catalog migration; licenses/labels never stored — resolved at query time) + Query::Align (registry-order witnesses, effective license label on every one, honest :ok/:no_match/:not_synced, sentence ref-spans, passage-urn pivot) + `nabu align REF [--work]` (new subcommand — Parallel's CTS-suffix pairing is a different lookup model, kept separate) + MCP nabu_align (fifth tool; restricted witnesses withheld bodily unless include_restricted). Rejected: materialized pairs (O(witnesses²), stale on the sixth witness). Refs fold both sides (upcase/space/':'→'.'), so `align mark 2:3` works. Live proof on a scratch parse-only proiel store (live db untouched): MARK 2.3 → 5/5 witnesses each labeled nc, the two-sentence Armenian verse honestly "[covers MARK 2.3, MARK 2.4]"; a real `nabu rebuild` of the scratch store regenerated the alignment identically; MARK 1.1 → 4/5 with Armenian "not attested"; nabu_align verified over real stdio JSON-RPC. Docs: architecture §10, mcp.md (five tools, incl. the stale-three fix + a concord entry), README rows. Suite 1066 green (13,077 assertions), lint clean.
2026-07-09 · P11-4 · 7b56011 · Dictionary shelf: LSJ + Lewis & Short (improvements §1.3; architecture §11 is the design record). Phase A scouting owner-approved before any fetch: upstream github.com/PerseusDL/lexica pinned b5e707b (CC BY-SA 4.0, verbatim grants in repo README + per-lexicon READMEs → attribution class); LSJ = 27 letter-split TEI P4 files with ALL Greek in betacode; L&S ingests only the Unicode eng2 variant (betacode eng1 twin skipped per upstream README); bibl/@n carries CTS urns since the 2014 upstream revision — work-level, edition-level (often an edition we don't hold: LSJ cites perseus-grc1 vs our grc2), bare-work, non-CTS ("Dig. 33.6.9") and occasionally malformed (virtus's `…:Orat::2:27:120`). Fixtures = 4 owner-approved trims (μῆνις n67485 + μηνίσκος; λόγος whole ~300 KB as the bounds stress; L&S a2/Aaron/officium n32391/virtus n51108) fetched by ranged reads at the pinned sha, byte-identical entries, manifest.yml + README documented. Shipped: dictionaries ARE registry sources with a declared content kind (Adapter.content_kind :passages default, :dictionary on Lexica; SyncRunner/Rebuild route to Store::DictionaryLoader — rejected a parallel mechanism that would re-implement retention/breakers/pins); catalog migration 006 (dictionaries, dictionary_entries urn urn:nabu:dict:<slug>:<id>, dictionary_citations, provenance.dictionary_entry_id — catalog not fulltext: entries are first-class derived data with Loader-grade idempotency/revision/withdraw semantics, entry-grained LoadReport counts, durable ledger revisions); Nabu::Betacode (table-driven decoder, canonical mark order, positional sigma, no gem); LexiconTeiParser (Reader-streamed, entry-at-a-time DOM; folded-headword keying per conventions §9 from the decoded @key; gloss = first non-cit tr, else first non-abbreviation italic of the first sense — "gen. plur." skipped, virtus glosses "manliness"; body = structured plain text with sense labels; citations only from urn:cts bibls, label + work prefix + dot citation); Query::Define + `nabu define LEMMA [--lang grc|lat]` (unbounded CLI) + MCP nabu_define (sixth tool: 6 KB body cap with honest note, resolved-first citation cap, restricted shelves withheld, no-shelf/no-corpus states) + lemma-search gloss integration (LemmaSearch Result gains gloss via one batched Define#glosses lookup, dictionary language must match the lemma's). Citation resolution at QUERY time (works sync after dictionaries; rebuilds re-mint): work-prefix match → in-catalog edition re-anchor, original language preferred, passage must exist; one honest fallback for 3+-part citations to (first, last) — the classical chapter/section double citation, discovered against the real De Officiis (Perseus cites book.section 1.4 where L&S cites 1, 2, 4; the fallback lands on the verbatim quoted passage, eyeballed). Demo on a scratch store (fixtures + the real canonical De Off file; live db untouched): `define μῆνις` → gloss wrath + "Il. 1.1 → …tlg0012.tlg001.perseus-grc2:1.1"; `define officium` → "Cic. Off. 1, 2, 4 → …phi0474.phi055.perseus-lat1:1.4" + 1, 9, 28 → 1.28; virtus → 1, 15, 46 → 1.46; rebuild-safety pinned by test (entries+citations byte-identical across two rebuilds). Known honest misses: L&S cites Livy as unified phi0914.phi001 vs Perseus per-book split — unresolved by design. Bosworth-Toller (CC BY 4.0 CSV) = future own adapter, same tables, language ang, citations empty until a crosswalk (architecture §11). sources.yml `lexica` enabled: false awaiting the owner-fired ~160 MB first clone. Suite 1128 green, lint clean. NO real sync run. REVIEW BLOCKER FIXED (same commit): Sequel::Model(:dictionaries) introspects at class-definition time, so Store.setup! itself crashed every CLI command against the live pre-006 catalog (read paths never migrate; MCP opens READONLY and never can) — fixed with Sequel::Model.require_valid_table = false set in setup! before the model requires (global, deliberately: the next table-adding migration cannot reintroduce the bug), runtime protection unchanged (table_exists? guards); regression pinned by test/store/pre_migration_catalog_test.rb (target-5 store: in-process rebind path + fresh-subprocess definition path, the readonly MCP condition); live read-only smoke after fix: status/search/--lemma/show/align ok, define + MCP nabu_define degrade to the honest no-shelf note, six tools listed over real stdio. Suite 1130 green (13,336 assertions), lint clean.
2026-07-09 · P11-5 · 2a04c8e · Biblical trio (improvements §2.1; architecture §10 updated). Phase A scouting owner-approved before any fetch ("Approved as is"), and the headline deviation was argued there: the trio is TWO new sources + one registry-only witness, because the open LXX is Swete 1909 and it is ALREADY IN THE CATALOG as First1KGreek tlg0527 (55 grc book-docs, verse-grain chapter.verse CTS urns, verified live read-only; the standalone nathans/lxx-swete CC-BY-SA repo derives FROM tlg0527 — ingesting it would duplicate the edition). RAHLFS BLOCKED honestly (02-sources #44): the 1935 text is PD by age but every machine-readable Rahlfs descends from CATSS/CCAT, whose user declaration requires verbatim "To control access to these materials and require any other party … to observe these conditions" (registration-gated no-redistribution); eliranwong's CC-BY-NC-SA relabel ships no LICENSE and concedes the declaration; CenterBLC's MIT covers only its Text-Fabric conversion; Rahlfs-Hanhart 2006 is © DBG; STEPBible has no Greek OT in-tree (verified). SHIPPED: source `vulgate` — the FULL Clementine bible (Tweedale/eBible.org via seven1m/open-bibles, one 4.65 MB USFX file, PUBLIC DOMAIN per README row + eBible.org "No person, company, or organization may claim any kind of copyright…" → open; pinned 8c31c38) through the new UsfxParser family (streaming Reader over milestone XML, one document PER BOOK, urn:nabu:vulgate:<osis-lc>, passages <doc>:<ch>.<v>, ligatures/spaced-punctuation verbatim); source `sblgnt` — the SBL Greek NT (Faithlife/SBLGNT pinned c4d241a, CC BY 4.0 verbatim in README with the old EULA superseded — sblgnt.com/license now serves CC BY; morphgnt's CC-BY-SA-3.0 copyleft morphology deliberately avoided) through the new SblgntParser family (verse-per-line TSV, per-book docs, ΚΑΤΑ ΜΑΡΚΟΝ first-line titles, ⸀⸂⸃ apparatus sigla kept verbatim, native "Mark 1:1" citation in annotations); both enabled:false/manual, conformance-green, GitFetch path. HUB: architecture §10's forecast "one new named extractor" landed as `cts-verse` (registry book token + passage-urn tail → "GEN 1.2"; single-chapter books fold flat, the Incipit stance) plus the one registry extension it needs: a witness may span PER-BOOK DOCUMENTS (`documents:` book-token→urn map; two witness forms cross-validated loudly — cts-verse requires the map, proiel-citation the single document; label required on the map form; work-level duplicate check across all urns). AlignmentIndexer iterates witness documents; Query::Align renders a multi-doc witness as ONE column (the HIT book's document heads it; a miss shows the label alone, never an arbitrary book's title; not_synced only when NO document is live; CLI nil-title render fixed). Registry shipped LIVE (not commented — registering before sync renders "not synced" by design): nt += sblgnt (27-book map, PHILEM: urn:nabu:sblgnt:phlm-style token mapping in the keys) + vulgate-NT (all 27 codes scout-verified); NEW ot work "Old Testament (Septuagint / Vulgate)" = LXX-Swete (55 catalog-verified urns incl. 1st1K-grc2 slugs for SIR/ISA; Theodotion as the plain DAN/SUS/BEL tokens with -OG variants; 2ES = Swete's Esdras B; tlg030 Ecclesiastes has no grc upstream — honest gap) + vulgate-OT (ONLY the 9 scout-verified codes — a guessed code would dangle silently; extend config-only from minted urns after the first sync). Fixtures per the approved plan, ranged reads at pinned shas, byte-identical slices: vulgate GEN 1 + MRK 1–2 + JHN 1:1–18 (14.4 KB), sblgnt Mark 1:1–2:12 + John 1:1–18 trims + 3John WHOLE (2,917 B), first1k tlg0527 Genesis ch. 1 (16 KB, pinned 4c9c843) exercising the LXX witness end-to-end from a real fixture (first1k pinned URN lists updated; manifest.yml entries + READMEs with verbatim license quotes). DEMO fixture scratch store (live db untouched): MARK 2.3 / MARK 1.1 / JOHN 1.1 → sblgnt grc ↔ vulgate lat attesting with the PROIEL five honestly "not synced"; GEN 1.1 → ΕΝ ΑΡΧΗ ἐποίησεν ↔ In principio creavit, 2-of-2. LIVE-witness demo (live catalog opened READ-ONLY, index built into scratch memory, zero live writes): 68,896 refs; MARK 2.3 = 5-of-7 with the trio pending owner syncs; GEN 1.1 + PSA 22.1 (Κύριος ποιμαίνει με — Vulgate Psalms share the Greek numbering, the ot registrar's §10 claim proven) + JON 2.1 attest from the live LXX. Owner-fired next: nabu sync vulgate (~76 MB), nabu sync sblgnt (~2.3 MB); LXX needs nothing (next sync/rebuild reindexes from the new registry). REVIEW FIX (amended into the same commit): the second registered work had made every bare `align REF` fail with "pick one with --work" — Query::Align#resolve_work now auto-resolves a bare ref through the alignment index (unique attesting work → picked, for citation refs AND passage-urn pivots; several attesters → ambiguity error naming ONLY the works that attest; none → honest "not attested in any registered work" with the --work hint; explicit --work keeps precedence; MCP nabu_align inherits — it passes work: straight to Query::Align). Cosmetic honesty: a not-synced multi-book witness cites the queried ref's OWN book urn (JOHN 1.1 → urn:nabu:sblgnt:john, never the map's first book), and when the witness map lacks the ref's book entirely the urn is nil and the CLI phrases the miss neutrally ("its registered documents are not in the catalog"). CLI --work help text updated. Tests: test_bare_ref_auto_resolves_to_the_only_attesting_work / _attested_in_several_works_names_only_the_attesters / _attested_nowhere_reads_not_found_with_the_work_hint, test_a_not_synced_multi_document_witness_cites_the_ref_relevant_book_urn / _with_the_book_unmapped_cites_no_urn, CLI test_align_not_synced_multi_document_witness_with_unmapped_book_reads_neutrally. Verified bare on live data (catalog read-only, scratch index): align MARK 2.3 → nt 5-of-7; align GEN 1.1 → ot, Swete attests; TOBIT 99.99 → not-found+hint; --work nt honored. Suite 1206/15,303 green, lint clean. No real sync run.
2026-07-10 · P11-6 · 6fdb882 · ORACC project expansion (config-only rider; 02-sources #8 scope note updated). Extended `Oracc::PROJECTS` from two to five: added `saao/saa01` (Sargon II letters), `rinap/rinap1` (Tiglath-pileser III), `dcclt` (lexical lists) — all CC0-scouted in P9-5a; the adapter reads license per-project at sync anyway, so nothing hardcoded. Entries are project PATHS (slash form) per the constant's own contract ("subprojects would be slash-paths") — the `slug`/`http_probe_targets` machinery flattens with `.tr('/','-')` so the zip basename/dir/urn segment is `saao-saa01` while the per-subproject `metadata.json` probe URL keeps the slash (`/saao/saa01/metadata.json`); the packet's shorthand "saao-saa01" is that flattened slug, NOT the PROJECTS entry (putting the hyphenated form in the list would mis-address the metadata probe). NO parser changes, NO new fixtures. Because `fetch` now iterates five projects, the fetch tests (which stubbed only the two fixtured zips) would hit unstubbed URLs — generalized `stub_project_zips` to walk the real `PROJECTS`, using the checked-in corpus fixtures for rimanum/etcsri and a metadata-only envelope (real CC0 `metadata.json`, zero corpusjson → 0 ingestible texts, no invented cdl data) for the three fixtureless projects; the mass-deletion breaker denominator is `discover` (non-empty corpusjson only) so the empty envelopes don't shift its 20% ratio. Turned the `repos.keys` assertion into a PROJECTS pin (keys == PROJECTS mapped through the hyphen-flatten). remote_probe_test's real-oracc HTTP probe stub flattened to match (`#{project.tr('/','-')}.zip`). 02-sources #8 + config/sources.yml oracc comment now say five projects and carry the guard: the owner-fired `bin/nabu sync oracc` after merge is the review gate, and if it surfaces a cdl node type `OraccJsonParser` does not handle, STOP for a follow-up packet rather than hack the parser. Suite green, lint clean. NO real sync run (owner-fired after merge).
2026-07-10 · P11-7 · 4bf2d8a · Silent-ingestion defects + skip visibility (census-first: the orchestrator's disk-vs-catalog audit across 12 sources). Seven fixes, one commit. (1) THE HEADLINE — ORACC subproject zips unpack with a NESTED ROOT (`saao-saa01/saa01/corpusjson`, not `saao-saa01/corpusjson`), so discover found 0 of saao/saa01 + rinap/rinap1's 361 texts while the sync reported "succeeded (+4675)"; `Oracc#project_dir` now resolves `corpusjson/` at the top level OR the subproject's last-segment nested root, and a registered project whose tree exists but yields no `corpusjson` is a LOUD `unrecognized` discovery note, never silence. (2) Verify crashed on dictionary sources (`document.urn` on a `DictionaryDocument`), aborting the ENTIRE verify run so every source after lexica went unchecked — Verify now routes `content_kind :dictionary` to entry-level hash reconciliation (recompute `ContentHash.dictionary_entry`, compare to `dictionary_entries.content_sha256` by `urn:nabu:dict:<slug>:<entry_id>`); a store with both kinds verifies and reports both. (3) dcclt NO-CONTENT shape (~112 files: object/surface skeleton, only `nonx` d-nodes, zero transcribed lines) — new `Nabu::DocumentSkipped` signal (NOT a ParseError); the loader counts it `skipped_by_rule`, never quarantines, never journals (the 0-byte stance), and its urn still shields the row from the withdrawal sweep. (4) dcclt LABEL-LESS `line-start` (~58 files: one bare line-start amid ~300 labeled, e.g. P010104) falls back to the enclosing sentence c-node's label (`r xi' 10'` → suffix `r.xi'.10'`); if that too is absent, only THAT line is skipped — the document loads. (5) LSJ "strays" — **the packet's census was WRONG: `eng1`/`eng9` are the α (largest, ~18950 entries) and θ (~1948) LETTER files, not "alternate single-file editions to exclude" (every `eng1..27` maps to a distinct Greek letter, verified on disk).** They quarantined the WHOLE file on a work-level `<bibl>` whose `@n` is a CTS urn with a trailing colon (`urn:cts:…tlg0088:` → empty suffix → invalid ""-string `DictionaryCitation` → ParseError). Excluding them would DELETE ~20900 entries incl. all of α; the real fix is `cite_parts` minting `citation: nil` for an empty suffix — both files parse whole (2 → 0). Classified loudly rather than forcing exclusion. (6) GRETIL silent strays — `sa_vijJAnezvara-mitAkSarA` (the 1.8 MB Mitākṣarā, 4788 prose passages) + `sa_haribhadrasUri-zAstravArttAsamuccaya` (701 xml:id verses) carry a `<text>` with NO `@xml:lang` (teiHeader `xml:lang="en"` is metadata), so `peek_header` → nil and discovery dropped them INVISIBLY; RECOVERED via `<body>/@xml:lang` (`sa-Latn` → `san-Latn`, accurate) then filename `sa_` fallback — genuine editions, not classified away. (7) SKIP VISIBILITY (systemic) — `Adapter#discovery_skips` → `DiscoverySkips(skipped_by_rule, unrecognized, notes)` at the seam (default = nothing skipped, so all families inherit it; ORACC/GRETIL override cheaply, no content reads), `LoadReport` gains `skipped_by_rule`, wired through `SyncRunner::Outcome#discovery` to a CLI `discovery: N selected · M skipped-by-rule · K unrecognized` line (unrecognized loud + notes), loud notes persisted to `runs.notes`. Fixtures: real trimmed slices in own trees (`oracc_p11_7` dcclt two shapes + nested saao, `gretil_p11_7` stray, `lexica_p11_7` α slice) so the discover-walked corpora stay clean. FROZEN-URN PROOF (parse-only, live db): `oracc  +407 added  ~0 updated  =6469 skipped  -0 withdrawn  !0 errored` / `discovery: 6876 selected · 299 skipped-by-rule · 0 unrecognized` — quarantines 170 → 0, every previously-loaded doc byte-identical (0 updated), +407 = recovered saao/rinap + label-less dcclt; gretil recovers the 2 strays, ~0 updated. `nabu verify` completes read-only over the full live catalog. Suite 1218/15,353 green, lint clean. Real networked saao/rinap ingestion + `enabled` flips remain owner-fired post-merge.
2026-07-10 · gate11 · — · Phase 11 complete: OE axis surveyed (ISWOC PROIEL-family pick #1 incl. verse-cited OE Mark; ASPR/OTA CC BY-SA pick #2 — the only fully-open OE; Bosworth-Toller CC BY 4.0 for the shelf; DOEC/web-editions blocked; no UD OE); HTTP remote-health probe (oracc alive/current, license:unchecked = upstream empty-metadata degradation, pinned); alignment hub (registry-driven, alignment_refs derived index, `align` + MCP nabu_align; five-way NT live at MARK 2.3; John 1:1 NOT five-way — recorded); dictionary shelf (LSJ + L&S, define + nabu_define, citation resolution to in-catalog urns, betacode decoder; REVIEW BLOCKER caught live: pre-006 catalogs crashed on model load → require_valid_table=false + target-5 regression test); biblical trio (= vulgate + sblgnt adapters, 2 new parser families USFX/SBLGNT + LXX registry-only via in-catalog Swete tlg0527, Rahlfs honestly BLOCKED on CATSS terms; cts-verse extractor, multi-book witnesses, ot work; REVIEW UX FIX: bare align auto-resolves work via index); ORACC scope 2→5 projects (slash-path entries). Owner-fired queue at merge: sync lexica (~160 MB) / vulgate (~76 MB) / sblgnt (~2 MB) / oracc (pulls 3 new projects), then enabled flips. Corpus unchanged this phase (no new syncs): 67,111 docs / 2,853,105 passages, lemma rows 1,780,553 in 9 languages. Suite 1206/15,303, lint clean. Stretch riders (morph facets, vocab profiling) not taken — phase ran full.
2026-07-10 · P11-8 · 5cb4ce5 · Readable aligned scripture: `align` ranges/chapters + the WEB English witness (one commit, not pushed; owner-requested after the show --parallel CTS-sibling wall). HALF 1 — Query::Align gained a citation-range grammar, the sibling of Query::Range in citation space (split on the LAST hyphen because verse citations hold no hyphens; the tail is a bare end suffix rebuilt against the start's book): `align "JON 1"` (whole chapter) and `align "JON 1.1-1.16"` (inclusive same-book verse range) enumerate the work's attested refs in DOCUMENT order (numeric-citation sort, so 1.2 precedes 1.10, tolerant of subverse strings), each ref rendered with the existing per-witness columns and honest per-ref attestation (a witness attesting some refs but not others reads no_match on the misses). New Result type RangeResult{query, groups, total, truncated}; the single-ref Result path is byte-unchanged (existing tests untouched). Rendered-ref CAP = 200 (MAX_REFS) with an honest truncation note, nabu_define's cap style. Work auto-resolution generalized to ranges (sole work → picked; else the works with any matching ref → unique picked / ambiguity naming only attesters / not-found + --work hint); reversed range errors as Range does. CLI renders compactly — query header, a ONE-line witness legend (titles/licenses shown once, not repeated per ref), then one line per witness per ref (text, or a not-attested/not-synced dash). MCP nabu_align mirrors the grammar: a chapter/range reply is type "alignment_range" with a `refs` array (total_refs/shown_refs/truncated), same witness payloads; schema + description updated. HALF 2 — new source `eng-web` (World English Bible, eBible.org via the ALREADY-VENDORED seven1m/open-bibles clone, SAME pinned sha 8c31c38 as vulgate, fixture approval inherited from P11-5). WEB argued over KJV (Crown-copyright in the UK) / DRA (archaic): modern, complete OT+NT+deuterocanon, unambiguously PUBLIC DOMAIN → open. Verbatim license: README row `| eng-web.usfx.xml | English | USFX | WEB | World English Bible | Public Domain |` + the file's own preface "Because the World English Bible is in the Public Domain (not copyrighted), it can be freely copied, distributed, and redistributed without any payment of royalties." ZERO new parser — reuses the UsfxParser family via a standalone EngWeb adapter (Vulgate's USFX-per-book sibling: one document per book, urn:nabu:eng-web:<osis-lc>, passages <doc>:<ch>.<v>, language eng). PARSER FIX (correctness, regression-tested): WEB carries inline `<f>` footnote apparatus the Clementine text lacks — the streaming parser was folding footnote text INTO the verse ("Now Yahweh's<f>…LORD…</f> word came" → Jonah 1:1 with the note mid-sentence); UsfxParser now skips the `<f>`/`<x>`/`<fe>` note subtrees (NOTE_ELEMENTS, depth-tracked with a per-verse reset backstop) so only scripture becomes verse text; Vulgate (no footnotes) is byte-unchanged. Registry: nt gains the WEB witness (all 27 NT codes, stable cross-tradition versification) and ot gains it CONSERVATIVELY (GEN + JON/OBA + the minor prophets NAM/HAB/ZEP/HAG/ZEC/MAL — Psalms and the deuterocanon deliberately omitted because WEB's Hebrew/Protestant numbering diverges from the LXX; where a number diverges the ref simply attests per-witness, never a fabricated pairing — the §10 owner-of-the-claim contract). Source enabled:false/manual (owner-fired first sync, ~76 MB clone shared with vulgate). Fixture: eng-web/ trim = whole books JON+OBA+PHM byte-identical from the pinned blob (15.9 KB) + README (inherited-approval note, verbatim license, footnote structure note) + manifest.yml; adapter test with conformance suite. COSMETIC RIDER: show --parallel's "is translations: true set" hint (misleading for cross-source Vulgate/LXX/WEB with no sibling CTS edition) now appends, when the urn is a registered alignment-hub witness, "try `nabu align REF` for cross-source alignment". LIVE read-only demo (existing index, zero writes): `align "JON 1"` → 16 refs, LXX Swete Greek chapter-wise (ΚΑΙ ἐγένετο λόγος Κυρίου πρὸς Ἰωνᾶν…), vulgate honestly "not attested" (JON absent from its scout-verified OT map), WEB "not synced"; `align "GEN 1.1-1.3"` → LXX ΕΝ ΑΡΧΗ ἐποίησεν ↔ vulgate In principio creavit, three-verse cross-source pairing; `align "PSA 1.1-30.1"` → 406 refs found, CAPPED at 200 with the truncation note; reversed range errors; single-verse format preserved (GEN 1.1 → 2-of-3, WEB not-synced). Suite 1248/16,086 green (was 1218; +30 covering range/chapter query+CLI+MCP, the footnote regression, and the EngWeb adapter), lint clean. No real sync run; eng-web READY awaiting owner sync.
2026-07-10 · P11-9 · 05ba004 · `show --random` + Vulgate-OT registry completion + an owner-feedback align rider (one commit, not pushed). PART A — `bin/nabu show --random [--source SLUG] [--count N]`: N random VISIBLE passages (default 1, cap 20 = Random::MAX_COUNT) in the standard `show <urn>` layout — the eyeball ritual at a source flip. New Query::Random includes CatalogJoin (refactored to expose a shared `visible_passages(lang:,license:)` dataset — the two-level withdrawn-exclusion rule now single-sourced, catalog_rows built on it) and draws with ORDER BY RANDOM() (honest randomness, not first-N), reusing Query::Show to build each PassageResult so a hit is byte-identical to `show <urn>` (text/document/license/provenance). Source scope filters sources.slug; an unknown slug raises Random::Error ("run nabu status"); a real-but-empty source is honest emptiness, not an error. CLI-only: `--source` without `--random` and a urn beside `--random` both error; NOT exposed over MCP (a conversational surface has no eyeball ritual — tool list stays six). PART B — config/alignments.yml ot-work Vulgate witness completed from 9 to 43 book codes, EVERY one verified read-only against the live catalog (`sqlite3 -readonly db/catalog.sqlite3`, all 43 urns present). The catalog holds 46 Vulgate OT documents; THREE are deliberately left unmapped and attest per-witness: `ezr`/`neh` (the §10 Esdras divergence — LXX 2ES=Esdras B is Ezra-Nehemiah in continuous chapters, the Vulgate SPLITS them with restarted numbering, and LXX 1ES/Esdras A has no Clementine counterpart) and `ecc` (Ecclesiastes — absent from the LXX witness upstream, tlg0527.tlg030 has no grc edition, so no work token to align on). WEB's OT map left untouched (versification divergence, per brief). PART C (OWNER FEEDBACK 2026-07-10, folded in) — the range/chapter align render was unreadable (`align "JON 1"` repeated "vulgate — not attested"/"WEB — not synced" on all 16 refs). Query::Align RangeResult gains `absent` (AbsentWitness{label, reason}); `lift_absent_witnesses` partitions witnesses into those present somewhere (:ok in ≥1 ref — kept per-ref, "— not attested" lines intact) and those absent from EVERY ref (lifted to the header, reason :not_attested for a live witness whose verses are all absent | :not_synced for a registered-but-unsynced one, dropped from the per-ref groups). Single-ref Result path byte-unchanged (no :absent member). CLI prints the summary once ("not attested in this range: …; not synced: …"); MCP nabu_align range reply gains range-level `absent_witnesses:[{label,reason}]` with the all-absent witnesses omitted from the per-ref witness arrays; docs/mcp.md + the ALIGN description updated. LIVE read-only demo (existing stale index, zero writes): `show --random` → Theriaca 750; `show --random --source vulgate --count 3` → three real Vulgate verses in full layout; unknown slug exits 1. `align "JON 2.1"` (single-ref) and `align "JON 1"` (range) show the PRE-INDEX-REFRESH honesty: LXX Swete attests, WEB (eng-web synced) attests, and the newly-mapped Vulgate JON renders ABSENT — "not attested (this witness lacks JON 2.1)" single-ref / summarized "not attested in this range: vulgate (Clementine)" range — because alignment_refs is DERIVED and predates this config edit; the doc IS live (`show urn:nabu:vulgate:jon:2.1` works), so this is honest staleness, NOT fabrication, and NOT wrong-rendering — the LXX↔Vulgate three-way materializes at the owner's next `nabu sync`/`nabu rebuild` (config-only change, index rebuilt there). Suite 1269/16,159 green (was 1248; +21 across Query::Random, the range absent-summary in query/CLI/MCP, and the single-ref-unchanged pin), lint clean. No real sync/index rebuild run.
2026-07-10 · P11-10 · 68d4d00 · status learns dictionary sources + USFX non-verse books (defect packet from owner report "lexica status weirdly zero docs"; one commit, not pushed). FIX 1 — StatusReport rendered the 168,133-entry lexica shelf as `docs=0 passages=0` because the row builder never learned `content_kind :dictionary` (the same missed-surface class as the P11-7 verify defect). `StatusReport#counts_fragment` now branches on `entry.adapter_class.content_kind`: a dictionary source renders `entries=N` (live `dictionary_entries` owned by the source across its dictionaries, withdrawn excluded) in place of the docs/passages/retired triple, keeping the rest of the row shape (enabled/policy/last-run); an unsynced dictionary source honestly reads `entries=0` (right shape, no misleading zero); a broken adapter class degrades to the passage shape rather than crashing status. Live read-only proof: `lexica  disabled  manual  entries=168133  last run … succeeded`. THE OTHER status surface — MCP `nabu_status` — had the same gap (lexica source row carried only documents/passages, both 0): `source_rows` now attaches a per-source `entries` count (0 for passage sources, never nil/absent) and `totals` gains `dictionary_entries` (the shelf sum), both guarded by `dictionary_entries`/`dictionaries` table existence so a pre-P11-4 catalog is unaffected. Health trends confirmed already kind-agnostic (runs-table counts) — no misleading zero there. FIX 2 — eng-web QUARANTINED FRT (front matter) + GLO (glossary): USFX/Paratext PERIPHERAL books, structural non-scripture matter with ZERO `<v>` verses, which `UsfxParser#parse` was declining with `ParseError` ("yielded no verses") → the loader quarantined them as damage (`!2 errored`). Quarantine implies damage; these are upstream norms → now skipped by rule: `UsfxParser::NON_SCRIPTURE_BOOKS` (FRT/INT/BAK/OTH/CNC/GLO/TDX/NDX — the documented peripheral vocabulary, so a future edition adding INT/BAK skips too) and `#parse` raises `Nabu::DocumentSkipped` (the P11-7 skip signal) for them BEFORE extraction; the loader counts them `skipped_by_rule`, verify already skips them (verify.rb:134), and the conformance suite learned the same tolerance (`parse_or_skip` in adapter_conformance — a discovered-yet-skipped ref is not a document to round-trip, damage still fails). discover still lists FRT/GLO (honest inventory); only parse declines. Vulgate VERIFIED unaffected (its lat-clementine file has 73 books, zero non-scripture codes — inspected, not assumed). Fixture: eng-web/eng-web.usfx.xml gained a trimmed FRT (first) + GLO (last) peripheral slice from the pinned blob (README + manifest updated, whole:false preserved). SANCTIONED parse-only proof (loader-idempotent, live db): `eng-web  parse-only  +0 added  ~0 updated  =84 skipped  -0 withdrawn  !0 errored` / `discovery: 84 selected · 2 skipped-by-rule · 0 unrecognized` — quarantines 2 → 0, the 84 previously-loaded books frozen (0 updated). Suite 1274/16,267 green (was 1269; +5 across dictionary-status render in StatusReport + MCP, the parser skip rule, and the eng-web discover/skip pair), lint clean.
2026-07-10 · P12-1 · 2a4dffe · ISWOC adapter — first Old English in the corpus (survey pick #1). Two-phase per protocol: Phase A scouted page-level only (gh api metadata + raw header Range-peeks) and produced the owner-approved fixture plan (approved as-is 2026-07-10, incl. the third fixture). REPO VERDICT: pinned the ORIGINAL iswoc/iswoc-treebank at final commit 574c81cd9dbf8124290e869bc65078c303a36911 — GitHub-ARCHIVED (read-only, genuinely frozen) → sync_policy: frozen (proiel precedent); the syntacticus/syntacticus-treebank-data successor's HEAD (525cee4, 2023-04-26) PREDATES the original's final commit and bundles proiel/+torot/+menotec/ (already-synced data; if ever adopted, scope to iswoc/ — future-migration note in the adapter header). Pinning the original means the inherited flat-root discover works verbatim — no subdir-scoping code. ADAPTER: Iswoc < Proiel (lib/nabu/adapters/iswoc.rb), TOROT pattern (manifest override) + exactly ONE new behavior: the ang language filter — private document_refs override `super.select { language == "ang" }`, excluding the repo's 10 medieval Romance texts (fro/por/spa) by PEEKED HEADER metadata, never filename. Keeps 5 OE texts (wscp/æls/apt/chrona/or, ~29,406 gold tokens). urn: inherited urn:nabu:proiel:<source-id> (torot precedent; REQUIRED — the alignments hub line keys urn:nabu:proiel:wscp); first non-ASCII source id æls minted verbatim NFC (explicit test). License CC BY-NC-SA 3.0 US re-verified verbatim (README + all three fixture source headers) → nc. FIXTURES (test/fixtures/iswoc/, upstream sha256s in README): wscp-mark.xml (3 whole divs — Matthew 7 fragment + Mark 1–2, 150 sentences, 305,320 B), æls-head20.xml (20 sentences, 86,069 B), eustace-head.xml (fro EXCLUSION PROBE, 3 sentences, 20,899 B — the owner-approved third fixture; the filter's exclusion branch is tested against a real Romance header, guarded non-vacuous). Honest trim deviation, recorded in fixture README: æls div 1 holds 197/198 sentences and eustace div 1 is ~95 KB, so both truncate their single kept div after N whole sentences (approved plan said "whole divs"; upstream structure made whole-div + approved size envelope mutually impossible — sentences never split, strict-parse verified). wscp actual bytes 305 KB vs Phase A ~90–130 KB estimate (estimate was low; content scope exactly as approved). HUB WITNESS #8: uncommented urn:nabu:proiel:wscp in config/alignments.yml; verified READ-ONLY via bin/nabu align "MARK 1.1" → renders "wscp — not synced (urn:nabu:proiel:wscp is registered but not in the catalog)" alongside 7 attesting witnesses (P11-9 header summary counts it honestly: "7 of 9 witnesses attest"). Registry: iswoc enabled:false, sync_policy frozen. Tests: 19-test iswoc battery (conformance incl. two-parse URN stability + registry round-trip + repo_url identity); alignment_registry_test shipped-registry expectation updated to the 9-witness truth (wscp at index 5, planned P12-1 change, not a weakening). 02-sources row 34 → READY. Suite 1293 runs / 17,384 assertions, 0 failures; rubocop 175 files, no offenses. Real first sync remains owner-fired; sha — (recorded post-commit convention: commit not pushed).
2026-07-10 · P12-2 · 46a90bb · ASPR adapter — the complete OE poetry corpus (survey pick #2; the only fully-open OE). Two-phase per protocol: Phase A re-verified the OTA 3009 bitstream page-level (200 auth-free, Content-Length 2,214,065 = survey byte count, Last-Modified 2019-07-19; server ignores Range, so the "small ranged read" degraded to the survey's one sanctioned full-file sample, retained read-only in scratch, sha256 4cf37022…5ceed5) and the in-file license verbatim (teiHeader availability: "Distributed by the University of Oxford under a Creative Commons Attribution-ShareAlike 3.0 Unported License" → attribution, MCP-safe), mapped the structure precisely (349 FLAT div[@rend=linenumber][@xml:id], div == poem — the survey's "374 texts" counted <head> elements, 25 being duplicate second heads within Meters-of-Boethius/Psalm-fragment poems; 30,550 <l>, zero outside divs; caesura always space-padded, <l> attribute-free, heads plain text, file NFC), and produced the owner-approved fixture plan (approved as-is 2026-07-10). HEADLINE STRUCTURAL FINDS: (1) the div xml:id values ARE the canonical Cameron/DOE record numbers (A4.1 Beowulf, A32.1/A32.2 Cædmon's Hymn Northumbrian/West-Saxon) — unique across all 349, scholar-cited, so urn:nabu:aspr:<Cameron> replaces the packet's <poem-slug> sketch (title-slugs REJECTED on hard evidence: A43.5 and A43.10 are both <head>"For Loss of Cattle"); (2) despite NO l/@n, the per-div <l> ordinal EQUALS the printed ASPR line number (Beowulf div = exactly 3,182 <l>, Judith = 349 — both canonical counts), so ordinal citations are canonical here, NOT GRETIL-style honest-synthetic. FETCH-PATH DECISION (argued Phase A): Nabu::FileFetch, a ZipFetch SIBLING not a mode — ZipFetch is irreducibly zip-shaped (unzip, tree-root, multi-file set-difference); FileFetch keeps the contract verbatim (If-Modified-Since replay from .file-fetch.json, 304 → untouched, wiped-tree unconditional refetch, sha256 body pin, guard BEFORE mutation, GitFetch-format attic manifest) with the one genuine doomed case (a stale differently-named previous download; a changed body is an update, never atticked — the git precedent) and reuses ZipFetch.default_http (vendored-cert Faraday) by reference. PARSER: AsprParser, 7th family, deliberately the smallest (one uniform file) — UsfxParser one-file-many-docs shape (#texts inventory for discover, #parse extracts one div, stops at its end), XML::Reader only, one passage per <l> (sequence 0-based, citation 1-based ordinal), <unclear>/<foreign rune>/<g> text kept inline (canonical means canonical), head/bibl never leak. Fixture-time finds, both regression-tested: a div-level <gap/> BETWEEN lines (Riddles 82) must not shift ordinals; Nokogiri Reader reports whitespace-only nodes as TYPE_SIGNIFICANT_WHITESPACE — dropping them fused sibling runes ("DNLH." → captured, "D N L H.", while concatenate-then-collapse keeps dom<g>ę</g>… joins tight). ADAPTER: one document per poem, minting frozen; health probe rides :http_zip with HttpProbeTarget gaining optional state_file (default .zip-fetch.json — ORACC untouched) and nil metadata_url short-circuiting the license row to honest unchecked with NO GET (ASPR's license lives in-file; drift-on-refetch is the license watch). FIXTURE (test/fixtures/aspr/, README + manifest.yml, whole:false trim): 3009.xml = teiHeader verbatim + 8 of 349 divs in file order — Beowulf A4.1 ll.1–24 ("Hwæt! We Gardena in geardagum," = urn:nabu:aspr:A4.1:1), Cædmon dialect pair, the title-collision charm pair, rune/gap/glyph micro-divs — 12,015 B, 80 lines, extracted by div-id from the retained Phase A sample (no second download). Registry aspr enabled:false sync_policy:manual (upstream effectively frozen but not archive-locked). Tests: 13 parser + 12 FileFetch + 18 adapter (conformance incl. two-parse urn stability) + 2 probe; one test expectation fixed during TDD (the head/bibl leak probe had matched "Beowulf" — line 18 legitimately reads "Beowulf wæs breme", probe now uses Dobbie/ASPR/New York). 02-sources row 35 → READY. Suite 1338 runs / 18,106 assertions, 0 failures; rubocop 181 files, no offenses. Real first sync owner-fired; sha — (recorded post-commit convention: commit not pushed).
2026-07-10 · P12-3 · 4cb3615 · Bosworth-Toller onto the reference shelf — the third dictionary (architecture §11's written plug-in note, executed as designed) and the FIRST non-TEI dictionary. PHASE A (page-level, owner-approved as-is 2026-07-10): verified the LINDAT record hdl 11234/1-3532 via the DSpace 7 REST API (the repo MIGRATED — old xmlui handle/bitstream URLs now serve the Angular shell, not files; item uuid da2f3c19-f5a9-48d2-bb8f-eb84a415f954), license verbatim dc.rights = "Creative Commons - Attribution 4.0 International (CC BY 4.0)" → attribution; dump = bosworth_backup_sql.sql (~605 MB, out of scope) + bosworth_entries_export.csv (88,387,561 B, MD5 7c50c0a47ad2365fa0fddea18a54f11d, Last-Modified 2021-04-26) + readme.txt (769 B, format verbatim: UTF-8, ';' separator, all fields quoted, id;headword;body, body "tagged in xml"); CSV reality from an 8 KB Range read: doubled-quote escaping, CRLF record ends with LF-only body newlines (real CSV reader mandatory), project-XML (NOT TEI) bodies with milestone <b-s/>/<i-s/> pairs, nested ragged <sense>, double-encoded entities (&amp;#39;, &amp;mdash;, &amp;para;), ids gap (…4, 6…). FETCH VERDICT: FileFetch-ready — the DSpace bitstream /content URL serves 200 + Last-Modified + ETag + Accept-Ranges, the exact conditional-GET/sha-pin contract P12-2 shipped; ASPR wiring verbatim (:http_zip probe, metadata_url nil — LINDAT item JSON is not the probe's {license:…} shape, honestly unchecked). ANG FOLD ARGUED NOT ASSUMED (conventions §9 new row): æ→ae, þ→th, ð→th — B-T's OWN dump alphabetizes æ as "ae" (æppel files between a-h-/a-l- words) and interfiles þ/ð as ONE letter (its <sort> field: æðele→aetþele, þing→tþing — the dictionary itself folds æ→ae and buckets ð/þ identically); ð→d REJECTED (would split the pair B-T unifies); wynn deliberately no rule; vowel length falls to the generic mark strip; gsub not tr (1→2 expansions); landed while zero ang rows exist (aspr/iswoc/bosworth-toller all disabled) so the §9 rebuild-storm caveat is vacuous. PHASE B: fixture via HTTP Range reads ONLY (~3.4 MB of 84 MB: bytes 0–1.45M + 45.6–47.0M + ordering probes) — 270 stratified entries, 497,144 B, every row asserted byte-verbatim vs the raw slices (extraction recipe in the fixture README; strata: A-section head ids 1–184 incl. the flagship "A" letter entry, 45 æ-initial incl. the æðel- block, 44 þ-initial from the "Þ" letter entry on, 7 homograph groups incl. ǽ ×3, shortest bodies; dump reality: NO ð-initial headwords exist — ð is medial — and 249/270 bodies are sense-less, tagging is the exception). SHIPPED: BosworthCsvParser (8th family: stdlib CSV streaming; gloss = first <equiv lang=eng> else first <def>, nil honest; body linearized with <search>/<sort>/<checked> skipped, sense line-breaks, numeric+whitelist second-pass entity decode, NFC; row errors → ParseError) + BosworthToller adapter (content_kind :dictionary, FileFetch fetch, one ref per CSV, urn:nabu:dict:bosworth-toller:<csv id> ↔ bosworthtoller.com/<id> spot-checked id 940 → HTTP 200) + registry bosworth-toller enabled:false sync_policy:manual. INHERITANCE VERIFIED BY TESTS, NOT ASSUMPTION: status renders entries=N (real adapter class), verify re-hashes all 270 entries clean through the same content_kind routing, MCP nabu_define reaches the shelf with lang=ang, Query::Define needed ZERO changes (it was genuinely language-agnostic) — the only surface edits were the hard-coded grc|lat gates (CLI define --lang, MCP enum + handler + description). Demo (scratch catalog from fixture, live db untouched): define aethele --lang ang → æðele [attribution] gloss "noble" with sense breaks; define thing → þing "a thing"; define ae → the three ǽ homographs (life/river/alas!); status entries=270. GEM: csv added to the Gemfile (stdlib extraction, ruby-core, zero deps — not a default gem since Ruby 3.4; the box runs 4.0). Tests: 14 parser + 12 adapter + 3 define + 1 each MCP/status/verify/normalize additions. Suite 1370 runs / 19,907 assertions, 0 failures; rubocop 185 files, no offenses. Real first sync owner-fired; commit not pushed.
2026-07-11 · P12-4 · 5e90e6a · The public face: README rebuilt as an open-source pitch (hero; "show me" block with four live-run outputs — nine-witness align "MARK 2.3" incl. the new OE wscp witness, define μῆνις with resolved citations, search --lemma λέγω suppletion, show --random oracc → Tiglath-pileser III; plus --parallel Iliad 1.1-1.5 and concord --lemma virtus in the feature tour); personas ×7 (classicist/biblical/indologist/assyriologist/medievalist-OE/DH/AI-builder); corpus table from the live catalog dated 2026-07-11 (72,734 docs / 3,143,750 passages — row sums cross-checked exactly against totals; 168,133 dict entries; 1,942,521 lemma rows in 13 langs, ang now 24,827); protection story; honest status (no CI badge — no CI; code license honestly "none yet = all-rights-reserved"); docs index with one-liners; dev-loop linked once. NEW docs/quickstart.md: zero-to-first-search on sblgnt (11 MB, ~3 s first sync per history ledger), every read-only command actually run (search ἀγάπη, show sblgnt:john:1.1-1.3, define ἀγάπη, search --lemma ἀγαπάω, align "JON 2.1" — with the verified 2.1-vs-1.17 versification footnote: eng-web jon:1.17 checked live), sync steps described with real du sizes. Coherence: library.md §9 stale claim fixed (MCP "4 tools" → 6, names verified against mcp.md); library.md header totals/OE sections left to the gate per its §10 duty (they predate the owner-fired iswoc/aspr syncs); noted in README that iswoc/aspr are synced but enabled:false pending sign-off, bosworth-toller queued (entries=0, never synced). No code touched; live db read-only throughout. Suite+lint untouched-green.
2026-07-11 · gate12 · — · Phase 12 complete: the Old English axis landed end-to-end — ISWOC adapter (TOROT-pattern, ang filter, frozen archived-repo pin over the stale syntacticus bundle; OE Mark = hub witness #9, "9 of 9 attest" live after owner sync); ASPR adapter (8th… 7th parser family AsprParser + Nabu::FileFetch single-file path with full attic contract; 349 poems / 30,550 lines, canonical printed line ordinals verified, Cameron-number slugs over colliding titles; Beowulf :A4.1:1 "Hwæt! We Gardena…"); Bosworth-Toller (8th family BosworthCsvParser, first non-TEI dictionary; DSpace-7 migration re-scouted, REST bitstream URL; ang fold æ→ae/þ,ð→th corroborated by the dump's own sort field; csv gem added — Ruby 4.0 dropped stdlib default, flagged); the public face (README rebuilt as pitch: hero, four live-run demo blocks, 7 personas, corpus table, protection story, honest status + no-license-yet callout; docs/quickstart.md every command executed; library.md 4→6 MCP tools staleness fixed). Owner synced iswoc+aspr mid-phase (+438 docs / +33,086 passages / +24,827 ang lemma rows). Corpus: 72,734 docs / 3,143,750 passages, 1,942,521 lemma rows in 13 languages. Suite 1370/19,907, lint clean. Owner queue at merge: sync bosworth-toller (~84 MB), flips for iswoc/aspr/bosworth-toller. Flagged for owner decision: repo has NO code license file (README now says so honestly) — an open-source face wants one (suggest MIT or Apache-2.0 for code; content stays per-source).
2026-07-11 · P13-1 · c35fc29 · Slavic survey II delivered (docs/slavic-survey-2.md): dictionaries + South Slavic/Slovenian, scouting-only (page reads + gh metadata, one 1.5 KB sample; all top-pick licenses verified against machine-readable sources first-hand). Owner's three questions answered: (1) more OCS/Slavic — one clean new win, UD_Old_East_Slavic-Ruthenian (README metadata verbatim `License: CC BY-SA 4.0`, prosta mova 1380–1650, zero overlap with the three orv treebanks held) → config-only TREEBANKS add recommended THIS phase beside CCMH; no other open machine-readable ChSl edition exists in any South Slavic recension (Zagreb RCJHR PDF-scans/no-license, SANU internal, Sofia histdict web-UI/bare-©, DIACU JSON unlicensed + mostly re-packaged TOROT). (2) dictionaries — the scholarly OCS lexica are NOT openly available: GORAZD hub query-only with no content license (SJS ~33k entries; Miklosich/Sreznevsky NOT in the hub — packet lead corrected); the nearest prize is the Miklosich BCDH/ELEXIS TEI (41,338 entries) sitting on CLARIN.si 11356/1666 as a METADATA-ONLY deposit (0 files) — one email to BCDH unblocks a drop-in for the existing TEI dictionary family; only clean ingest today is Wiktionary-OCS/kaikki (CC-BY-SA+GFDL verbatim, ~4,548 senses JSONL → small new dictionary family, LATER). (3) South Slavic/Slovenian — Freising Manuscripts eZISS edition fully downloadable TEI P4 BUT the TEI source's <availability> reads verbatim "Priznanje avtorstva-Brez predelav 2.5 Slovenija" = CC BY-ND (the English HTML page mislabels it BY-SA; verified in bs.xml directly) → owner posture gate (permission email to ZRC SAZU vs restricted local ingest); CLARIN.SI goo300k (gold, verbatim "CC BY 4.0") + IMP (CC BY-SA 4.0, 17.7M tokens) ingestable but Early-Modern (1584+) → owner scope call; no historical South Slavic UD treebank exists. Axis c: obdurodon/Manuscript.ru/TITUS all UNCHANGED (Manuscript.ru front host now cert/DNS-broken). Register: row #18 updated, #45–49 added, #4/#13/#30/#32/#33 annotated. Docs-only; suite 1370/19,907 + lint 185 clean.
2026-07-11 · P13-1b · 589cafd · UD Ruthenian treebank (survey-II pick #1, the P10-2 + P10-4 recipe verbatim): added `old-east-slavic-ruthenian` (UD_Old_East_Slavic-Ruthenian) to the `ud` adapter's TREEBANKS map — the THIRD East Slavic branch, "prosta mova" (Old Belarusian/Old Ukrainian) legal/chancery prose ca. 1380–1650 (Polotsk letters, Lithuanian Metrica, Lokhvitsa town-hall book), zero text overlap with Birchbark (Novgorod letters), RNC (Muscovite Middle Russian) or TOROT/PROIEL (OCS canon). No new parser family, no fetch/discover change — pure ConlluParser + UD multi-repo reuse. LICENSE GATE PASSED before committing the fixture: `LICENSE.txt` verbatim "The treebank is licensed under the Creative Commons License Attribution-ShareAlike 4.0 International." + by-sa/4.0/legalcode link (byte-identical to Birchbark/RNC) AND README machine-readable metadata `License: CC BY-SA 4.0` — the stop-if-different condition never fired (GitHub license field reads NOASSERTION; the in-repo grant is authoritative, as at P10-2). LANGUAGE CODE `orv` (following upstream: file stem `orv_ruthenian`, the shared East-Slavic UD code; the per-newdoc `# lang = orv-be` is a finer BCP-47 regional subtag, NOT the treebank tag). License override via the P10-4 seam: TREEBANKS entry carries license "CC BY-SA 4.0" / license_class `attribution` → `documents.license_override` labels it attribution downstream while the source class stays `nc` (most-restrictive present); the four legacy treebanks stay bare. Fixture: first 50 complete sentence blocks of orv_ruthenian-ud-test.conllu (390 blocks, 940,453→309,311 B; whole split has no MWT `n-m` or empty-node `n.m` line — file-wide check — so head-50 is representative as-is; long legal/chancery sentences; all token lines 10 tab-columns, ends with a blank line); README (retrieval 2026-07-11, source URL, verbatim license quotes, trim note) + manifest.yml updated. Tests follow the existing Slavic-treebank pattern: EXPECTED_URNS + discover language/count → 7 files/urns; SLAVIC_SLUGS gains ruthenian (attribution override asserted, four legacy still NULL, source class nc); lemma-row acceptance extends to assert the orv NOUN lemma артыкулъ "article" at urn:nabu:ud:old-east-slavic-ruthenian:orv_ruthenian-ud-test-head50:StatutVKL1566-1 (surface АРТЫКУЛЪ) plus a ruthenian passage_lemmas contribution; DEDUP GUARD test UNTOUCHED (Ruthenian is neither chu-PROIEL nor orv-TOROT); multi-repo breaker test comment refreshed to the 7-treebank arithmetic (delete 2 of 7 ≈ 29% > 20% still trips). Registry unchanged (ud already registered, enabled untouched). 02-sources UD row → 7 treebanks. Suite 1370/20,231 green, lint 185 clean. NO real `bin/nabu sync ud` (owner-fired at gate). One commit, not pushed.
2026-07-11 · P13-2 · 436a606 · CCMH adapter — the OCS canon completion (survey-I pick #2, two-phase with owner gate). PHASE A (page reads + small format samples only): the catalogue's bare "Open" RESOLVED to CC BY 4.0 — bundle README.txt verbatim "Licence: CC-BY (https://creativecommons.org/licenses/by/4.0)", download index labels ccmh-src.zip "CC BY" → attribution; download path CLEAR (PUB, no auth, plain HTTPS file tree kielipankki.fi/download/ccmh-src/www/); the decisive structural fact: the "very simple" XML is CES cesDoc v4 (book→chapter→verse divs/segs) and EXISTS ONLY for the four gospel manuscripts — Suprasliensis + the two Vitae are 7-bit-txt-only with per-text folio/prose schemes. Two XML sub-shapes under one schema: assemanianus/savvina wrap verse text in <ver> children (multi-<ver> segs = line splits + lectionary parallels), marianus/zographensis put text directly in <seg>; quirks verified against the full files: non-canonical chapter 0 (marianus JOH heading list), duplicate (book,ch,verse) seg ids WITH DISTINCT TEXT (mar 8, zog 3, ass 1), %-marks = the editors' own unchecked-spot flags, BOM in 3 of 4 files. OWNER-APPROVED 2026-07-11: v1 = the four gospel manuscripts (both prizes — Assemanianus + Savvina kniga, absent from all holdings — plus Marianus/Zographensis alt-editions; never dedupe vs PROIEL/TOROT, conventions §3); Suprasliensis + Vitae DEFERRED (future ccmh-txt family; obdurodon row 30 queues the richer Suprasliensis anyway). PHASE B SHIPPED: CcmhCesParser (9th… the ccmh-ces family; AsprParser one-file-many-documents shape, Reader-only streaming, depth-tracked book divs) — ONE accumulation rule covers both sub-shapes (passage text = all character data in the seg, collapsed); citations from seg ids with zero-padding stripped (b.MAT.01.01 and b.MAT.5.23 cite uniformly), chapter 0 kept, duplicate ids → positional :b2 suffix in document order (GRETIL collision-tolerance precedent) pinned by both real dups; transliteration stored VERBATIM incl. editorial marks (Cyrillic back-transliteration = future enrichment, not parse). Ccmh adapter: one doc per (ms, gospel), urn:nabu:ccmh:<ms>:<book> (MAT/MAR/LUK/JOH lowercased — upstream codes verbatim), lang chu; fetch = FOUR per-manuscript FileFetch subdirs (FileFetch dooms unrecognized dir siblings — sharing a dir would cross-doom the other manuscripts) aggregated ORACC-style (prepare×4 → ONE breaker over the doomed union → complete×4), FetchReport.repos per-file url→sha; probe :http_zip ×4 targets, metadata_url nil (license lives in the bundle README). Fixtures: 4 trimmed real slices (30 KB total; MAT 1 + JOH 21 w/ dup, MAT 5 + JOH 0 w/ dup, MAT 1 + mid-chapter LUK 1.32, lacunose MAT 3) + README (license chain verbatim + full ASCII grapheme key) + manifest.yml (whole:false ×4, adapter_test null). Registry ccmh enabled:false sync_policy:manual (upstream frozen since 2021); 02-sources row 19 → READY. Tests: 24 (conformance + both shapes + both dups + chapter 0 + fetch/probe/registry). Demo: urn:nabu:ccmh:assemanianus:mat:1.1 → "*k$nIg&I !rodstva !!iUxva . !sna !ddva . !sna *avra/am/l@ ." (Mt 1:1). Suite 1394/21,263 green, lint 188 clean. NO real sync (owner-fired at gate). One commit, not pushed.
2026-07-11 · P13-3 · 6138093 · ORACC expansion II (config-only breadth per the P11-6 pattern, two-phase with owner gate). PHASE A (page-level only: projects.json + HEAD per json/<slug>.zip, NO zip downloads): all 25 packet candidates exist (200, application/zip, Last-Modified); the standalone <project>/metadata.json STILL serves an empty body over HTTP (200, 0 bytes, every candidate — the known quirk), so per-project license was NOT re-readable in Phase A; CC0 expectation rests on the P9-5a family scout (saao/riao/ribo/blms/dcclt all sampled CC0) with the adapter's per-project license gate at sync as the actual guarantee. Proposal table + 149.2 MB total written into the backlog block. OWNER VERDICT (2026-07-11, coordinator-relayed): "Approve all 25, full SAA is the point" → all 25 approved AND the batch extended past the packet's saa02…saa19 cap: saao/saa20 (4.4 MB) + saao/saa21 (3.6 MB) HEAD-verified, and saao/saas2 (1.5 MB) evaluated-and-INCLUDED (project page: lemmatised corpus of the Assyrian Eponym List/King List editions from SAA Studies 2, Millard 1994, lemmatised Morello 2019 — a normal saao corpus subproject, not a different series shape). FINAL BATCH: 28 new projects, 158.7 MB of zips; ribo's babylon* subprojects stay out (packet says "ribo", the top-level 6.6 MB corpus). PHASE B: Oracc::PROJECTS 5 → 33 (slash-paths per the P11-6 convention — the slug/http_probe_targets machinery hyphen-flattens; hyphenated entries would mis-address the metadata probe), scope-history comment on the constant, stale "Both in-scope projects report CC0" → "All". NO test changes needed and none made: the P11-6 generalization already walks the real PROJECTS everywhere — stub_project_zips ships the metadata-only CC0 envelope for every fixtureless project, the repos.keys assertion IS the PROJECTS pin (derived), remote_probe_test's real-oracc probe stubs HEADs per constant. NO fixtures, NO parser changes. 02-sources #8 scope note rewritten (33 projects, license-evidence honesty, standing new-node-type guard); config/sources.yml oracc comment → 33. NEW-NODE-TYPE GUARD stands for the owner-fired bin/nabu sync oracc after merge: unknown cdl shape → census + follow-up packet, no hacks. Suite 1394/21,263 green (run count unchanged — config-only), lint 188 clean. One commit, not pushed.
2026-07-11 · P13-4 · 51df2e0 · ATF translations — cuneiform readable like Homer (fable; two-phase, design owner-approved same day: "Approved design, Two-stage SAA-first crawl"). PHASE A (scout): NO public bulk ATF carries the running English — oracc/catf on GitHub ("Canonical ATF … permitted to be released under CC0") covers the whole SAA run but is C-ATF transliteration-only (0 #tr lines, stale 2019); per-text .atf/.xtf/_project-en.json are soft-404s (200 + literal "404\n") on all mirrors; the json zips carry only a STEMMED translation index (index-tra.json). The one machine path: the official per-text fragment /<project>/<textid>/html (CORS-open, ~55 KB/letter) whose xtr cells carry the SAME node refs as the corpusjson line-start d-nodes → mechanical unit↔line alignment; coverage machine-read from metadata formats["tr-en"] (saa01 264/265, rimanum 378/378, etcsri 1448/1456 + 1441 Hungarian, dcclt 1229/4980). SAA reality is @translation labeled → paragraph-grained units (39 lines / 6 units in SAA 01 175) — exactly the P8-1b span-group model. License is layered: CC0 attaches to the json build files (which exclude the prose); the prose is SAAo project content "Content released under a CC BY-SA 3.0 license, 2007-20" → attribution. Design pick: (a) sibling -en documents in the P7-4 shape (annotations rejected: no render surface, wrong license carrier; hub witnesses rejected: §10's own within-source line). PHASE B: OraccTranslationParser (nokogiri; markup-based extraction — prose = span.cell, print marker = span.xtr-label element, state-notice cells skip by rule, non-line anchors reattach forward, same-label units join); Oracc adapter translations: kwarg (registry seam, default inert) + PROJECT-SCOPED crawl (TRANSLATION_PROJECTS = saao/* stage 1; stage 2 = list extension) into <workdir>/html-en/<slug>/ OUTSIDE the zip trees (never atticked), polite 0.25 s sequential, resumable via zip Last-Modified gating, soft-404s counted missing; discover file-driven (-en ref per fragment with live tablet corpusjson, orphans counted); Query::Parallel gained the ORACC work family (tablet urn IS the work, siblings <work>-<variant>, both directions) — show --parallel + MCP nabu_show light up with zero renderer change; -en docs carry license_override "attribution" (P10-4), tablets stay open/CC0. Fixtures per approved plan: saa01 P224395 pair (real nested root saao-saa01/saa01/…) + rimanum P405432/P405134 fragments (primed/seal labels) + trimmed saa01 metadata (X010028 = the real untranslated text) — manifest + README updated with the verbatim license evidence. sources.yml oracc translations: true. Demo (scratch store): P224395 --parallel → block [:o.1 — covers :o.1..:o.3]: a-na LUGAL EN-ia / ARAD-ka {1}10-ha-ti / lu DI-mu … + "To the king, my lord: Your servant Adda-hati. Good health to the king, my lord!". Suite 1415/21,684 green, lint 190 clean. Stage-1 crawl (saao, ~250 MB) is the owner-fired sync at the gate. One commit, not pushed.
2026-07-11 · P13-5 · 74a8f46 · Psalms alignment work — the versification divergence FACED, not dodged. New `psalms` work in config/alignments.yml: three verse-grain witnesses under the GREEK/LXX numbering as work vocabulary (LXX-Swete tlg0527.tlg027 + Gallican Vulgate psa align DIRECTLY; WEB English psa is HEBREW/Masoretic-numbered — the P11-8 dodge). NEW MECHANISM: an optional per-witness `numbering:` registry key (architecture §10) — `system:` provenance label + `ranges:` list of `{from, to, shift}` piecewise-linear rules that remap the LEADING citation segment (the psalm number) into the work vocabulary, in `Witness#normalize_ref` AFTER the `books:` alias and INDEX-SIDE only (the extractor set stays closed at two; `numbering:` is orthogonal to extraction). The one new power: an unmapped psalm returns nil → the ref is DROPPED (indexer compact/filter_map skip it), so the psalms the LXX joins/splits (Hebrew 9,10,114,115,116,147) never false-align — they render an honest WEB miss (verified: Greek 113 = the Hebrew 114+115 join → LXX+Vulgate "In exitu Israel", WEB not-attested). Mapping table (provenance = the standard LXX↔Masoretic concordance: Rahlfs' Septuaginta front-matter, NETS, Douay/Vulgate-vs-Hebrew tables, all agreeing, cross-checked live — WEB 22 = "why have you forsaken me" = Greek 21): Hebrew 1–8 identity, 11–113 → Greek 10–112 (−1), 117–146 → 116–145 (−1), 148–150 identity; the six join/split psalms deliberately unmapped. DISPLAY: the remapped witness's own Hebrew ref is recovered at QUERY time from the passage urn (never stored) and surfaced — header "· Hebrew (Masoretic) numbering", per sentence "[Hebrew (Masoretic): PSA 23.1]" — so the divergence is VISIBLE, not silently corrected. HONEST RESIDUAL disclosed: the remap fixes the psalm NUMBER only; verse numbering within a titulus-bearing psalm can still differ (superscription counting), left uncorrected not fuzzed. PARIS PSALTER (ASPR A5.51–A5.150) DEFERRED with evidence, NOT registered: the adapter numbers passages by printed POETIC LINE (psalm number in the document id, not the passage tail), one Latin verse → several OE metrical lines, covers only psalms 51–150 — verse alignment would need a hand-built line→verse concordance the corpus lacks, psalm-level would never co-render. Byte-stable: existing works untouched (numbering defaults nil); shipped-registry pin test grew a `psalms` case openly. Acceptance (scratch index over a read-only live-catalog copy — live index picks psalms up at owner's next sync/rebuild, config-only): `align "PSA 22.1"` → LXX "Κύριος ποιμαίνει με" + Vulgate "Dominus regit me" + WEB at Hebrew 23.1 "Yahweh is my shepherd", labeled. Files: config/alignments.yml, lib/nabu/alignment_registry.rb, query/align.rb, cli.rb, docs/architecture.md §10. Tests: registry (remap/drop/validation + shipped pin), indexer (remap + drop), align (native-ref render + join/split miss), cli (visible label). Suite 1426/21,735 green, lint 190 clean. NO real sync. One commit, not pushed.
2026-07-11 · P13-6 · f9f2b46 · Morph facets — `search --lemma X --morph case=dat,number=pl` over the gold shelves, one façade over three tagsets. DESIGN NOTE FIRST (conventions §6.1), verdicts argued from the LIVE data. Tagset reality (measured): CoNLL-U stores UD `feats` (`Case=Dat|Number=Plur`), PROIEL/TOROT a 10-position positional `morphology` tag (`-p---mgpwi`), ORACC a NER-flavoured `pos` (N/PN/GN/V…) with NO inflection. VOCABULARY VERDICT — unified UD façade, not per-family passthrough: query keys are UD feature names (case/number/gender/person/tense/mood/voice/degree, values dat, pl/sg, masc, aor, opt, sub…); CoNLL-U `feats` parsed as-is (already UD, zero translation); PROIEL positional tag DECODED position-by-position into the same UD names via a fixed 10×~8 code map (Query::MorphFacets::PROIEL_FIELDS; positions 9–10 = Germanic strong/weak + inflecting flag left undecoded — no clean UD facet, absence over a wrong map); ORACC has no inflectional morphology so inflectional facets NEVER match it (honest absence, tested), and a unified `pos` facet deliberately deferred (its tagset is not UD upos — welding a third scheme in would be dishonest for one field). Per-token family detection (has `morphology` → PROIEL, has `feats` → UD, else {}), so no source-family lookup. Tiny query language: comma-joined key=value ANDed, tiny value-alias table (sg→sing, pl→plur, du→dual; everything else IS the lowercased UD value). INDEX VERDICT — NO new index/migration: morphology post-filtered in Ruby over the lemma-anchored candidate passages' annotations_json. MEASURED on the live 1.94M-row lemma index (not assumed): `λόγος` dat-pl 37 ms/46 hits, `sum` subjunctive 720 ms/4129 hits, PROIEL orv `и` dat-pl 471 ms/537 hits, worst case (article ὁ) 757 ms/2255 hits — realistic content-word queries tens of ms, even the most common lemma sub-second; a facet index would multiply rows + need a rebuild for no interactive gain, and the lemma table's per-(passage,lemma) aggregation is the wrong granularity for per-token morph anyway. Morph test (cheap string parse) runs BEFORE the per-language fold so a selective facet folds only survivors; surface_forms + decoded `morph` evidence restricted to the MATCHING tokens (λόγος-in-two-cases surfaces only its dative-plural λόγοις). OUT OF SCOPE (honest): bare morph search without --lemma (would scan every annotated passage); ORACC pos-only facets; UD-vs-PROIEL tense/aspect encoding divergence follows each treebank's own convention (grc-perseus aorist = Aspect=Perf|Tense=Past). New lib/nabu/query/morph_facets.rb (parse/features/match?/describe + the PROIEL decode + UD passthrough); LemmaSearch#run gained `morph:` → run_with_morph (candidate_passage_ids pulls the WHOLE lemma set — morph is more selective than license so no pre-filter slice — then annotated_catalog_rows + morph_hit); Result gained `morph` (nil for plain hits, back-compat). Surfaces: `search --morph` (requires --lemma, malformed → Thor::Error) + evidence in print_lemma_results ({…}); MCP nabu_search gained a `morph` arg (requires lemma → InvalidArguments, malformed → InvalidArguments) + payload `morph` + description/schema. Scope: search/nabu_search only (concord/CLI-wide deferred). Demo (live db, read-only): `search --lemma λόγος --morph case=dat,number=pl --lang grc` → urn:nabu:proiel:greek-nt:20342 λόγος → λόγοις {number=plur|gender=masc|case=dat} "…ἐπὶ τοῖς λόγοις τῆς χάριτος…"; `--lemma богъ --morph case=dat,number=pl --lang orv` → urn:nabu:proiel:lav:130218 богъ → бгм҃ь {number=plur|gender=masc|case=dat}; `--lemma awīlu --morph case=dat --lang akk` → no matches (ORACC honest absence, lemma itself resolves). Docs: conventions §6.1 (the design note w/ timings table), README cmd row, mcp.md nabu_search, backlog findings. Tests across conllu + proiel + oracc-absence: query/morph_facets_test (10), query/lemma_search_test (+6: UD dat-pl, PROIEL dat-pl, no-match/ORACC, lang+limit compose, malformed), mcp/tools_test (+3). Suite 1445/21,787 green, lint 192 clean. No migration, no sync, no network. One commit, not pushed.
2026-07-11 · P13-9 · d70f921 · Slovenian axis: goo300k + IMP — Early Modern Slovene lands (fable; two-phase, option B OWNER-APPROVED same day: both corpora via one shared imp-tei family + orig-canonical split confirmed). PHASE A (page-level, both records verified): CLARIN.SI DSpace zip bitstreams auth-free (goo300k-tei.zip 7.1 MB hdl 11356/1025; IMP-corpus-tei.zip 150.31 MB hdl 11356/1031); licenses verbatim "Creative Commons - Attribution 4.0 International (CC BY 4.0)" / "Creative Commons - Attribution-ShareAlike 4.0 International (CC BY-SA 4.0)" → attribution; actual TEI schemas downloaded and read. OVERLAP VERDICT: same documents, complementary layers — goo300k SAMPLES pages with GOLD annotation ("fully manualy validated", README sic), IMP has FULL texts with AUTOMATIC annotation (deposit verbatim "a fair amount of errors"); the gold labels are NOT inside IMP; shared sigils (ZRC_00001-1584 = Dalmatin Biblia) = alt-editions across sources, conventions §3, never dedupe. PHASE B: ImpTeiParser (imp-tei; streaming Reader; block = element with direct <s> children; passage text = HISTORICAL orig surface from <orig>/bare <w>/<pc>/<c> leaves, reg NEVER enters text; :gold emits tokens {form=orig, reg, lemma, msd #-stripped, gloss/gloss_bibl}, :none emits nothing; #header peeks sourceDesc bibl). Goo300k adapter: xi:include page walk, upstream document-global ab.N citations (cross-page ab part="F" = own passage), urn:nabu:goo300k:<sigil>-<year>, ZipFetch single zip, gold tokens feed passage_lemmas — proven end-to-end (fixture → Loader → Indexer: joger attested by pristine "Iogre"; svoj by "ſvoje, ſvojga"). Imp adapter: self-contained *-ana.xml, un-id'd <p>/<head> → per-tag counters (deposit frozen 2015), TEXT ONLY per the owner's silver default — no tokens, no lemma rows, lemma index stays gold-only (documented in adapter/registry/02-sources). NEW conventions §9 fold: sl ſ→s (Bohorič long s survives plain downcase — only FULL case folding maps it — so ſ-words were unfindable; digraph modernization deliberately NOT folded; landed before any sl sync, rebuild-storm caveat vacuous). Fixtures: goo300k 5 files/2 docs (1584 Biblia roots+pages incl. gloss + part="F" quirks, 1695 Sacrum promptuarium) + imp 2 docs (1584 alt-edition trim, WIKI00290-1855 whole) with zip-extract manifests. Registry goo300k + imp enabled:false sync_policy manual; 02-sources row 45 → READY. Demo parse (Dalmatin 1584, urn:nabu:goo300k:zrc_00001-1584:ab.2): "INu on je ſvoje dvanajſt Iogre k' ſebi poklizal…" — text_normalized "…svoje dvanajst iogre…", lemma joger glossed "apostol, učenec [sskj]". Suite 1504/22,072 green, lint 198 clean. NO real sync (owner fires the 7 MB / 150 MB GETs). One commit, not pushed.
2026-07-11 · P13-10 · 9be8d0b · Wiktionary-OCS dictionary (kaikki.org) + the reconstruction seed. Fourth dictionary-shelf occupant, THIRD shelf format (JSONL after TEI/CSV): `WiktionaryJsonlParser` (9th parser family; streamed line-per-record JSON; no upstream record id, so entry ids are minted `word:pos[:etymology_number]` with a positional `:n` for the 10 residual collisions MEASURED on the full 4,615-record file — incl. видимъ:verb:2 colliding WITH an ety number and the Glagolitic ⰿⰾⱑⰽⱁ; gloss = first gloss of the first glossed sense, trailing colon trimmed; body = etymology_text KEPT verbatim first — the Proto-Slavic/PIE chains are the owner's reconstruction-axis seed — then sense lines, raw_glosses preferred for their "(anatomy)" labels, glossless senses render upstream tags so bodies are never empty) + `WiktionaryCu` adapter (content_kind :dictionary, FileFetch single ~44 MB GET, :http_zip probe, slug wiktionary-cu, lang chu) + registry enabled:false/manual + CLI/MCP define gates widened to chu (Query::Define unchanged AGAIN — third shelf language in a row). License verbatim (kaikki.org/dictionary/): "This data is made available under the same licenses as Wiktionary - both CC-BY-SA and GFDL" → attribution. DEPRECATION documented: upstream flags the per-language JSONL (wiktextract #1178) though it serves; fallback recorded in 02-sources (filter full enwiktextract by lang_code=="cu"). Fold verdict: the EXISTING generic chu fold suffices (ан҃г→анг via the U+0483 Mn strip; jers/yuses are letters) — the survey's §9 open question settled, no new rule. Fixture: 278 stratified byte-verbatim lines (all 10 collision pairs, TOROT-gold богъ/глаголати, all 4 glossless, 18-sense слово, 4-per-POS, 25 PIE + 40 Proto-Slavic etymologies, every-32nd sweep, 12 homograph groups). Demo (scratch catalog): define богъ --lang chu → "god" + "Inherited from Proto-Slavic *bogъ."; define о → both homographs, PIE *h₃ebʰi verbatim in the prep body. Deliverable (b): improvements §1.11 registers the reconstruction/etymology shelf — kaikki Proto-Slavic (45.4 MB, ~5,195 words, sla-pro, structured `descendants` trees = reconstruction→attestation edges naming cu/orv/ru/be/uk reflexes) + PIE (11.5 MB, ~1,781 words, ine-pro) + Proto-Germanic, same dual license; join signals = kept etymology_text (forward/text) + descendants arrays (reverse/graph); NO adapter by owner scope. Tests: wiktionary_jsonl_parser_test (18) + wiktionary_cu_test (13, mirrors the B-T dictionary-shape conformance + loader idempotency + the define-glosses join) + mcp OCS-shelf test + CLI help pin.
2026-07-11 · P13-11 · c16f970 · Freising Manuscripts (Brižinski spomeniki) adapter — the first research_private source. Phase A verified in-file and the owner gate passed same day ("Approve, all six, sl"): license VERBATIM in bs.xml <availability> "Creative Commons Priznanje avtorstva-Brez predelav 2.5 Slovenija" (by-nd/2.5/si) = CC BY-ND 2.5 SI — the English page's BY-SA label is wrong, the machine-readable header governs; download path corrected (zips live at nl.ijs.si/e-zrc/bs-text.zip, NOT under /bs/ as the survey recorded — the /bs/-relative URL 404s); bs-text.zip (7.5 MB, single top dir bs/, TEI under bs/tei/, 41 XML) is the fetch target, bs.zip (341 MB audio) + bs-facs.zip excluded as separately copyrighted. THE structural finding: all 9 layers (bsCT/bsDT/bsPT/bsTR-{slv,eng,ger,ita,lat,pol}) share one skeleton div[mon]→page[folio]→line[n] with IDENTICAL @n/@id keys — 228 lines each, a perfectly line-aligned parallel corpus; the master bs.xml composes them by external entities (NOT resolved: each layer file parses standalone) and carries the ZRCola PUA glyph table (charDesc, <g corresp> refs only, no raw PUA in text). Owner-approved design executed: critical transcription = Passage#text (primary docs urn:nabu:freising:bs1..bs3, lang sl), diplomatic/-dt, phonetic/-pt + ALL SIX translations/-tr-* as line-aligned sibling documents (ORACC -en precedent); passage = manuscript line, citation "BS I, fol. 78r, l. 1" + folio + tei_id in annotations; per-line @lang honored (BS I's Latin tail 37–39 = lat); empty line 36 skipped. NEW PARSER FAMILY FreisingTeiParser (freising-tei): charDesc glyph map (unmapped ref = ParseError), flatten rules corr-over-sic / expan-over-abbr / scribal del-dropped-add-kept (the witness's FINAL state — keeping both would mint "potrae", a reading the parchment never carried) / notes+milestones+gap dropped, NFC at boundary. Adapter Freising: ZipFetch, license_class research_private, license string carries the Slovene grant verbatim; registry enabled:false sync_policy manual (edition frozen at 1.0, 2007). Query::Parallel gained the FREISING work pattern (bs<n> + -variants) AND a small cross-family refinement: the work's OWN document now outranks its variants in sibling selection (bs1-tr-eng --parallel sl → the critical bs1, not the string-max -tr-slv; CTS unaffected — bare work urns are never documents there; ORACC unaffected — the tablet was the only candidate anyway). MCP-exclusion EVIDENCE (verified, not assumed): freising_test wires the real manifest license_class through source row → Loader → Indexer → Tools and proves nabu_search default-hides bs1:1 ("glagolite" finds nothing), include_restricted opts in per call (hit carries license_class research_private), nabu_show withholds/reveals likewise; tools.rb header comment updated (research_private's first real occupant). Permission point logged in improvements §4.3 (first occupant: external-access features must exclude nd-class or secure permission; contact Ogrin/Erjavec). Fixtures: trimmed REAL files (Nokogiri trim script, mon I complete incl. the famous opening + the Latin tail, mons II/III first-page/3-lines, master bs.xml with full availability + 25-glyph charDesc), README with retrieval date/URLs/license verbatim/ZRCola note. Language codes: sl for the three transcription layers + slv translation (owner), eng/lat per repo precedent (deviation from the Phase-A en/la proposal — users type --parallel eng, the lat v/j fold applies), ger/ita/pol per upstream's own TEI ids. Deferred honestly: historical witness variants (bsCT-mik, bsDT-isa/koe/kop/pir/rak/von, bsPT-grf/rak), glossary bsLX (dictionary-shelf candidate), eZISS siblings (same family should generalize). Owner action queued: fire bin/nabu sync freising (7.5 MB GET), eyeball, flip enabled.
2026-07-11 · P13-8 · 5db4147 · Open-source finishers (docs-only, no code). Verified the P12-4 "no CI exists" claim was WRONG: `.github/workflows/ci.yml` (workflow `name: CI`) runs `bundle exec rake test` + `bundle exec rake lint` on push and pull_request, Ruby 3.3, concurrency-cancel — so added the GitHub Actions status badge `[![CI](https://github.com/arvicco/nabu/actions/workflows/ci.yml/badge.svg)](https://github.com/arvicco/nabu/actions/workflows/ci.yml)` directly under the README H1, and corrected the now-false "No CI badge because there is no public CI" line in the Status—honest section (it now says CI runs the suite + rubocop on every push/PR, the badge is the contract). CONTRIBUTING.md written to match the README's voice (personal project, tight editorial control, contributions welcome but curated): house rules distilled from CLAUDE.md — TDD non-negotiable, fixture discipline (real trimmed upstream samples, licenses quoted verbatim in fixture READMEs), shared conformance suite for new adapters, small diffs, ask-before-gem, canonical-is-read-only, no network in tests; a one-paragraph DCO note (git commit -s sign-off certifies you may contribute under the MIT LICENSE from commit cedec96, content licensing separate); how-it's-built one sentence + single link to docs/dev-loop.md; propose-a-source via an issue with license evidence (the oe/slavic-survey pattern). SECURITY folded in as a support one-liner inside CONTRIBUTING rather than a standalone SECURITY.md — argued: no release, no server, MCP is local stdio + structurally read-only, so a formal policy is ceremony at this size. README "Contributions:" paragraph now links CONTRIBUTING.md. No code touched; suite/lint untouched and green. One commit, not pushed.
2026-07-11 · gate13 · — · Phase 13 complete (11 packets, 2 parallel merges — the no-blocking rule's first live runs): Slavic survey II (honest: dictionaries locked, Miklosich one email away — email drafted for owner; Freising BY-ND caught in-TEI) → UD Ruthenian (7th treebank, Statut VKL 1566) → CCMH (4 OCS gospel mss, CC BY, Assemanianus+Savvina close the canon) → ORACC 5→33 projects (full SAA per owner "full SAA is the point") → ATF translations pivot (bulk ATF dead; per-text HTML with gold line refs; -en siblings → show --parallel on tablets; prose CC BY-SA not CC0, labeled via override) → Psalms alignment (piecewise Greek↔Hebrew numbering map, join/split psalms honestly unmapped, native ref shown; Paris Psalter deferred with evidence) → morph facets (UD façade over conllu+proiel, measured no-index verdict, ORACC honest absence) → Slovenian goo300k gold + IMP silver (imp-tei family, orig-canonical, ſ→s fold) → Wiktionary-OCS (9th family JSONL, 4.6k entries, etymologies kept; reconstruction shelf registered as improvements §1.11 — Proto-Slavic descendants trees + PIE extracts scouted for the owner's new comparativistics axis) → Freising (ND research_private posture, first MCP-excluded occupant, permission-points list seeded §4.3; GLAGOLITE line-aligned across 3 layers + 6 translations) → finishers (CI badge, CONTRIBUTING+DCO). Owner rulings codified: BY-ND in-scope, Early Modern Slovenian in-scope, compact status format, gate-waits-don't-block. P13-7 vocab dropped to register (phase ran full). Suite 1580/26,545, lint clean. Owner queue at merge: sync ccmh / ud / oracc (33 projects + SAA-en crawl) / goo300k / imp / wiktionary-cu / freising, then flips.

2026-07-12 · P14-1 · 4c9206e · The reconstruction shelf (improvements §1.11 SHIPPED; architecture §12 is the design record; owner-approved as-is 2026-07-12 after the Phase A gate). Phase A network-verified the three kaikki wiktextract reconstruction extracts (Proto-Slavic sla-pro 47.6 MB/5,431 records/5,195 words; PIE ine-pro 12.0 MB/1,905/1,781; Proto-Germanic gem-pro 65.3 MB/5,717/5,552; extraction 2026-07-09; license verbatim "…both CC-BY-SA and GFDL" → attribution; same DEPRECATED caveat + fallback as wiktionary-cu) and MEASURED the crosswalk against the live gold lemma index (564-record ranged sample): 64.5%/64.2%/54.7% of proto headwords naming a held language link to ≥1 attested folded gold lemma; the descendants `roman` field is load-bearing (got/san/xcl 0%→59/41/40% — db lemmas are romanized, Wiktionary words are in script). Shipped: DictionaryReflex value + DictionaryEntry#reflexes with the ContentHash only-when-non-empty guard (pre-P14-1 shas pinned — no revision storm); wiktionary-jsonl parser reflexes: option (depth-first worded-node flatten, lang-code map cu→chu/la→lat/sa→san + shape-valid identity + nil for the lone malformed "ML.", leading-asterisk-stripped conventions-§9 folds; wiktionary-cu untouched, backfill deferred); migration 007 dictionary_reflexes (catalog; indexed (language, word_folded)/(language, roman_folded)); DictionaryLoader reflex persistence (citation semantics — content of the sha, wholesale replace on revision); WiktionaryRecon adapter — ONE source, THREE dictionaries (wiktionary-sla-pro/-ine-pro/-gem-pro), three FileFetch subdirs with the shared top-level attic and the UD two-phase prepare/breaker/complete choreography, three :http_zip probe targets, registry enabled:false sync_policy:manual (~125 MB owner-fired first sync); Query::ReflexViews (query-time attestation counts from passage_lemmas — nothing resolved stored, the §10/§11 stance); Query::Etym + `nabu etym` + MCP nabu_etym (SEVENTH tool: attested→proto via word_folded/roman_folded match, cognates attested-first with honest nil counts, ONE bounded proto-to-proto ascent hop, include_restricted contract, graceful pre-007 degradation); define gained the asterisk convention (`define *bogъ` strips + scopes to -pro shelves, starred display, reflex views; CLI+MCP lang gates widened to sla-pro/ine-pro/gem-pro). Language-code posture (conventions §4): Wiktionary's non-ISO codes adopted verbatim — they pass the shape-only validation unchanged and the crosswalk speaks them. Fixtures: 210 byte-verbatim stratified records incl. the demo chains богъ→*bogъ→*bʰeh₂g- (grc ἔφᾰγον rides along) and guþ→*gudą(𐌲𐌿𐌸 roman)→*ǵʰutós, the ML. quirk line, Glagolitic script children, homographs, structural edges; deterministic recipe in the fixture README. Docs: architecture §12, conventions §4+§9, 02-sources #50, mcp.md seven tools, improvements §1.11 → SHIPPED, backlog findings. Deferred riders: wiktionary-cu descendants backfill, ine-pro ASCII fold.
2026-07-12 · P14-2 · aad360f · CCMH gospels into the alignment hub — the four Old Church Slavonic gospel manuscripts (Corpus Cyrillo-Methodianum Helsingiense; CC BY 4.0) wired into the `nt` work, taking the flagship five-way parallel to a THIRTEEN-way one. Config-only + one extractor guard (P11-5 `documents:` cts-verse shape, no new extractor): four witnesses appended to config/alignments.yml — labels `CCMH Assemanianus/Marianus/Savvina/Zographensis`, the "CCMH" prefix rendering them apart from the fifth witness PROIEL `marianus` so `align "MARK 2.3"` shows the two Marianus editions (PROIEL Cyrillic vs CCMH Helsinki transliteration) side by side, the alt-edition showcase. Work-vocabulary token (MATT/MARK/LUKE/JOHN) keys the CCMH per-gospel urn (…:mat/mar/luk/joh); the passage-urn tail IS the verse. BOOK MAP verified read-only against the live catalog (all 16 documents non-empty): every manuscript holds all four gospels, so all four books map for each — no whole-book lacunae, coverage fragmentary at the VERSE level (the lectionaries Savvina/Assemanianus honestly sparse: Savvina Mark 131 vs Marianus Mark 723), "not attested" per verse (P11-9). CHAPTER-0 VERDICT — EXCLUDE, argued from content: only the continuous-text codices carry chapter-0 refs (Marianus joh 19/luk 85/mar 47, Zographensis joh 2/luk 90; never the lectionaries), and inspection proves they are APPARATUS not verses — Marianus mar:0.1 "glavy eun[gelija]" + numbered kephalaia, Zographensis joh:0.1-0.2 "evaggeli-/-e ot Joana" (the incipit). They cross-align spuriously (Marianus + Zographensis both number Luke kephalaia 0.5), so `Store::AlignmentIndexer#cts_verse_refs` now DROPS a leading chapter-0 segment (`chapter_zero_apparatus?`) — general and safe (Bible chapters 1-indexed; verified LXX tlg0527 + Clementine Vulgate carry no chapter 0; a verse-0 superscription …:3.0 keeps its non-zero chapter and stays), INDEX-side only (kephalaia remain addressable via show/search). Confirmed on the scratch index: 0 chapter-0 refs for CCMH, Marianus rows drop by EXACTLY the census (854−19=835 joh, 1238−85=1153 luk, 723−47=676 mar), MARK 0.5 → 0 rows. :b2 VERDICT — NO handling, self-isolating: the parser's duplicate suffixes hit both chapter-0 headings (dropped with their chapter) and real verses (marianus:luk:13.11:b2); the generic ":"→"." fold turns a real-verse tail "13.11:b2" into a DISTINCT ref "LUKE 13.11.B2" that never false-aligns onto the primary verse. ACCEPTANCE (scratch alignment index over the READ-ONLY live catalog — no sync, no db/ mutation; live index picks the witnesses up at the owner's next sync/rebuild, config-only): `align "MARK 2.3"` renders all 13 nt witnesses, every one :ok on the live corpus, the four CCMH manuscripts (chu, transliteration) beside the five PROIEL treebanks + wscp + sblgnt + vulgate + WEB. Shipped-registry pin test updated openly (four CCMH labels + CCMH Marianus book map); new indexer test pins chapter-0 exclusion + b2 isolation. DEVIATION: made the chapter-0 drop general to cts-verse (not CCMH-gated) — chapter 0 is universally apparatus for any verse-grain edition, verified no existing witness relies on it. Files: config/alignments.yml, lib/nabu/store/alignment_indexer.rb, test/alignment_registry_test.rb, test/store/alignment_indexer_test.rb, docs (architecture §10, 02-sources row 19, backlog P14-2). Suite + lint green. NO real sync. One commit, not pushed.
2026-07-12 · P14-3 · 5c847ea · Vocab profiling — `nabu vocab URN [--limit N]` (the dropped P13-7, improvements §1.7). New `Nabu::Query::Vocab`: profiles a document / citation range / single passage against the whole gold-lemma corpus — total gold-lemma TOKENS, distinct lemmas, top DISTINCTIVE vocabulary, and in-document HAPAX legomena. Scope resolution is literal-first like Show (document || passage || range, reusing Query::Range); the tally parses only the scope's own passages' annotations_json (the cheap '"lemma"' substring guard + per-passage-language fold via Normalize.search_form, exactly as the Indexer folds), so token counts are exact; corpus baseline is the pre-built passage_lemmas index (passage-frequency, one indexed `lemma_folded IN(…)` GROUP BY, batched at 500 for SQLite's bind limit) + one `COUNT(*)`. METRIC = log-odds-ratio with an informative Dirichlet prior (Monroe, Colaresi & Quinn 2008), NOT the simple ratio the packet led with: measured on the live db, simple ratio floats near-hapax noise (alarius/liceor, doc-2/corpus-1, z-less) ABOVE the campaign vocabulary — the classic small-count blow-up — while the z-score's variance penalty ranks the real subject lexicon without any arbitrary min-count floor (Caesar → Caesar/hostis/castra/legio/proelium/exercitus; Cicero De officiis → officium/honestas/decorum/cupiditas/societas — validated). COVERAGE HONESTY: a document with zero gold tokens is not an error — it says so and names the gold-bearing languages straight from the lemma index (the only path that scans it, ~0.6s, once; normal path never does). COST (P13-6 measure-before-index precedent upheld): plain SELECTs, no new index — warm live timings Caesar 100ms, whole Vulgate NT (11,851 passages) 329ms, no-gold Beowulf 324ms, small ORACC Akkadian tablet 8ms; nothing exceeds ~350ms so a bespoke index would cost a rebuild to beat a sub-second post-filter. MCP verdict = CLI-only for v1, deferred (argued in the class note): unlike --random it IS research-facing, but an AI client gains little over nabu_search --lemma + nabu_define, and the 8% gold ceiling means it would answer "no gold lemmas" for most documents a model names (Homer/Vergil/papyri lack gold) — re-open when §3.1 lemma projection lifts coverage. Tests: test/query/vocab_test.rb (10, fixture treebank doc over a background corpus — totals, hapax, the log-odds-beats-singletons discrimination, --limit caps, range + single-passage scopes, no-gold honesty naming lat, withdrawn-excluded, unknown-urn NotFound). README command row added; docs/library.md §9 left for the gate. Suite 1590/26,584 green, lint 208 clean. Live db read-only throughout (delete-journal, SELECT-only, no mutation). One commit in worktree, not pushed.
2026-07-12 · P14-6 · 5c2135e · Incremental indexing — PHASE A MEASUREMENT ONLY (opus; no code, docs-only; STOP for owner decision). Instrumented the real per-sync reindex against an APFS clone of the live 3.9 GB catalog (live db read-only throughout), reusing production Store::Indexer/AlignmentIndexer verbatim with per-phase monotonic timers; the timed rebuild reproduced the live index EXACTLY (3,757,019 FTS / 2,513,786 lemma / 130,543 align rows). MEASURED cost ~70 s wall (2 runs): FTS5 insert ~36–37 s (~53%, the dominant phase), lemma build+insert ~23 s (~33%), catalog stream 6–10 s, alignment ~1.3 s (~2%), DDL negligible; NO ANALYZE/FTS5-optimize step exists (rebuild is clean-per-run — relevant to the incremental trade-off). Growth curve is EMPIRICAL not assumed (5-point FTS probe 1/2/3/3.76M): ~7–9 µs/row, gently rising = near-linear, mildly super-linear (segment-merge log factor); lemma tracks annotated-passage count (383k, 10.2%) NOT total N, alignment tracks registry NOT N. Projection: 5M ≈ 1.4–1.5 min, 10M ≈ 2.4–3 min (two scenarios on whether treebanks grow). Pain = two axes: absolute (crosses ~2 min ≈6–7M) and amplification (every per-source sync pays the whole corpus reindex — a ccmh sync = 340× over-index; per-source counts papyri 921k…freising 2k measured). Incremental options sketched with risk: (1) per-source reindex ~4–300× win, coarse/correct boundary — RECOMMENDED first; (2) dirty-doc tracking — finest but silent-staleness risk, forfeits the f(catalog) guarantee; (3) FTS5 tombstone deletes — needs periodic optimize the codebase lacks, worst simplicity/gain trade, skip. RECOMMENDATION: re-check-at-N, do NOT implement now (~70 s tolerable + provably correct; §4.2's "when it hurts" holds); trigger = interactive reindex crossing ~2 min (≈6–7M passages) or per-source cadence making the 340× amplification the daily annoyance. Report written into the P14-6 backlog block. Suite/lint untouched (docs-only). One commit, not pushed.
2026-07-12 · P14-8 · 0ae8971 · Proximity search — `search A --near B [--window N]`, the TLG-style daily-use collocation probe, lemma-aware. Design-first + measure-first per the P13-6/P14-3 precedent, NO schema addition (as the packet expected): built entirely on FTS5 NEAR over the EXISTING boundary-folded index. NEAR semantics probed on SQLite 3.53, not assumed: `NEAR(a b, N)` = at most N tokens BETWEEN the phrases, order-independent, N=0 adjacent — exposed verbatim as `--window` (default 10, FTS5's own). The window counts FOLDED tokens (conventions §9): honest per-word for grc/lat/…, documented caveat for akk/sux where sign-joins/determinatives fold to spaces (one transliterated word = several tokens, window reads tighter). Fold-both-sides carried into NEAR: each side folds to the query_forms union, MATCH = OR of NEAR clauses over the cartesian product of the sides' variants (the P6-4 cannot-miss argument per side). Lemma-aware anchor: `--lemma X --near B` expands X → distinct attested folded surface forms via passage_lemmas (each folded by its passage language), each a NEAR phrase — εἶπε near κύριος counts as λέγω near κύριος; expansion is naturally bounded (folding collapses accent variants: ὁ→25, εἰμί→99, λέγω→140 forms; MAX_LEMMA_FORMS=400 guards FTS expression limits only); homograph imprecision documented honestly (no token offsets in FTS). New Query::Proximity shares Search's Result/snippet/bm25 + CatalogJoin, so hits render as plain search hits with BOTH terms bracketed. MCP nabu_search gains near + window (clamp 0–50). Out of scope, said in help/docs: cross-passage adjacency (passage = the unit; tested), --morph+--near (usage error), collocation statistics (Phase 15 menu — this returns the raw material), FTS operators inside proximity terms (phrase-quoted literal). Measured live (3.6M passages, read-only): κύριος NEAR θεός w5 grc 43–113 ms; λόγος NEAR θεός w5 24–37 ms — John 1:1 AND the P.Oxy. 8.1151 amulet quoting it, the intertext promise already visible; --lemma λέγω --near κύριος w3 = 280 NEAR clauses, 95–284 ms, surfacing the prophetic formula τάδε λέγει κύριος; pathological ὁ w3 79 ms. Tests: query/proximity_test (10 — grc + lat folds, lemma-expanded suppletive, window boundaries incl. N=0, order independence, filters, cross-passage honesty, anchor XOR), cli_test +5 (real UD fixture), mcp/tools_test +3. README row, mcp.md, search long_desc. Suite 1598/26,593 green, lint clean. One commit in the P14-8 worktree, not pushed.
2026-07-12 · P14-9 · — · ORACC sync defects from the 2026-07-12 full sync (!20). Census-first corrected the orchestrator's framing: the 19 "duplicate passage urn" quarantines were 7 blms + 12 saao-saa08 (not "all blms"), plus 1 anchor. (1) Both dup groups are ONE root cause — the P11-7 sentence-label fallback: bilingual literary (blms) interleaves a Sumerian line with a LABEL-LESS Akkadian interlinear line-start, and range-labeled saa08 omens carry several label-less line-starts under one whole-text sentence, so distinct physical lines mint one suffix. `OraccJsonParser#disambiguate_suffixes` applies the house `:b2`/`:b3` positional suffix in document order (GRETIL/ccmh P9-4c precedent) — never quarantine, never merge; clean tablets keep byte-identical urns. (2) saao-saa08:P336145-en's final prose anchors at a TRAILING unlemmatized "traces of a name" row (print label "(r 3)") the corpusjson never mints; reattach-forward found nothing after it, so `OraccTranslationParser#anchor_label` now reattaches BACKWARD to the last line-start (r 2) — prose never dropped, suffix valid for Query::Parallel. (3) The 3 zero-doc projects (riao, ribo, dcclt-jena) are proxy corpora (`type:corpus` + `proxies` map, texts in out-of-scope sibling subprojects) that own no corpusjson BY DESIGN; `discovery_skips` (`proxy_corpus?`) recognizes them as a benign skip instead of falsely flagging unpack/layout errors (unrecognized 3 → 0). TDD (three failing tests first); fixtures = trimmed real slices in test/fixtures/oracc_p14_9/. Parse-only re-sync: +20 added / =17775 skipped / !0 errored, discovery 0 unrecognized (was 3). Suite 1669 runs 0 failures, lint clean.
