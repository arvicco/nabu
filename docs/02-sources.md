# Source Inventory & Ranking

Sources that need (or may eventually need) dedicated adapters. Scores are 1–5, higher is better. **Ease** = ease of integration (format cleanliness, bulk access, stability). **License** = openness/clarity (5 = CC/PD with bulk grant, 1 = restricted/unclear). Verify licenses and endpoints at implementation time — this table reflects the landscape as of mid-2026 and these projects do change terms and infrastructure.

## Tier 1 — build first (large, open, clean formats)

| # | Source | Content | Format | Size | Value | Ease | License | Notes |
|---|--------|---------|--------|:----:|:-----:|:----:|:-------:|-------|
| 1 | **PerseusDL canonical-greekLit / canonical-latinLit** | Core classical Greek & Latin literature | TEI XML (EpiDoc/CapiTainS), CTS URNs, git | 5 | 5 | 5 | 5 (CC BY-SA) | The backbone. Clean, versioned, citation scheme built in. Adapter here defines the reference implementation. **Status: LIVE (greekLit)** — first sync 2026-07-03: 744 docs / 238,525 passages, 25 quarantined (upstream `@n` gaps). **latinLit: READY** — `PerseusLatin < Perseus` shipped P7-3 (the designed one-line NAMESPACE sibling; registry `perseus-latin`, `enabled: false`), awaiting the owner-initiated first sync (multi-GB clone of canonical-latinLit) at the phase gate. |
| 2 | **OpenGreekAndLatin / First1KGreek** | Greek works not in Perseus (first 1,000 years CE) | TEI XML, CTS, git | 4 | 5 | 5 | 5 (CC BY-SA) | Same conventions as Perseus — nearly free once adapter #1 exists. **Status: LIVE** — first sync 2026-07-04: 1,054 docs / 226k passages, 37 quarantined. **P11-5: tlg0527 IS the LXX** — Swete's *Septuaginta* (1901), 55 grc book-documents already synced at verse grain (chapter.verse CTS urns) + ~40 Brenton eng translations; it is the alignment hub's `ot`-work Greek witness via the `cts-verse` extractor (registry-only, zero new code — see #44 for why Rahlfs is not ingestable). |
| 3 | **PROIEL treebanks** | Gothic (Wulfila), OCS Codex Marianus, Greek NT, Classical Armenian, Latin — fully lemmatized/parsed | PROIEL XML, git | 3 | 5 | 4 | 5 (CC BY-NC-SA) | Highest linguistic value per byte for comparative IE work. NC clause: record it. **Status: LIVE** — first sync 2026-07-04: 12 docs (release 20180408, frozen), 0 quarantined. |
| 4 | **Universal Dependencies (ancient treebanks)** | Ancient Greek, Latin, OCS, Old East Slavic, Vedic Sanskrit, Gothic, Coptic, Classical Chinese | CoNLL-U, git | 4 | 5 | 5 | 4–5 (mostly CC) | One parser handles a dozen languages. Overlaps PROIEL (TOROT/PROIEL data appear here too) — dedupe by document ID. **Status: LIVE** — first sync 2026-07-04: 4 treebanks / 12 docs, 0 quarantined. **P10-2 (Slavic expansion, shipped):** the `TREEBANKS` map grew to **6 treebanks** (now **7** with P13-1b below) — gothic-proiel, greek-proiel, sanskrit-vedic, latin-ittb, plus **`old-east-slavic-birchbark`** + **`old-east-slavic-rnc`** (both `CC BY-SA 4.0`, verified in each repo's README/LICENSE at fixture time 2026-07-09; language `orv`; genuinely-new vernacular/Middle-Russian OES, no PROIEL/TOROT overlap). Config+fixture change, no new family. The `chu-PROIEL`/`orv-TOROT` UD conversions are deliberately **excluded** (they re-load the native PROIEL/TOROT already synced), pinned by a dedup-guard test. Re-sync owner-fired at the phase gate. See `.docs/surveys/slavic-survey.md`. **P11-1: no Old English treebank exists in UD** (verified against the org repo list — the hoped-for config-only OE add is unavailable; watch releases). **P10-4 (split licensing):** the source class stays `nc` (most-restrictive present, correct for the PROIEL-derived treebanks) but the two Old East Slavic treebanks (birchbark, rnc) are attribution-class → carried as a per-document `license_class: attribution` via `documents.license_override` (adapter `TREEBANKS` map → `Document#license_override` → loader, a metadata-only relabel: no revision bump, `content_sha256` untouched), so the shareable/MCP shelf labels them attribution while the four legacy treebanks stay bare and inherit `nc`. **P13-1b (2026-07-11, shipped): `UD_Old_East_Slavic-Ruthenian` added** — the seventh treebank, `old-east-slavic-ruthenian`. "prosta mova" (Old Belarusian/Ukrainian) legal/chancery texts ca. 1380–1650 (Polotsk letters, Lithuanian Metrica, Lokhvitsa town-hall book), the THIRD East Slavic branch with zero text overlap with Birchbark/RNC/TOROT. LICENSE GATE PASSED at fixture time: `LICENSE.txt` verbatim "The treebank is licensed under the Creative Commons License Attribution-ShareAlike 4.0 International" + README machine-readable metadata `License: CC BY-SA 4.0` (GitHub license field reads `NOASSERTION`; the in-repo grant is authoritative, as for Birchbark/RNC) → `attribution` via the same P10-4 per-document `license_override`; source class stays `nc`. Language `orv` (file-stem prefix `orv_ruthenian`; the per-doc `# lang = orv-be` is a finer BCP-47 regional subtag, not the treebank tag). Config+fixture change, no new family; dedup guard untouched. Full UD org re-check: no Old Slovene / historical South Slavic treebank exists (survey: `.docs/surveys/slavic-survey-2.md`, gitignored planning material). **P25-2 (2026-07-17, shipped): two Old Irish glosses treebanks** — the Celtic axis's UD pair (survey `.docs/surveys/celtic-survey.md`), both diplomatic-edition conversions by Adrian Doyle (NUI Galway), both test-set only, language `sga` (the glosses code-mix Latin inside Irish — the same one-tag-per-treebank honesty as RNC under `orv`). **`old-irish-dipsgg`** (`UD_Old_Irish-DipSGG`): the St Gall Priscian glosses, 3,471 glosses in the collection (the dependency-annotated test split serves 64 sentences today), converted from Bernhard Bauer's morphological analysis (stgallpriscian.ie, Moran). LICENSE GATE PASSED at fixture time 2026-07-17: `LICENSE.txt` is verbatim, in its entirety, "CC BY-NC-SA 4.0" (README metadata agrees) → **rides the source `nc` class unchanged, no override**. **`old-irish-dipwbg`** (`UD_Old_Irish-DipWBG`): the Würzburg glosses on the Pauline Epistles, 42 glosses (tiny; upstream says more will be added as annotation continues), from wurzburg.ie. LICENSE verbatim: `LICENSE.txt` "The treebank is licensed under the Creative Commons License Attribution-ShareAlike 4.0 International" + README metadata `License: CC BY-SA 4.0` → `attribution` via the same P10-4 per-document `license_override` (the birchbark/RNC mechanics); source class stays `nc`. OVERLAP NOTE, no dedup: DipSGG's text = the same St Gall glosses CorPH (P25-0) carries at a different grain (UD dependencies here, CorPH morphology there) — two honest witnesses, the MW-beside-kaikki precedent; the dedup guard exists for RE-EXPORTS of already-synced sources (chu-PROIEL/orv-TOROT), which this is not. Config+fixture change, no new family; rows flow at the next owner-fired `bin/nabu sync ud` (no per-treebank enablement exists — the `ud` source syncs its whole TREEBANKS map). |
| 5 | **TOROT (Tromsø OCS & Old Russian Treebank)** | OCS + Old East Slavic, lemmatized | PROIEL XML / CoNLL-U | 3 | 5 | 4 | 5 | Directly serves the Slavic research axis. Shares PROIEL adapter. **Status: LIVE** — first sync 2026-07-04: 40 docs incl. Zographensis, 0 quarantined. |
| 6 | **GRETIL** | Vast Sanskrit (+ Pali, Prakrit, Tibetan…) e-texts | TEI P5 XML (mass-converted), legacy HTML/text | 5 | 2 | 3 | 3 (CC BY-NC-SA 4.0, `nc`) | **Status: READY (P9-4b + P9-4c + P10-3 recovery, 2026-07-09) — adapter + `gretil` parser family shipped, `enabled: false` awaiting owner-fired first sync.** Current corpus = mass-converted TEI P5 (**784** XML texts, ~240 MB; fetched via the shared git path from the byte-identical GitHub mirror `mmehner/gretil-corpus-tei@master` — mirror-scope verified: its 784 XML = the live `corpustei/` count, GRETIL's TEI conversion stable since 2021). **NOT EpiDoc** — no `refsDecl`, no CTS: a new bespoke parser family (`GretilParser`), not EpidocParser reuse. Four addressability rungs handled: (a) attribute-cited `div[@n]`/`lg[@xml:id]`/`l[@n]` → citation = `l/@n` verbatim (e.g. `1.001.01a`), lg/div as annotation context; (b) in-text `// BrbUp_N //` verse markers (the marker closes the verse it follows; stripped from the passage, its number the citation); (c) unaddressed prose → `pN` ordinals, flagged non-canonical; **(d, P9-4c) pure `lg[@xml:id]` citation** (`<lg xml:id="RgV_1.1.1">` → `1.1.1`, prefix stripped), a fallback pass run only when (a)/(b)/(c) leave the doc empty. **P9-4c quarantine recovery (118 → ~0):** the P9-4b first sync quarantined 118/781 files in two classes, recovered by (1) the xml:id rung above (~60 "no-citable" pure-`lg[@xml:id]` files — Rāmāyaṇa, Buddhacarita, Gītagovinda, Kirātārjunīya, …); (2) a single-pipe marker variant `| BBs_1,1.1 |` (~13 files — also a fallback pass, so already-loaded docs that merely contain `| … |` reading text stay untouched); (3) collision tolerance — a duplicate citation no longer quarantines the document but disambiguates deterministically (`:b2`, `:b3`, … in document order; the ddbdp precedent), plus a **multi-prefix join** for interleaved kārikā/commentary (≥2 marker prefixes whose bare numbers collide → `DhvK.1.1` vs `DhvA.1.1`). **P10-3 residue micro-packet (quarantines 8 → 4):** the P9-4c census left 8 quarantined — 4 genuinely unaddressable flat `<lg>`/`<l>` lists (`sa_abhinavagupta-paramArthasAra`, `sa_bIjanighaNTu`, `sa_lagadha-RgvedavedAGgajyotiSa`, `sa_vAmadeva-janmamaraNavicAra` — zero `@n`/xml:id/marker/prose, stay quarantined by design) + 4 recoverable **line-terminated marker** files whose marker sits at the END of an `<l>`, terminated by the `</l>` boundary rather than a closing delimiter: (a) hyphenated closed `// Abhidh-d_N //` (`sa_vimalamitra-abhidharmadIpa`; the primary `// Abbr_N //` prefix charset excludes the hyphen) and (b) leading-`//`-only `// Abbr_N</l>` (`sa_sAtvatatantra`, `sa_somAnanda-zAktavijJAna`, `sa_puruSottamadeva-ekAkSarakoza`; no closing `//`). Both recovered by a `LINE_MARKER` regex (hyphen-admitting prefix, optional closing `//`, `\z`-anchored) applied per `<l>` in a `LineMarkerExtraction` pass run LAST — only when the primary/xml:id/pipe passes leave the doc empty, so the two existing marker regexes stay byte-identical. Frozen-urn guard held: all 663 (P9-4c) / 773 (P10-3) previously-clean docs re-parse byte-identical (read-only two-parse census). IAST (`sa-Latn` → `san-Latn`), NFC. Vedic accents ride in **bare `<orig>`** (combining U+0331/U+030D) — policy KEEPS `<orig>` (pristine accented text) and DROPS any paired `<reg>`, the INVERSE of DdbdpParser's Leiden rule. License uniformly **CC BY-NC-SA 4.0** → `license_class: nc`, under an aggregator takedown disclaimer; ingestable for local research, kept out of the P8-1 MCP surface and any redistribution. urn `urn:nabu:gretil:<literal-filename-slug>:<citation>`. **P11-7 (silent strays, 2026-07-10):** two genuine editions — `sa_vijJAnezvara-mitAkSarA` (the 1.8 MB Mitākṣarā, 4788 prose passages) and `sa_haribhadrasUri-zAstravArttAsamuccaya` (701 xml:id verses) — carry a `<text>` with NO `@xml:lang` (their teiHeader `xml:lang="en"` describes the metadata, not the edition), so `peek_header` returned nil and discovery dropped them INVISIBLY (not even quarantined — never counted). Language now falls back to `<body>/@xml:lang` (`sa-Latn` → `san-Latn`, the accurate corpus form), then the filename `sa_` prefix — recovering both (+2 docs, live). The 4 P10-3-residue flat-`lg` files stay quarantined by design (unchanged). Systemic: `Gretil#discovery_skips` now makes any non-edition XML a VISIBLE skip count, never a silent drop. |
| 7 | **Digital Corpus of Sanskrit (DCS)** | Lemmatized Sanskrit corpus | Custom text format, git (dcs-data) | 4 | 5 | 4 | 5 (CC BY) | Every token analyzed. Complements GRETIL (analysis vs breadth). |
| 8 | **ORACC** | Assyriology projects: royal inscriptions (RINAP), SAA correspondence, ETCSRI, RĪM-Anum… (Akkadian + Sumerian) | ORACC JSON (`corpusjson/` cdl tree), per-project zip over HTTP | 4 | 4 | 5 | 5 (**CC0**, per-project) | **Status: READY (P10-1, 2026-07-09; `enabled: false` awaiting owner-fired first sync).** Adapter `Nabu::Adapters::Oracc` + new parser family `OraccJsonParser` + the first non-git fetch path (`Nabu::ZipFetch`: Last-Modified change detection, attic parity) landed with real fixtures (rimanum P405432/P405134/P405254-empty; etcsri Q004151/Q001299). Scope (P13-3, 2026-07-11, owner-approved "full SAA is the point" — 33 projects): the P11-6 five (**rimanum, etcsri, saao/saa01, rinap/rinap1, dcclt**) plus the P13-3 batch of 28 — **the complete SAA run saao/saa02–saa21 + saao/saas2** (SAA Studies 2: the lemmatised Assyrian Eponym/King List editions — a normal saao corpus subproject, verified Phase A), **riao** (17.3 MB), **ribo** (top level only; its babylon* subprojects out of scope), **blms**, and the four **dcclt subprojects** (ebla, jena, nineveh, signlists) — ~159 MB of new zips, all HEAD-verified live 2026-07-11 (200, `application/zip`, Last-Modified). License evidence is the P9-5a family scout (saao/riao/ribo/blms/dcclt all sampled CC0) — the standalone `metadata.json` still serves an EMPTY body over HTTP, so Phase A could not re-read it; the adapter's per-project license gate at sync is the actual guarantee. Extend `Oracc::PROJECTS` per project (slash-paths flatten to slugs: `saao/saa01` → `saao-saa01.zip`). Both expansions are config-only — parser family unchanged, no new fixtures; the owner-fired `bin/nabu sync oracc` after merge is the review gate. **Guard (standing, P11-6/P13-3): if that sync surfaces a cdl node type the `OraccJsonParser` does not handle, STOP and open a follow-up packet rather than hack the parser.** urn:nabu:oracc:<project>:<P/Q>:<line-label, spaces→dots> (o.1, r.5, seal.1.1'); cf→passage_lemmas (lemma search live for akk/sux); akk/sux translit fold in conventions §9. `nabu health --remote` probes ORACC over HTTP (P11-2): HEAD per project zip (reachability + `Last-Modified` drift vs the stored `.zip-fetch.json` pin) + `metadata.json` GET for license drift, through ZipFetch's vendored-cert path — reads honestly (reachable / never-synced), not gone. (Live note: the standalone `metadata.json` serves an empty body over HTTP, so the license row reads *unchecked* until upstream serves it; drift via the zip `Last-Modified` is unaffected.) Scout record (P9-5a, 2026-07-08): 144 public projects/subprojects; the founding dream (Nabu's own tablets). Open data = **ORACC JSON**, delivered as a per-project **zip over HTTP** at `https://oracc.museum.upenn.edu/json/<project>.zip` (subprojects hyphenated: `saao-saa01.zip`); `application/zip` + `Last-Modified` for change detection. **NO git data repo** (`oracc/publicdata` empty, `oracc/json` 404) → the six existing adapters' git-clone `fetch` does **not** apply; ORACC needs a **new HTTP-zip fetch path** (the first non-git fetcher). Each `corpusjson/<id>.json` is a nested **`cdl` tree** (`c` chunk / `d` discontinuity / `l` lemma): a transliteration line reconstructs by concatenating `l`-node `form` fragments between `line-start` `d`-nodes, tracking `surface` d-nodes (obverse/reverse) and line numbers — determinatives (`{ki}`, `{d}`), subscript numerals (`ZI₃`), and š/ṣ/ṭ preserved. Every `l`-node carries **gold lemmatization**: `form` (translit) · `norm` (normalization) · `cf` (lemma/citation form) · `gw`+`sense` (English gloss) · `pos`/`epos` — the `annotations_json` lemma-search goldmine (P7-5 path reuses it). **Prose translations are NOT in the JSON** (0 translation nodes across 265 SAA texts) — and no public bulk artifact carries them (P13-4 Phase A: `oracc/catf` on GitHub is C-ATF transliteration-only, 0 `#tr` lines, stale 2019; per-text `.atf`/`.xtf`/`_project-en.json` endpoints are soft-404s — a 200 whose body is `404\n` — on every mirror). **P13-4 (owner-approved 2026-07-11, "Two-stage SAA-first crawl"): the running English IS ingested** — crawled from the official per-text fragment `/<project>/<textid>/html` (CORS-open; a typical SAA letter ≈ 55 KB), whose translation cells carry the SAME node refs as the corpusjson `line-start` d-nodes, giving mechanical unit↔line alignment. `OraccTranslationParser` mints one `-en` sibling document per translated text (`urn:nabu:oracc:<project>:<P/Q>-en`, one passage per translation UNIT at its anchor line's label — SAA units are paragraph-grained: 39 lines / 6 units in SAA 01 175), and `Query::Parallel` gained the ORACC work pattern, so `show <tablet-urn> --parallel` span-groups tablet lines under their English blocks exactly like the card-cited Homers (MCP `nabu_show parallel: true` likewise). Which texts have English is machine-read from `metadata.json` `formats["tr-en"]` (saa01 264/265, rimanum 378/378, etcsri 1448/1456 + 1441 Hungarian (future), rinap1 88/96, dcclt 1229/4980). Crawl: PROJECT-SCOPED (`Oracc::TRANSLATION_PROJECTS`) — stage 1 = the saao projects (~250 MB, owner-fired); stage 2 extends the list, no machinery change. Fragments land at `<workdir>/html-en/<slug>/` OUTSIDE the zip-managed trees (never atticked by a build swap); polite sequential crawl (0.25 s delay), resumable (zip 304 ⇒ fetch only missing; changed build ⇒ re-crawl project); soft-404 texts counted missing, never written. **Translation license is NOT the CC0**: the prose is SAAo project content ("Content released under a CC BY-SA 3.0 license, 2007-20" — project footer; catf's README pointedly scopes CC0 to data "permitted to be released under CC0", and catf excludes translations) → `-en` documents carry `license_override: "attribution"` while the source stays open. Fixtures: saao-saa01 P224395 pair (corpusjson + fragment, real nested root) + fragments for the two fixtured rimanum tablets + trimmed saa01 metadata (X010028 = the real atf-but-no-tr-en text, exercising the crawl gate). Ids: **P-numbers** (physical artifacts: rimanum, saao) AND **Q-numbers** (composite/reconstructed texts: rinap, etcsri) — both stable CDLI/ORACC ids. **License is machine-readable per project AND per text** (`metadata.json` + every corpusjson carry `"license"`/`"license-url"`): all 8 sampled projects (saao, rinap, etcsri, riao, dcclt, blms, ribo, rimanum) report **CC0** (public domain) → `license_class: open`. NB the website/docs footer still states the 2014 blanket "CC BY-SA 3.0" (what this row previously recorded; a 2018 mirror cited it too) — the current JSON build supersedes it with CC0; the adapter **reads the per-project field**, never hardcodes. **New bespoke parser family (fable, ~DdbdpParser-tier)** — the cdl walk is mechanically simpler than XML mixed-content, but the policy decisions (translit folding for cuneiform: determinatives/subscripts/š-ṣ-ṭ; P-vs-Q urn; `akk-x-oldbab`/`sux` language tagging with Sumerian logograms inside Akkadian; norm/cf/gw annotations schema) are genuinely new. Starter candidates (sizes): `rimanum` 2.9 MB/337 texts (Akkadian, P-nums), `etcsri` 12.9 MB/1456 texts (Sumerian, Q-nums, well-lemmatized), `saao/saa01` 5.0 MB/264 (Sargon II letters), `rinap/rinap1` 3.3 MB/96 (composites). Fixture plan owner-approved 2026-07-08, executed in P10-1. **P11-7 (silent-ingestion defects, 2026-07-10):** (1) THE HEADLINE — subproject zips unpack with a NESTED ROOT (`saao-saa01/saa01/corpusjson`, not `saao-saa01/corpusjson`), so discover found 0 of saao/saa01 + rinap/rinap1's 361 texts while the sync reported success; `project_dir` now resolves `corpusjson/` at either depth, and a registered project whose tree exists but yields no `corpusjson` is a LOUD discovery note (`unrecognized`), never silence. (2) the catalog-only NO-CONTENT shape (object/surface skeleton, only `nonx` d-nodes, zero transcribed lines; ~112 dcclt files) now skips by rule (`Nabu::DocumentSkipped`, counted honestly) — the non-0-byte cousin of the P10-1 empty-file skip — instead of quarantining. (3) a lone LABEL-LESS `line-start` amid ~300 labeled lines (~58 dcclt files, e.g. P010104) falls back to the enclosing sentence c-node's label (`r xi' 10'`), recovering the line rather than quarantining the document; if that label is absent too, only that line is skipped. Together these drop oracc quarantines 170 → ~0 (parse-only). Skip visibility (the systemic fix): `Adapter#discovery_skips` classes content-pattern files selected / skipped-by-rule / unrecognized, printed as a `discovery:` line under the sync counts, unrecognized loud. **P14-9 (sync defects from the 2026-07-12 full sync, +10,899 docs / 30 projects, !20):** census corrected the orchestrator's framing (the 19 "duplicate passage urn" quarantines were NOT all blms — 7 blms + 12 saao-saa08, plus 1 anchor). (1) **collision tolerance** — bilingual literary (blms) and range-labeled omen (saa08) texts carry MULTIPLE label-less `line-start`s under one sentence, so the P11-7 sentence-label fallback resolves distinct physical lines (a Sumerian line + its Akkadian interlinear; an apodosis gloss + its base line) to ONE suffix; `OraccJsonParser#disambiguate_suffixes` now applies the house `:b2`/`:b3` positional suffix in document order (the GRETIL/ccmh P9-4c precedent) — never quarantine, never merge; clean tablets keep byte-identical urns (frozen guard). (2) **trailing-prose anchor** — saa08 P336145's final translation unit anchors at an unlemmatized "traces of a name" row (print label "(r 3)") the corpusjson never mints; reattach-FORWARD found no line-start after it, so `OraccTranslationParser#anchor_label` now reattaches BACKWARD to the last line-start (r 2) — prose never dropped, suffix still exists in the tablet for `Query::Parallel`. (3) **proxy corpora** — the 3 zero-doc projects (riao, ribo, dcclt-jena) are `type:corpus` PROXY corpora (a `proxies` map, texts hosted in out-of-scope sibling subprojects) that own no `corpusjson` BY DESIGN; `discovery_skips` recognizes them (`proxy_corpus?`) as a benign skip instead of falsely flagging an unpack/layout error. Fixtures: trimmed real slices in `test/fixtures/oracc_p14_9/` (blms P345480, saa08 P336559, the P336145 corpusjson+html pair, riao proxy corpus.json). Parse-only re-sync drops oracc quarantines 20 → ~0; all previously-loaded docs re-parse =skipped. |
| 8 | **ORACC** | Assyriology projects: royal inscriptions (RINAP), SAA correspondence, ETCSRI, RĪM-Anum… (Akkadian + Sumerian) | ORACC JSON (`corpusjson/` cdl tree), per-project zip over HTTP | 4 | 4 | 5 | 5 (**CC0**, per-project) | **Status: READY (P10-1, 2026-07-09; `enabled: false` awaiting owner-fired first sync).** Adapter `Nabu::Adapters::Oracc` + new parser family `OraccJsonParser` + the first non-git fetch path (`Nabu::ZipFetch`: Last-Modified change detection, attic parity) landed with real fixtures (rimanum P405432/P405134/P405254-empty; etcsri Q004151/Q001299). Scope (P13-3, 2026-07-11, owner-approved "full SAA is the point" — 33 projects): the P11-6 five (**rimanum, etcsri, saao/saa01, rinap/rinap1, dcclt**) plus the P13-3 batch of 28 — **the complete SAA run saao/saa02–saa21 + saao/saas2** (SAA Studies 2: the lemmatised Assyrian Eponym/King List editions — a normal saao corpus subproject, verified Phase A), **riao** (17.3 MB), **ribo** (top level only; its babylon* subprojects out of scope), **blms**, and the four **dcclt subprojects** (ebla, jena, nineveh, signlists) — ~159 MB of new zips, all HEAD-verified live 2026-07-11 (200, `application/zip`, Last-Modified). License evidence is the P9-5a family scout (saao/riao/ribo/blms/dcclt all sampled CC0) — the standalone `metadata.json` still serves an EMPTY body over HTTP, so Phase A could not re-read it; the adapter's per-project license gate at sync is the actual guarantee. Extend `Oracc::PROJECTS` per project (slash-paths flatten to slugs: `saao/saa01` → `saao-saa01.zip`). Both expansions are config-only — parser family unchanged, no new fixtures; the owner-fired `bin/nabu sync oracc` after merge is the review gate. **Guard (standing, P11-6/P13-3): if that sync surfaces a cdl node type the `OraccJsonParser` does not handle, STOP and open a follow-up packet rather than hack the parser.** urn:nabu:oracc:<project>:<P/Q>:<line-label, spaces→dots> (o.1, r.5, seal.1.1'); cf→passage_lemmas (lemma search live for akk/sux); akk/sux translit fold in conventions §9. `nabu health --remote` probes ORACC over HTTP (P11-2): HEAD per project zip (reachability + `Last-Modified` drift vs the stored `.zip-fetch.json` pin) + `metadata.json` GET for license drift, through ZipFetch's vendored-cert path — reads honestly (reachable / never-synced), not gone. (Live note: the standalone `metadata.json` serves an empty body over HTTP, so the license row reads *unchecked* until upstream serves it; drift via the zip `Last-Modified` is unaffected.) Scout record (P9-5a, 2026-07-08): 144 public projects/subprojects; the founding dream (Nabu's own tablets). Open data = **ORACC JSON**, delivered as a per-project **zip over HTTP** at `https://oracc.museum.upenn.edu/json/<project>.zip` (subprojects hyphenated: `saao-saa01.zip`); `application/zip` + `Last-Modified` for change detection. **NO git data repo** (`oracc/publicdata` empty, `oracc/json` 404) → the six existing adapters' git-clone `fetch` does **not** apply; ORACC needs a **new HTTP-zip fetch path** (the first non-git fetcher). Each `corpusjson/<id>.json` is a nested **`cdl` tree** (`c` chunk / `d` discontinuity / `l` lemma): a transliteration line reconstructs by concatenating `l`-node `form` fragments between `line-start` `d`-nodes, tracking `surface` d-nodes (obverse/reverse) and line numbers — determinatives (`{ki}`, `{d}`), subscript numerals (`ZI₃`), and š/ṣ/ṭ preserved. Every `l`-node carries **gold lemmatization**: `form` (translit) · `norm` (normalization) · `cf` (lemma/citation form) · `gw`+`sense` (English gloss) · `pos`/`epos` — the `annotations_json` lemma-search goldmine (P7-5 path reuses it). **Prose translations are NOT in the JSON** (0 translation nodes across 265 SAA texts) — and no public bulk artifact carries them (P13-4 Phase A: `oracc/catf` on GitHub is C-ATF transliteration-only, 0 `#tr` lines, stale 2019; per-text `.atf`/`.xtf`/`_project-en.json` endpoints are soft-404s — a 200 whose body is `404\n` — on every mirror). **P13-4 (owner-approved 2026-07-11, "Two-stage SAA-first crawl"): the running English IS ingested** — crawled from the official per-text fragment `/<project>/<textid>/html` (CORS-open; a typical SAA letter ≈ 55 KB), whose translation cells carry the SAME node refs as the corpusjson `line-start` d-nodes, giving mechanical unit↔line alignment. `OraccTranslationParser` mints one `-en` sibling document per translated text (`urn:nabu:oracc:<project>:<P/Q>-en`, one passage per translation UNIT at its anchor line's label — SAA units are paragraph-grained: 39 lines / 6 units in SAA 01 175), and `Query::Parallel` gained the ORACC work pattern, so `show <tablet-urn> --parallel` span-groups tablet lines under their English blocks exactly like the card-cited Homers (MCP `nabu_show parallel: true` likewise). Which texts have English is machine-read from `metadata.json` `formats["tr-en"]` (saa01 264/265, rimanum 378/378, etcsri 1448/1456 + 1441 Hungarian (future), rinap1 88/96, dcclt 1229/4980). Crawl: PROJECT-SCOPED (`Oracc::TRANSLATION_PROJECTS`) — stage 1 (P13-4) = the saao projects (~4.7k texts ≈ 250 MB, owner-fired); **stage 2 (P14-4, owner-approved 2026-07-12 "Full crawl") = the FULL project list**, the promised data-only change (no machinery touched). Stage-2 scope, machine-read locally from the synced metadata (55 KB/text calibration): 3,982 non-saao tr-en fragments ≈ 214 MB — rimanum 378, etcsri 1448, rinap/rinap1 88, dcclt 1229, blms 206, dcclt/ebla 105, dcclt/nineveh 440, dcclt/signlists 88; **riao/ribo/dcclt-jena are zero-English catalog hubs** (no corpusjson, empty `formats`) and stay provably inert through the tr-en gate. 84 of the 3,982 are orphans (tr-en id, no live corpusjson) — crawled, then counted skipped-by-rule at discover. English only: etcsri's 1441 `tr-hun` (Hungarian) remains the flagged config-shaped follow-up. Fragments land at `<workdir>/html-en/<slug>/` OUTSIDE the zip-managed trees (never atticked by a build swap); polite sequential crawl (0.25 s delay), resumable (zip 304 ⇒ fetch only missing; changed build ⇒ re-crawl project); soft-404 texts counted missing, never written. **Translation license is NOT the CC0**: the prose is SAAo project content ("Content released under a CC BY-SA 3.0 license, 2007-20" — project footer; catf's README pointedly scopes CC0 to data "permitted to be released under CC0", and catf excludes translations) → `-en` documents carry `license_override: "attribution"` while the source stays open. Fixtures: saao-saa01 P224395 pair (corpusjson + fragment, real nested root) + fragments for the two fixtured rimanum tablets + trimmed saa01 metadata (X010028 = the real atf-but-no-tr-en text, exercising the crawl gate). Ids: **P-numbers** (physical artifacts: rimanum, saao) AND **Q-numbers** (composite/reconstructed texts: rinap, etcsri) — both stable CDLI/ORACC ids. **License is machine-readable per project AND per text** (`metadata.json` + every corpusjson carry `"license"`/`"license-url"`): all 8 sampled projects (saao, rinap, etcsri, riao, dcclt, blms, ribo, rimanum) report **CC0** (public domain) → `license_class: open`. NB the website/docs footer still states the 2014 blanket "CC BY-SA 3.0" (what this row previously recorded; a 2018 mirror cited it too) — the current JSON build supersedes it with CC0; the adapter **reads the per-project field**, never hardcodes. **New bespoke parser family (fable, ~DdbdpParser-tier)** — the cdl walk is mechanically simpler than XML mixed-content, but the policy decisions (translit folding for cuneiform: determinatives/subscripts/š-ṣ-ṭ; P-vs-Q urn; `akk-x-oldbab`/`sux` language tagging with Sumerian logograms inside Akkadian; norm/cf/gw annotations schema) are genuinely new. Starter candidates (sizes): `rimanum` 2.9 MB/337 texts (Akkadian, P-nums), `etcsri` 12.9 MB/1456 texts (Sumerian, Q-nums, well-lemmatized), `saao/saa01` 5.0 MB/264 (Sargon II letters), `rinap/rinap1` 3.3 MB/96 (composites). Fixture plan owner-approved 2026-07-08, executed in P10-1. **P11-7 (silent-ingestion defects, 2026-07-10):** (1) THE HEADLINE — subproject zips unpack with a NESTED ROOT (`saao-saa01/saa01/corpusjson`, not `saao-saa01/corpusjson`), so discover found 0 of saao/saa01 + rinap/rinap1's 361 texts while the sync reported success; `project_dir` now resolves `corpusjson/` at either depth, and a registered project whose tree exists but yields no `corpusjson` is a LOUD discovery note (`unrecognized`), never silence. (2) the catalog-only NO-CONTENT shape (object/surface skeleton, only `nonx` d-nodes, zero transcribed lines; ~112 dcclt files) now skips by rule (`Nabu::DocumentSkipped`, counted honestly) — the non-0-byte cousin of the P10-1 empty-file skip — instead of quarantining. (3) a lone LABEL-LESS `line-start` amid ~300 labeled lines (~58 dcclt files, e.g. P010104) falls back to the enclosing sentence c-node's label (`r xi' 10'`), recovering the line rather than quarantining the document; if that label is absent too, only that line is skipped. Together these drop oracc quarantines 170 → ~0 (parse-only). Skip visibility (the systemic fix): `Adapter#discovery_skips` classes content-pattern files selected / skipped-by-rule / unrecognized, printed as a `discovery:` line under the sync counts, unrecognized loud. |
| 9 | **CDLI** | Cuneiform catalog + transliterations (~350k artifacts) | ATF + catalog dumps, git/API | 5 | 3 | 3 | 5 (CC) | Huge but many entries are catalog-only. Ingest transliterated subset. |
| 10 | **Papyri.info (DDbDP/HGV/APIS)** | Documentary papyri | EpiDoc TEI, full git dump (idp.data) | 4 | 4 | 4 | 5 (CC BY) | Everyday Greek/Latin — legal texts, letters. Own DDbDP parser (no CTS layer upstream). **Status: LIVE** — P5-1 restart-minting fix + owner sign-off 2026-07-04: 61,347 docs / 920,766 passages; 9,354 remaining quarantines audited honest (text-less cross-reference stubs; ~40 recoverable `<del>`-wrapped docs → Phase 6 candidate). |
| 11 | **Sefaria (sefaria-export)** | Hebrew Bible, Talmud, rabbinics + translations | JSON, git | 5 | 4 | 4 | 4 (varies per text, mostly PD/CC) | Massive, well-structured, per-text licensing metadata included. |
| 12 | **Wulfila Project** | Gothic corpus with Streitberg apparatus | HTML/XML | 2 | 4 | 3 | 4 (research use) | Small; PROIEL covers the text, Wulfila adds edition detail. Low priority if #3 done. |
| 40 | **PerseusDL/lexica — LSJ** | A Greek-English Lexicon (Liddell-Scott-Jones 1940), the reference Greek dictionary | TEI P4 (PersDict; betacode Greek), git, 27 letter-split files ~250 MB total repo | 4 | 5 | 4 | 5 (CC BY-SA 4.0) | **Status: READY (P11-4, `lexica` source, `enabled: false`)** — the first DICTIONARY-shaped source (`content_kind :dictionary`: entries in their own catalog tables, not passages; architecture §11). Betacode keys/orths/quotes decoded at the boundary (`Nabu::Betacode`); `<bibl n>` carries CTS urns (2014 upstream revision) → query-time citation resolution into the catalog. License verbatim in repo README + per-lexicon READMEs: "freely distributed under a CC BY-SA 4.0 license … You credit Perseus". Surfaces: `nabu define` / MCP `nabu_define` / lemma-search glosses. Owner-fired first sync pending (one ~160 MB clone covers both lexica). **P11-7 (α/θ recovery + verify, 2026-07-10):** the first sync quarantined the WHOLE `grc.lsj.perseus-eng1.xml` (α — the LARGEST letter section, ~18950 entries) and `grc.lsj.perseus-eng9.xml` (θ, ~1948) on a work-level `<bibl>` whose `@n` is a CTS urn with a TRAILING COLON (`urn:cts:greekLit:tlg0027.tlg0088:` — empty citation suffix), which built an invalid empty-string `DictionaryCitation` → `ValidationError` → `ParseError`. **The P11-7 census misdiagnosed these as "alternate single-file editions to exclude by rule"; disk evidence shows they are ordinary α and θ letter files (every `eng1`…`eng27` maps to a distinct Greek letter).** Excluding them would have DELETED ~20900 entries including the entire α section; the real fix is `cite_parts` minting a work-level citation (`citation: nil`) for an empty suffix, so both files parse whole (quarantines 2 → 0). Separately, `nabu verify` on any dictionary source crashed (`document.urn` on a `DictionaryDocument`), aborting the WHOLE verify run — Verify now routes `content_kind :dictionary` to entry-level hash reconciliation (`urn:nabu:dict:<slug>:<entry_id>`), completing over passage + dictionary sources together. |
| 41 | **PerseusDL/lexica — Lewis & Short** | A Latin Dictionary (Lewis & Short 1879), the reference Latin dictionary | TEI P4 (PersDict; Unicode variant `lat.ls.perseus-eng2.xml`, 77 MB — the betacode eng1 twin is skipped) | 4 | 5 | 4 | 5 (CC BY-SA 4.0) | **Status: READY (P11-4)** — same `lexica` source/clone/adapter as #40, dictionary slug `lewis-short`. Plain-Latin keys with homograph digits (`volo1`); glosses ride in italics (first-sense non-abbreviation run) rather than `<tr>`; `<bibl n>` CTS urns often edition-anchored (`phi0474.phi055.perseus-lat1:1:2:4` — Cic. Off., live in-catalog) with occasional malformed/wrong urns upstream → best-effort resolution, honest misses. |
| 42 | **Clementine Vulgate** (Tweedale/eBible.org text, via seven1m/open-bibles) | The FULL Latin bible — Sixto-Clementine 1592, OT + deuterocanon + NT (the in-catalog PROIEL latin-nt is NT-only) | **USFX milestone XML** (`<book id="MRK">` + `<c id>`/`<v id>…<ve/>` milestones), ONE 4.65 MB whole-bible file in the ~76 MB open-bibles git collection → new small `UsfxParser` family (streaming Reader) | 3 | 5 | 5 | 5 (**Public Domain** → `open`) | **Status: READY (P11-5, `enabled: false` awaiting owner-fired first sync).** License verbatim: open-bibles README row "Clementine Latin Vulgate \| Public Domain"; eBible.org: "No person, company, or organization may claim any kind of copyright or restriction on this version of the Bible... even if they make changes." (NB no repo-wide LICENSE file — per-file assertion; the Clementine text itself is 1592; NOT the DBG-copyrighted Stuttgart/Weber-Gryson.) One document per book: `urn:nabu:vulgate:<osis-code-lc>` (mrk, gen…), passages `<doc>:<ch>.<v>`, per-book `<h>` titles (Marcus, Genesis). Both alignment-hub works list its books (`cts-verse`): NT books beside the PROIEL five + SBLGNT; OT books beside the LXX (only scout-verified book codes mapped in the `ot` registry — extend from minted urns after first sync, config-only). Upstream pinned at scout time: `8c31c38` (2026-06-05). Fixture: GEN 1 + MRK 1–2 + JHN 1:1–18 trim. |
| 43 | **SBLGNT** (SBL Greek New Testament, Faithlife/SBLGNT) | Critical edition of the Greek NT (Holmes 2010, v1.2 2023) — the hub's second Greek NT witness beside PROIEL's treebank | **Verse-per-line TSV** (`Mark 1:1<TAB>text` after a Greek title line), 27 per-book files, ~2.3 MB git repo → new trivial `SblgntParser` family. The word-level XML variant + `sblgntapp` apparatus files are not ingested; ⸀⸂⸃ apparatus sigla embedded in the verse text are kept verbatim | 2 | 4 | 5 | 5 (**CC BY 4.0** → `attribution`) | **Status: READY (P11-5, `enabled: false` awaiting owner-fired first sync).** License verbatim (repo README): "The SBLGNT is licensed under a Creative Commons Attribution 4.0 International License. Copyright 2010 by the Society of Biblical Literature and Logos Bible Software." — the historically restrictive SBLGNT EULA is SUPERSEDED (sblgnt.com/license itself now serves CC BY 4.0); GitHub license detection agrees. NB `morphgnt/sblgnt`'s morphology layer is CC-BY-SA-3.0 copyleft — deliberately NOT used (plain text only). One document per book file: `urn:nabu:sblgnt:<stem-lc>` (mark, 1cor, phlm…), passages `<doc>:<ch>.<v>`, Greek first-line titles (ΚΑΤΑ ΜΑΡΚΟΝ). `nt` alignment witness (`cts-verse`, 27-book map; Phlm→PHILEM-style token mapping done in the registry keys). Pinned at scout time: `c4d241a` (2025-01-19). Fixture: Mark 1:1–2:12 + John 1:1–18 trims, 3John whole. |

## Tier 2 — high value, moderate friction

| # | Source | Content | Format | Size | Value | Ease | License | Notes |
|---|--------|---------|--------|:----:|:-----:|:----:|:-------:|-------|
| 13 | **TITUS (Frankfurt)** | Widest IE coverage: Avestan, Old Persian, Tocharian, Hittite, Old Prussian, Vedic, OCS… | Legacy HTML, framesets, custom encodings | 4 | 5 | 1 | 2 (restrictive, access requests for some texts) | The prize and the pain. Scrape politely with per-text caching, or pursue access (see unlockables doc). Encoding archaeology required. **Status: SURVEYED-BLOCKED (P9-6, 2026-07-08).** Custom terms (quoted, `texte2.htm`): "can be used freely for scholarly purposes, provided that they are quoted as sources … must not be used for any kind of commercial usage"; some texts members-only; **no redistribution grant, no standard license** → `research_private` at best. Legacy HTML/custom encodings, dir index 403s bots. OCS canon covered more cleanly by CCMH/obdurodon (P9-6). Unblock: private per-text caching + written permission. See `.docs/surveys/slavic-survey.md`. **P13-1 re-check (2026-07-11): UNCHANGED** — same terms verbatim; UB Frankfurt e-media terms add "Volltexte dürfen weder elektronisch noch in gedruckter Form an Dritte weitergegeben … noch öffentlich zugänglich gemacht werden"; no bulk endpoint; front page still stamped 2003. |
| 14 | **ETCSL (Oxford)** | Sumerian literary corpus, composite texts + translations | Legacy TEI/HTML | 2 | 4 | 3 | 4 (free for scholarship) | Frozen project — scrape once, never re-sync. |
| 15 | **Thesaurus Linguae Aegyptiae (AES corpus)** | Ancient Egyptian, lemmatized | JSON/text dumps | 4 | 4 | 3 | 5 (CC BY-SA) | Earlier Egyptian corpus downloadable; hieroglyphic encoding (MdC/Unicode) needs care. |
| 16 | **Kanseki Repository (kanripo)** | Classical Chinese canon | Plain text + metadata, git (one repo per text) | 5 | 3 | 4 | 5 (open) | Thousands of small git repos; batch-clone adapter. Value depends on whether Chinese enters scope. |
| 17 | **Perseus Ancient Greek/Latin dependency treebanks (AGDT/LDT)** | Treebanked Homer, tragedy, etc. | XML/CoNLL-U | 2 | 4 | 4 | 5 | Partially inside UD already; native format has richer annotation. |
| 18 | **Monumenta Frisingensia (Brižinski spomeniki, ZRC SAZU)** | Oldest Slovenian (and oldest Latin-script Slavic) texts, ca. 972–1039: diplomatic + critical + phonetic transcriptions, 6 translations, glossary | **TEI P4** (`TEI.2`/tei2.dtd, ZRCola PUA glyphs — NOT P5/EpiDoc → small bespoke family) — full TEI source openly downloadable, no auth (`nl.ijs.si/e-zrc/bs/tei/`, `bs-text.zip`) | 1 | 5 | 3 | 2 (**CC BY-ND 2.5 SI** → restricted posture) | **Status: READY (P13-11, 2026-07-11; `enabled: false` awaiting owner-fired first sync) — ingested under the ND posture, owner-ruled 2026-07-11.** Adapter `Nabu::Adapters::Freising` + new parser family `FreisingTeiParser` (freising-tei: the eZISS P4 layout — master `bs.xml` charDesc ZRCola-glyph map, per-layer `<div>` fragment files read directly, external entities never resolved). **The ND posture:** `license_class: research_private` (license string CC BY-ND 2.5 SI, verbatim in the manifest) — default-excluded from every MCP tool, per-call `include_restricted` opt-in (evidence tests in `test/adapters/freising_test.rb` wire the real manifest through source row → indexer → tools); permission-point entry logged in `improvements.md` §4.3 (first occupant): any future external-access feature excludes nd-class sources by design or secures permission (contact Ogrin/Erjavec, ZRC SAZU). **Design (owner-approved 2026-07-11):** document = monument × layer; the CRITICAL transcription is the primary (`urn:nabu:freising:bs1..bs3`, language `sl`); diplomatic (`-dt`), phonetic (`-pt`) and ALL SIX translations (`-tr-slv/-tr-eng/-tr-ger/-tr-ita/-tr-lat/-tr-pol`) ride as line-aligned sibling documents (the ORACC `-en` precedent; `Query::Parallel` gained the freising work pattern — layers are line-for-line, every anchor a verse pair, and the work's own document outranks variants on back-resolution). Passage = manuscript line (upstream keeps `@n`/`@id` identical across all 9 layers, 228 lines each); citation `"BS I, fol. 78r, l. 1"` + folio + upstream tei_id in annotations; the empty `<line n="36"/>` of BS I yields no passage; the Latin tail (BS I 37–39) carries per-line `language: lat`. Flattening: corr-over-sic, expan-over-abbr, scribal del dropped/add kept (the witness's final state), notes/milestones dropped, ZRCola `<g>` refs resolved to standard Unicode (unmapped glyph = ParseError). Fetch = ONE 7.5 MB `bs-text.zip` via `Nabu::ZipFetch` — NB the zip lives at `nl.ijs.si/e-zrc/bs-text.zip` (NOT under `/bs/`); the 341 MB `bs.zip` (audio © ZRC SAZU/RTVS) and `bs-facs.zip` (© BSB München) are excluded, separately copyrighted. Deferred (future packets): the historical witness variants (`bsCT-mik`, `bsDT-isa/koe/kop/pir/rak/von`, `bsPT-grf/rak`) and the glossary `bsLX` (a `content_kind :dictionary` candidate). THE survey-II license catch stands recorded: the English HTML page says "Attribution-Share Alike 2.5 Slovenia" but the TEI `<availability>` reads verbatim "Creative Commons Priznanje avtorstva-Brez predelav 2.5 Slovenija" (`creativecommons.org/licenses/by-nd/2.5/si/`) — the machine-readable header governs. Clean unblock remains a permission email to Ogrin/Erjavec (owner-initiated, not sent). eZISS siblings (Škofjeloški pasijon 1725–27 — also verified BY-ND — Kapelski pasijon, oath texts, Zois letters) same deal: open TEI P4 zips, per-edition `<availability>`, the freising-tei family should generalize. Zero overlap with the OCS canon (Latin-script Old Slovene). See `.docs/surveys/slavic-survey-2.md`, `test/fixtures/freising/README.md`. |
| 45 | **CLARIN.SI historical Slovene: goo300k + IMP** (Erjavec, JSI) | goo300k: 89 texts / 293,919 words **1584–1899**, GOLD word-level annotation (modernized form + lemma + MSD + archaic-vocab glosses); IMP 1.1: 658 texts / **17,723,566 tokens** 1584–1919, automatic annotation (trained on goo300k) + digital library | TEI P5 (IMP schema, not EpiDoc/CTS — the `imp-tei` family) + vertical; open zip bitstreams, no auth (`hdl.handle.net/11356/1025`, `11356/1031`) | 4 | 3 | 3 | 5 (goo300k **CC BY 4.0**; IMP **CC BY-SA 4.0**) | **Status: READY (P13-9, 2026-07-11)** — scope ruled IN (owner: "there isn't much before Early Modern Slovenian at all, so it's in-scope"), **option B OWNER-APPROVED 2026-07-11**: BOTH corpora on the one shared `imp-tei` parser family. Licenses verbatim from the deposit pages: "Creative Commons - Attribution 4.0 International (CC BY 4.0)" / "Creative Commons - Attribution-ShareAlike 4.0 International (CC BY-SA 4.0)" → `attribution`, MCP-safe. **`Goo300k` = the gold flagship**: passage text is the HISTORICAL orig spelling (Bohorič ſ kept; conventions §9 gained the `sl` ſ→s fold), the gold reg/lemma/MSD layer rides in `annotations["tokens"]` and feeds `passage_lemmas` — the sl lemma axis. **`Imp` = the silver sibling, TEXT ONLY** (owner default): upstream's own caveat verbatim — "Note that the annotations are automatic, so they contain a fair amount of errors" — so no tokens ingested, no lemma rows; the lemma index stays gold-only. Shared documents (e.g. Dalmatin's 1584 Biblia, sigil ZRC_00001-1584 in both) are ALT-EDITIONS across the sources (goo300k samples pages, IMP full text) — never dedupe (conventions §3). Registry `goo300k` + `imp`, both `enabled: false` awaiting the owner-fired first real syncs (7.1 MB / 150.31 MB single-zip ZipFetch). Companion lexicon imp25k (11356/1032) = normalization data, not dictionary-shelf. Also on CLARIN.SI: **MEMIS** (11356/1376, CC BY-SA 4.0 TEI, 51 medieval **Latin** inscriptions from Koper/Piran 1222–17th c. — tiny, out of the Slavic axis). See `.docs/surveys/slavic-survey-2.md`, `test/fixtures/goo300k/README.md`, `test/fixtures/imp/README.md`. |
| 46 | **Wiktionary OCS extraction (kaikki.org / wiktextract)** | Machine-readable OCS glossary: ~4,548 senses / ~4,100 lemmas (headword, POS, senses, forms) — a crowd glossary, NOT a scholarly critical dictionary (no attestation apparatus) | **JSONL** (one object per sense), open download → dictionary shelf's would-be third format (after TEI + CSV): small new JSONL family, `content_kind :dictionary`, citations empty (Bosworth-Toller precedent) | 2 | 2 | 4 | 4 (CC-BY-SA + GFDL) | **Status: SURVEYED (P13-1, 2026-07-11) — the ONLY ingestable Slavic dictionary today; LATER.** License verbatim (kaikki.org/dictionary/): data "made available under the same licenses as Wiktionary - both CC-BY-SA and GFDL" (+ wiktextract citation request) → `attribution`. Would light up `define` for `chu` lemma hits from TOROT/PROIEL/CCMH gold. Best bundled with Miklosich (#48) into one dictionary packet if that unblock lands; standalone it is a modest win. OCS folding needs a conventions §9 entry (jers/yuses). See `.docs/surveys/slavic-survey-2.md`. |
| 45 | **CLARIN.SI historical Slovene: goo300k + IMP** (Erjavec, JSI) | goo300k: 89 texts / ~294k words **1584–1899**, GOLD word-level annotation (modernized form + lemma + POS); IMP 1.1: 658 texts / **17.7M tokens** 1584–1919, automatic annotation (trained on goo300k) + digital library | TEI P5 (IMP schema, not EpiDoc/CTS → small bespoke TEI profile) + vertical; open zips, no auth (`hdl.handle.net/11356/1025`, `11356/1031`) | 4 | 3 | 3 | 5 (goo300k **CC BY 4.0**; IMP **CC BY-SA 4.0**) | **Status: SURVEYED (P13-1, 2026-07-11) — ingestable, LATER pending owner scope call.** Licenses verified verbatim on the deposit pages: "Creative Commons - Attribution 4.0 International (CC BY 4.0)" / "Creative Commons - Attribution-ShareAlike 4.0 International (CC BY-SA 4.0)" → `attribution`, MCP-safe. The scope question: Early Modern print, not ancient — RNC Middle Russian (to 1700) is precedent for the 16th–17th c. slice; the 19th c. tail is beyond charter. If the Slovenian axis is wanted, goo300k (gold) first. Companion lexicon imp25k (11356/1032) = normalization data, not dictionary-shelf. Also on CLARIN.SI: **MEMIS** (11356/1376, CC BY-SA 4.0 TEI, 51 medieval **Latin** inscriptions from Koper/Piran 1222–17th c. — tiny, out of the Slavic axis). See `.docs/surveys/slavic-survey-2.md`. |
| 46 | **Wiktionary OCS extraction (kaikki.org / wiktextract)** | Machine-readable OCS glossary: ~4,548 senses / ~4,100 lemmas (headword, POS, senses, forms) — a crowd glossary, NOT a scholarly critical dictionary (no attestation apparatus) | **JSONL** (one object per line = one word × POS × etymology), open download → the dictionary shelf's third format (after TEI + CSV): parser family `wiktionary-jsonl`, `content_kind :dictionary`, citations empty (Bosworth-Toller precedent) | 2 | 2 | 4 | 4 (CC-BY-SA + GFDL) | **Status: READY (P13-10, 2026-07-11)** — `WiktionaryCu` adapter shipped: the fourth dictionary-shelf occupant, first JSONL dictionary. One ~44 MB JSONL over HTTP (FileFetch; 4,615 records / 4,548 words at fixture time, enwiktionary dump 2026-07-06). License verbatim (kaikki.org/dictionary/, "Copyright and license"): "This data is made available under the same licenses as Wiktionary - both CC-BY-SA and GFDL" (+ wiktextract citation request: Ylönen, LREC 2022) → `attribution`. **DEPRECATION CAVEAT:** upstream labels the per-language JSONL "DEPRECATED, will be removed in the near future" (wiktextract #1178) though it serves today; recorded fallback = filter the full enwiktionary extract (~2.6 GB compressed) by `lang_code == "cu"`. No top-level record id → entry ids `<word>:<pos>[:<ety#>]` + positional `:n` for the 10 residual full-file collisions. Etymology text KEPT in bodies (the Proto-Slavic/PIE chains — the reconstruction-axis seed, improvements register). Folding: the EXISTING generic `chu` fold suffices — jers/yuses are letters and stay, titlo/palatalization (U+0483/U+0484) are combining marks and strip; no new conventions §9 rule (the survey's open question, settled at fixture time). `define богъ --lang chu` → "god"; TOROT/PROIEL/CCMH gold `chu` lemma hits carry these glosses. Registry `wiktionary-cu`, `enabled: false`, `sync_policy: manual`, awaiting the owner-fired first real sync. Crowd glossary, NOT SJS — still best bundled with Miklosich (#48) if that unblock lands. **P16-5 (2026-07-13): descendants backfill shipped** — the OCS records' `descendants` trees now crosswalk into `dictionary_reflexes` (`reflexes: true`, same path as #50; census: 589/4,615 entries → 2,210 edges, ~244 gold-joinable, mostly orv/sl), landing in the live db at the next owner-fired `sync wiktionary-cu --parse-only` (re-mints the shelf's revisions). See `.docs/surveys/slavic-survey-2.md` + `test/fixtures/wiktionary-cu/README.md`. |
| 50 | **Wiktionary reconstructions (kaikki.org / wiktextract): Proto-Slavic, PIE, Proto-Germanic** | The reconstruction shelf: ~5,195 Proto-Slavic + ~1,781 PIE + ~5,552 Proto-Germanic reconstructed headwords (senses, etymology, forms) PLUS the machine-readable `descendants` trees — reconstruction→reflex edges naming the catalog's own languages (cu/orv/sl; got/ang/non; grc/la/sa/xcl) | **JSONL** (the same wiktextract shape as #46, plus `descendants` on ~89% of records + `original_title` "Reconstruction:…"); parser family `wiktionary-jsonl` with `reflexes: true`, `content_kind :dictionary` | 3 | 3 | 4 | 4 (CC-BY-SA + GFDL) | **Status: READY (P14-1, 2026-07-12; `enabled: false` awaiting owner-fired first sync, ~125 MB across three GETs)** — `WiktionaryRecon` adapter shipped: ONE source, THREE dictionaries (`wiktionary-sla-pro`/`wiktionary-ine-pro`/`wiktionary-gem-pro`; three FileFetch subdirs, shared top-level attic, UD two-phase choreography, three probe targets). License verbatim (kaikki.org/dictionary/, re-verified 2026-07-12): "This data is made available under the same licenses as Wiktionary - both CC-BY-SA and GFDL" (+ Ylönen LREC 2022 citation request) → `attribution`. Same **DEPRECATION CAVEAT** as #46 (wiktextract #1178; fallback = filter the full enwiktionary extract by lang_code). Language codes `sla-pro`/`ine-pro`/`gem-pro` adopted verbatim from Wiktionary (non-ISO; conventions §4). The `descendants` trees flatten into the `dictionary_reflexes` crosswalk (migration 007, architecture §12): worded nodes only, upstream lang_code verbatim + mapped catalog language (cu→chu, la→lat, sa→san; the lone malformed "ML." → NULL), word+roman with conventions-§9 folds (leading asterisk stripped; `roman` is the script bridge — got/san/xcl gold lemmas are romanized). Resolution at QUERY time against `passage_lemmas`. Scout-measured crosswalk value (Phase A, live db): record-level 64.5% sla-pro / 64.2% ine-pro / 54.7% gem-pro of proto headwords naming a held language link to ≥1 attested gold lemma; roman rescues got 0→59%, san 0→41%, xcl 0→40%. Query surface: `define *bogъ` (asterisk convention) + **`nabu etym`** + MCP **`nabu_etym`** (seventh tool) — attested→proto→cognates with attestation counts, one bounded proto-to-proto ascent hop (богъ→\*bogъ→\*bʰeh₂g-; guþ→\*gudą→\*ǵʰutós; the царь chain crosses \*cěsařь/\*kaisaraz). Crowd-curated (Wiktionary), NOT Derksen/LIV — an honest crowd etymology layer over a private attested corpus. See `test/fixtures/wiktionary-recon/README.md`. |
| 51 | **Monier-Williams Sanskrit-English Dictionary — Cologne CDSL** (The Sanskrit Library + Thomas Malten, sanskrit-lexicon.uni-koeln.de) | The canonical Sanskrit dictionary (Oxford 1899, supplement merged): 286,525 records / **193,890 grouped entries**, machine-readable grammar (`lex=` gender on 91%, verb class+pada on 10,606 roots, Dhātupāṭha/Whitney links), **328,060 tagged `<ls>` citations** (871-row sigla key upstream), and a dictionary-native cognate layer (973 records / 2,537 `<lang>` comparanda tags, 98.9% parseable) | **mwxml.zip** (11.1 MB → 64 MB `mw.xml`, one record per LINE, DTD contract; headwords + in-body Sanskrit **SLP1**, Greek cognates polytonic Unicode, NO Devanagari) → parser family `mw-xml` (line-streaming; H1–H4 mains grouped with their A/B/C/E continuations), `content_kind :dictionary` | 3 | 5 | 4 | 3 (**CC BY-NC-SA 3.0** → `nc`) | **Status: READY (P17-4, 2026-07-13)** — `Mw` adapter shipped: the dictionary shelf's Sanskrit occupant, completing the desk loop LSJ:grc :: L&S:lat :: B-T:ang :: **MW:san**. License verbatim from `mwheader.xml` INSIDE the zip ("Copyright © 2014 The Sanskrit Library and Thomas Malten … Creative Commons Attribution Non-Commercial Share Alike … credit is explicitly acknowledged exactly as described herein") → `nc`, the GRETIL class; CDSL licenses PER DICTIONARY — verdict covers MW 1899 only; upstream ACTIVELY corrected (zip Last-Modified 2026-07) → `sync_policy: manual`, never described as frozen. FileFetch of the zip (sha pin, attic + guard); parse streams the `xml/mw.xml` member (`unzip -p`). **SLP1→IAST at the boundary** (`Nabu::Slp1`, deterministic both directions; the betacode precedent) — the generic §9 fold then joins MW↔GRETIL (fold("aṃśa") = "amsa"), NO fold-rule change. Entry ids = stable Cologne `<L>` (urn `urn:nabu:dict:mw:<L>`); the `<info>` apparatus decodes into a `grammar:` body line (gender incl. feminine stems, verb class-pada, Westergaard/Whitney refs). **Citations land in full**: curated `MwSigla` map (24 GRETIL-held works, filenames verified against the mirror listing, + 11 authority labels) — passage grain for RV./BhP./R./Ragh./Yājñ./Kum./Sāh./MārkP./VP./Daś. (roman→arabic + per-work pad templates; "RV. v, 86, 5" → `urn:nabu:gretil:sa_Rgveda-edAufrecht:5.086.05a`, the survey's END-TO-END verified resolution — Define probes pada suffixes a–d at query time), document grain for Mn./Pāṇ. (single-blob parses), Hariv. (numbering mismatch) and the drama/prose works, authority labels rendered as labels, ~45–48% honestly **not held** (MBh., Vedic prose, Suśruta — a GRETIL-coverage fact, reported, never faked); **per-siglum coverage printed at every sync** (`Mw.citation_coverage` — the survey's projections as verifiable output). **Cognate notes → `dictionary_reflexes`** (zero schema change; ≈3.9k comparanda expected corpus-wide): Gk./Lat./Goth./Lith./Angl.Sax./Zd.(→ae)/Eng./Germ./Russ./Armen. mapped to catalog tags, Slav./Hib. kept display-only, register markers (Ved., ep.) filtered — MW enters `etym`/`cognates` as a SECOND, 19th-century witness beside kaikki, provenance-distinct. Registry `mw`, `enabled: false`, awaiting the owner-fired first real sync (11 MB GET → ~100–130 MB catalog). v2 deferrals: ibid-propagation (+10.4k), Devanagari display forms, See-ref/phwparent links graph, root families, full 871-sigla key. See `.docs/surveys/mw-survey.md`, `test/fixtures/mw/README.md`. |
| 51 | **EDH — Epigraphic Database Heidelberg** | 82,450 Latin inscriptions (81,975 with text, ~447k citable lines): epitaphs, votives, milestones, diplomas across 103 Roman provinces — the THIRD documentary shelf beside the papyri and the tablets | **EpiDoc TEI** (nine flat dump zips, 153.8 MB, frozen 2021-12-16) + two CSV sidecars (`edh_data_text.csv` 57 MB — the per-record language `nl_text`, diplomatic `btext`, `tm_nr`, facet codes, dating, coordinates; `edh_data_pers.csv` 8.9 MB — the 93,646-row structured prosopography) → new `EdhEpidocParser` family (DdbdpParser-adjacent, the SMALLER Leiden subset: expan/supplied/gap/del and nothing else) | 4 | 4 | 4 | 5 (**CC BY-SA 4.0**, per-file `<licence>` + `/data` page → `attribution`) | **Status: LIVE (P17-2; synced + flipped 2026-07-13 — 81,856 docs / 406,281 passages; P23-3c 2026-07-15: the 26 fully-lost-inscription quarantines land as whole-inscription `:text` passages at the owner's `--parse-only` resync; hd059778 stays the one honest malformed-XML quarantine).** Survey: `.docs/surveys/edh-survey.md` (fixture plan owner-approved 2026-07-13). Upstream ARCHIVED (funding closed 2021, staff gone, `/data/api` already 404s) → `sync_policy: frozen` — the preservation argument. urn:nabu:edh:hd<number> (HD numbers, the aggregator-stable id; the dumps' own `<idno type="URI">` points at a STAGING host and is never trusted). Line-grain passages, textpart urn segments with per-textpart line restarts (HD000082, the bilingual Homer herm); per-document language from the CSV `nl_text` — the `<langUsage>` header is BOILERPLATE (the verified trap: Greek editions declare only en/de/lat); per-passage grc by script for the GL bilinguals. **Policy divergence, per-source:** `<del rend="erasure">` (damnatio memoriae — legible, edited, printed `[[…]]` by EDH itself) is KEPT wrapped `⟦…⟧` + `"cancelled"` annotation — the direction conventions §5's future-work note points, adopted where no frozen urns exist. ~475 text-less metadata-only stubs skip by rule at discover (ORACC precedent), counted. **What it feeds:** `document_axes` +60,474 dated records (signed years, no year 0, open-ended honest — AxisBuilder::EdhDates; findspot + Pleiades/GeoNames refs) — the largest axis feed since HGV; the NEW `document_facets` table (migration 009: genre/province/material/object_type, EAGLE terms as values, raw codes with `?`-certainty preserved → `search --type/--province/--material`, composing with the date filters and `--fuzzy`); persons/tm_nr/literature/verse-flag ride in `documents.metadata_json` (the §3.5 prosopography seed — 93,646 gold person records; persons TABLE + attestation query = v2). `fuzzy_index: true` (the P16-4 comment's promised one-config-line join; +~70 MB projected). NOT taken (rights-clean, recorded): photos/facsimiles (HeidIcon, separate rights), CIDOC photo dump; deferred v2: coordinates/geo layer, `btext` diplomatic annotation, PIR/EDH-URI links-journal edges, GODOT day-precision dates. Fixtures: HD000001 (3-person epitaph), HD000082 (bilingual + damnatio), HD080825 (expan/supplied/gap + `lb n="0"`), HD029093/HD081183 (fully-lost inscriptions — the whole-inscription fallback exemplars, P23-3c) + both CSVs trimmed (`test/fixtures/edh/README.md`). |
| 50 | **Wiktionary reconstructions (kaikki.org / wiktextract): Proto-Slavic, PIE, Proto-Germanic** | The reconstruction shelf: ~5,195 Proto-Slavic + ~1,781 PIE + ~5,552 Proto-Germanic reconstructed headwords (senses, etymology, forms) PLUS the machine-readable `descendants` trees — reconstruction→reflex edges naming the catalog's own languages (cu/orv/sl; got/ang/non; grc/la/sa/xcl) | **JSONL** (the same wiktextract shape as #46, plus `descendants` on ~89% of records + `original_title` "Reconstruction:…"); parser family `wiktionary-jsonl` with `reflexes: true`, `content_kind :dictionary` | 3 | 3 | 4 | 4 (CC-BY-SA + GFDL) | **Status: READY (P14-1, 2026-07-12; `enabled: false` awaiting owner-fired first sync, ~125 MB across three GETs)** — `WiktionaryRecon` adapter shipped: ONE source, THREE dictionaries (`wiktionary-sla-pro`/`wiktionary-ine-pro`/`wiktionary-gem-pro`; three FileFetch subdirs, shared top-level attic, UD two-phase choreography, three probe targets). License verbatim (kaikki.org/dictionary/, re-verified 2026-07-12): "This data is made available under the same licenses as Wiktionary - both CC-BY-SA and GFDL" (+ Ylönen LREC 2022 citation request) → `attribution`. Same **DEPRECATION CAVEAT** as #46 (wiktextract #1178; fallback = filter the full enwiktionary extract by lang_code). Language codes `sla-pro`/`ine-pro`/`gem-pro` adopted verbatim from Wiktionary (non-ISO; conventions §4). The `descendants` trees flatten into the `dictionary_reflexes` crosswalk (migration 007, architecture §12): worded nodes only, upstream lang_code verbatim + mapped catalog language (cu→chu, la→lat, sa→san; the lone malformed "ML." → NULL), word+roman with conventions-§9 folds (leading asterisk stripped; `roman` is the script bridge — got/san/xcl gold lemmas are romanized). Resolution at QUERY time against `passage_lemmas`. Scout-measured crosswalk value (Phase A, live db): record-level 64.5% sla-pro / 64.2% ine-pro / 54.7% gem-pro of proto headwords naming a held language link to ≥1 attested gold lemma; roman rescues got 0→59%, san 0→41%, xcl 0→40%. Query surface: `define *bogъ` (asterisk convention) + **`nabu etym`** + MCP **`nabu_etym`** (seventh tool) — attested→proto→cognates with attestation counts, one bounded proto-to-proto ascent hop (богъ→\*bogъ→\*bʰeh₂g-; guþ→\*gudą→\*ǵʰutós; the царь chain crosses \*cěsařь/\*kaisaraz). Crowd-curated (Wiktionary), NOT Derksen/LIV — an honest crowd etymology layer over a private attested corpus. **P17-3 (2026-07-13): SEVEN dictionaries** — the survey (.docs/surveys/recon2-survey.md) added the four remaining served on-axis extracts as EXTRACTS rows on this same source: Proto-Balto-Slavic (`ine-bsl-pro`, 491 records, 1.7 MB — the STRUCTURAL intermediate shelf, near-zero direct gold), Proto-West Germanic (`gmw-pro`, 5,551, 49.4 MB — the OE proto desk + second intermediate), Proto-Italic (`itc-pro`, 745, 5.2 MB — 76.9% record join, lat/grc keys), Proto-Indo-Iranian (`iir-pro`, 799, 3.3 MB — san-via-roman + the flagged xcl loan layer); ~60 MB across the four owner-fired GETs, +7,586 entries. The `descendants` loan markers now mint the `borrowed` flag per edge (migration 010; raw_tags/tags `/borrow/i`), and the closure ascends multi-hop (shelf-visited walk — architecture §12 P17-3 addendum). Not served upstream (404, verified per-URL in the survey): Proto-Hellenic, Proto-Semitic (the akk synergy is also dead at naming density: akk named 1× in 611,901 live edges), Proto-Indo-Aryan, Proto-Iranian, Proto-Anatolian. **P25-2 (2026-07-17): TEN dictionaries — three ATTESTED Celtic extracts** join as EXTRACTS rows on this same source (the Celtic axis, survey `.docs/surveys/celtic-survey.md` — gitignored planning material; the wiktionary-cu attested precedent: reflex edges minted, no display asterisk, real ISO 639-3 codes adopted as themselves): **Old Irish** (`wiktionary-sga`, 6,564 records / 5,828 distinct words, 19.8 MB — DIL-derived depth: 2,093 records with descendants, 3,116 with etymology text, 1,427 naming Proto-Celtic and 1,263 naming PIE, kept verbatim in bodies — the cel-pro/ine-pro chains the future Proto-Celtic shelf will join on), **Middle Irish** (`wiktionary-mga`, 767 / 710, 1.3 MB), **Middle Welsh** (`wiktionary-wlm`, 766 / 695, 1.3 MB). The sga descendants trees reach mga/ga/gd/gv, so the shelf-visited etym walk ascends Middle Irish → Old Irish (rí → rí fixture-pinned), and the loan markers ride as usual (mga clann → en clan flagged borrowed). License statement re-verified verbatim 2026-07-17 (unchanged). ~22.4 MB across three owner-fired GETs, +8,097 entries at the next `bin/nabu sync wiktionary-recon`; the source's enabled state is unchanged. See `test/fixtures/wiktionary-recon/README.md`. |
| 52 | **IE-CoR — Indo-European Cognate Relationships database** (Heggarty, Anderson & Scarborough et al. 2023, *Science* 381; lexibank/iecor) | The cognacy MATRIX: 160 varieties × 170 Concepticon meanings, 25,731 lexemes in 4,981 member-bearing expert-curated cognate sets (2,341 singletons, included), 1,596 laryngeal-notated PIE roots + per-clade/attested root labels, 1,036 curated loan events — an independent second comparativistics witness beside the kaikki shelves, landing on TEN held gold languages at once (grc lat got chu orv sl ang san xcl hit; 2,261 measured held-pair edges; .docs/surveys/pie-survey.md §1 is the survey of record) | **CLDF multi-table CSV** (cognatesets × cognates × forms × languages × parameters × loans); parser family `cldf-csv` (nabu's first CLDF source), `content_kind :dictionary` | 1 | 4 | 4 | 5 (CC BY 4.0) | **Status: READY (P18-5, 2026-07-14; `enabled: false` awaiting owner-fired first sync — 6.4 MB)** — `Iecor` adapter shipped: ONE dictionary (slug `iecor`), one entry per cognate set, headword = `Root_Form` verbatim (asterisk/?-doubt included; `Root_Form_calc` fallback), dictionary language = the ISO 639-2 collective **`ine`** (argued in the adapter header: per-clade shelves would key entry identity to the curatable Root_Language and break frozen URNs; costs — no auto-asterisk, no `*root` direct lookup — stated, mitigated by verbatim asterisks + attested-side etym/bare define). Members → `dictionary_reflexes` rows (12-variety held map keyed by upstream variety ID, incl. Slovene EM `slv`→`sl`; comma/spaced-slash multiforms split, native+roman paired; paren/trailing-hyphen fold strips; `roman` the script bridge — got 𐌷𐌰𐌹𐍂𐍄𐍉/hairto). `loans.csv` ORs `borrowed: true` into every member edge of a loan-event set (path-grain, the hlaibaz rule). Honest gaps stated in the parser header: san nom.sg-vs-stem lemma shapes, hit hyphenated stems (~0 gold either way), orv Novgorod dialect, per-judgment Doubt dropped. LANGUAGE-INFO RIDER: languages.csv → `language_notes` accretion (kind/provenance `iecor`, one note per catalog-facing code, co-coded varieties grouped, append-only latest-per-(code,kind) — the P18-4 accumulated layer's first programmatic writer; seed provenance untouched). Fetch: **Zenodo versioned record 10.5281/zenodo.13304537 (= v1.2) via ZipFetch, hard sha256 pin** (immutable artifact beats GitHub's non-byte-stable zipballs and a history-dragging GitFetch; a new release is a new DOI = owner re-pin). Projected live: 4,981 entries / ~26.3k reflex rows (~2.3k loan-flagged) / ~144 language notes. See `test/fixtures/iecor/README.md`. |
| 19 | **Corpus Cyrillo-Methodianum Helsingiense (CCMH)** | 7 OCS canonical texts: Assemanianus, Marianus, Suprasliensis, Zographensis, Savvina kniga + Vitae Constantini/Methodii | Corpus's own CES XML (cesDoc v4; gospels only) + 7-bit-ASCII transliteration txt; plain HTTPS file tree | 3 | 4 | 3 | 5 (**CC BY 4.0**) | **Status: READY (P13-2, 2026-07-11)** — adapter + `ccmh-ces` family shipped, `enabled: false` pending the owner-fired first real sync. License resolved: the catalogue's "Open" = **CC BY 4.0**, verbatim bundle `README.txt` "Licence: CC-BY (creativecommons.org/licenses/by/4.0)" → `attribution`. **v1 scope (owner-approved 2026-07-11): the four gospel manuscripts** — the only ones upstream encodes as XML — one doc per (ms, gospel), `urn:nabu:ccmh:<ms>:<book>:<ch>.<verse>`, duplicate verse ids `:b2`-suffixed. **Canon gaps closed: Codex Assemanianus + Savvina kniga** (absent from TOROT/PROIEL). **Alt-edition notes (never dedupe, conventions §3):** CCMH Marianus vs PROIEL `urn:nabu:proiel:marianus` and CCMH Zographensis vs TOROT's Zographensis are distinct editions (Helsinki transliterations of the print editions vs treebank text) — separate documents by design. **Deferred (txt-only, no upstream XML): Suprasliensis + Vitae Constantini/Methodii** — prose/folio 7-digit schemes needing a future `ccmh-txt` family; TOROT holds a Suprasliensis and the richer obdurodon edition is row 30. See `.docs/surveys/slavic-survey.md`, `test/fixtures/ccmh/README.md`. |
| 19 | **Corpus Cyrillo-Methodianum Helsingiense (CCMH)** | 7 OCS canonical texts: Assemanianus, Marianus, Suprasliensis, Zographensis, Savvina kniga + Vitae Constantini/Methodii | Corpus's own CES XML (cesDoc v4; gospels only) + 7-bit-ASCII transliteration txt; plain HTTPS file tree | 3 | 4 | 3 | 5 (**CC BY 4.0**) | **Status: READY (P13-2, 2026-07-11)** — adapter + `ccmh-ces` family shipped, `enabled: false` pending the owner-fired first real sync. License resolved: the catalogue's "Open" = **CC BY 4.0**, verbatim bundle `README.txt` "Licence: CC-BY (creativecommons.org/licenses/by/4.0)" → `attribution`. **v1 scope (owner-approved 2026-07-11): the four gospel manuscripts** — the only ones upstream encodes as XML — one doc per (ms, gospel), `urn:nabu:ccmh:<ms>:<book>:<ch>.<verse>`, duplicate verse ids `:b2`-suffixed. **Canon gaps closed: Codex Assemanianus + Savvina kniga** (absent from TOROT/PROIEL). **Alt-edition notes (never dedupe, conventions §3):** CCMH Marianus vs PROIEL `urn:nabu:proiel:marianus` and CCMH Zographensis vs TOROT's Zographensis are distinct editions (Helsinki transliterations of the print editions vs treebank text) — separate documents by design. **P14-5 (2026-07-12, shipped): the txt half — full 7-text corpus.** Second family `ccmh-txt` (`CcmhTxtParser`, same source/fetch): **Codex Suprasliensis** at diplomatic **folio-line grain** (`urn:nabu:ccmh:suprasliensis:<part>.<folium>.<side>.<line>`, the file's own Severjanov line codes; ~17k lines, 51% hyphen-wrapped mid-word) + **Vitae Constantini/Methodii** at **chapter-verse grain** (`urn:nabu:ccmh:vita-constantini:<ch>.<verse>`, upstream's "line number (in this file only)" digit never cited; both NEW to the holdings). **Split-word mechanics (owner req.):** pristine text keeps the diplomatic line verbatim; `text_normalized` folds the REJOINED derivation (`hyphen_join` annotation — tail appended, orphan dropped; conventions §9), so FTS/`--near`/golden queries see whole words and `concord` highlights the visible fragment honestly. Upstream slips carried verbatim: side-digit-3 codes, duplicate line codes (`:b2`), non-adjacent duplicate verse (`VC 11.10:b2`). **Suprasliensis alt-edition discipline: THREE distinct editions, never dedupe any pair** — TOROT's (treebank text), CCMH's (Helsinki 7-bit transliteration of Severjanov), and the queued obdurodon critical edition (row 30). **P14-2 (alignment hub, architecture §10):** all four manuscripts wired into the `nt` work as `documents:` cts-verse witnesses (labels `CCMH Assemanianus/Marianus/Savvina/Zographensis`, distinct from PROIEL `marianus`), taking `align "MARK 2.3"` to 13 witnesses (the alt-edition showcase: PROIEL Marianus Cyrillic beside CCMH Marianus transliteration). CHAPTER-0 kephalaia/incipit apparatus is excluded at the extractor (index-side only; still addressable via `show`/`search`); `:b2` verse duplicates fold to a distinct `.B2` ref and never false-align. Config-only + one extractor guard; the alignment index picks the CCMH witnesses up at the owner's next `nabu sync ccmh`/`nabu rebuild`. See `.docs/surveys/slavic-survey.md`, `test/fixtures/ccmh/README.md`. |
| 20 | **e-codices / e-manuscripta (Switzerland)** | Manuscript facsimiles incl. classical & medieval | IIIF images + metadata | 4 | 3 | 3 | 4–5 (per-MS CC) | Not text — images. Feeds the ad-hoc/HTR pipeline as a *bulk image adapter*. Swiss infrastructure, excellent IIIF. |
| 21 | **Menota** | Medieval Nordic manuscripts, multi-level transcription | TEI (Menota schema) | 3 | 3 | 3 | 3–4 (per-text) | If Old Norse enters scope; schema is well documented. P11-1: confirmed **no Old English** (front page: Old Icelandic/Norwegian + some Swedish/Danish/Latin only). |
| 22 | **Avestan Digital Archive** | Avestan manuscripts | Images + some transcriptions | 2 | 4 | 2 | 3 | Mostly facsimiles → HTR pipeline candidate. |
| 30 | **Codex Suprasliensis — obdurodon critical edition** (Miltenova & Birnbaum) | Largest OCS ms (saints' lives + homilies), diplomatic transcription + parallel Greek + English + glossary | XML-backed (RelaxNG; per-text "Raw XML"), **no located bulk repo** → per-text HTTP crawl | 3 | 5 | 2 | 3 (**CC BY-NC-SA 3.0** → `nc`) | **Status: SURVEYED (P9-6, 2026-07-08).** `suprasliensis.obdurodon.org`. Richest single OCS edition online; license quoted "Creative Commons BY-NC-SA 3.0 Unported" → `nc` (local-ingestable, MCP-excluded). Citation text·folio·side·line (`1 008r 18`) → `urn:nabu:suprasliensis:<text>:<folio><side>.<line>`. Fuller *alternative edition* of TOROT's Suprasliensis (not a dedupe). Behind #1/#2: `nc` + website crawl friction. See `.docs/surveys/slavic-survey.md`. **P13-1 re-check (2026-07-11): UNCHANGED** — license still verbatim "Creative Commons BY-NC-SA 3.0 Unported License"; still no bulk repo (`gh api`: no Suprasliensis data repo under obdurodon; only the third-party unlicensed `StabiBerlin/Stanza-NLP-Supr` harvest, which at least proves the per-folio "Raw XML" crawl works). **P14-5 note:** CCMH's Suprasliensis (row 19, Severjanov transliteration at folio-line grain) is now READY — a SECOND held edition beside TOROT's; obdurodon would be the third. Distinct editions all, never dedupe any pair (conventions §3). |
| 34 | **ISWOC Treebank** (Bech & Eide, Oslo) | 5 Old English texts, ~29,406 tokens: Ælfric's Lives of Saints, Apollonius of Tyre, Anglo-Saxon Chronicles, Orosius, **West-Saxon Gospels (= Gospel of Mark, verse-cited)** — plus 10 medieval Romance texts we'd filter out | **PROIEL XML 2.1** (same `proiel.xsd` as proiel/torot) + CoNLL-X, git, flat repo root | 2 | 5 | 5 | 3 (**CC BY-NC-SA 3.0** → `nc`) | **Status: READY (P12-1, 2026-07-10) — adapter shipped, `enabled: false` until the owner-fired first sync.** `Iswoc < Proiel` TOROT-pattern subclass + `ang` language filter (source headers carry `language=`; the 10 Romance texts are excluded by peeked metadata, never filename). License re-verified at fixture time — README verbatim: "freely available under a Creative Commons Attribution-NonCommercial-ShareAlike 3.0 License"; per-source headers agree (`wscp.xml`: `<license>CC BY-NC-SA 3.0</license>`). Repo verdict (Phase A, owner-approved): pinned the ORIGINAL `iswoc/iswoc-treebank` — GitHub-**archived** at final commit `574c81c` (2023-05-02) → `sync_policy: frozen` (proiel precedent); the successor `syntacticus/syntacticus-treebank-data` HEAD *predates* it and bundles already-synced `proiel/`/`torot/` (if ever adopted, **scope to `iswoc/` subdir**). urn: inherit `urn:nabu:proiel:<source-id>` (torot precedent; non-ASCII id `æls` preserved NFC). Fixtures: `test/fixtures/iswoc/` (wscp Matthew-7+Mark-1–2 divs, æls 20 sentences, eustace `fro` exclusion probe). `citation-part="MARK 1.1"` verse refs feed the P11-3 hub — the `urn:nabu:proiel:wscp` witness line is live in `config/alignments.yml` and renders "not synced" honestly until first sync. Gold lemma+morph+syntax → lemma index for OE. See `.docs/surveys/oe-survey.md`. |
| 35 | **ASPR — Anglo-Saxon Poetic Records** (OTA 3009, Hidley/Macrae-Gibson e-text of Krapp & Dobbie) | The complete OE poetry corpus, all 6 volumes: Beowulf, Junius MS, Vercelli Book, Exeter Book, Paris Psalter, Minor Poems (Cædmon's Hymn in Northumbrian AND West-Saxon versions) — 349 poems / ~30.5k verse lines (the survey's "374 texts" counted `<head>` elements; 25 are duplicate second heads within single poems) | **TEI P5, NOT EpiDoc/CTS** (no refsDecl; 349 flat `div[@rend=linenumber][@xml:id] → head + bibl? + l*`, `<caesura/>`, `<unclear>`, `<foreign rune>`, `<gap/>`, `<g>`; **no `l/@n`**, but the per-div `<l>` ordinal EQUALS the printed ASPR line number — Beowulf's div is exactly 3,182 `<l>`), single 2.2 MB XML over HTTP | 3 | 5 | 3 | 5 (**CC BY-SA 3.0** → `attribution`) | **Status: READY (P12-2, 2026-07-10; `aspr`, `enabled: false` awaiting owner-fired first sync).** Adapter `Nabu::Adapters::Aspr` + new (seventh) parser family `AsprParser` (streaming Reader, Vulgate one-file-many-docs shape) + the first single-file HTTP fetch path (`Nabu::FileFetch`, ZipFetch's sibling: If-Modified-Since state pin, sha256, attic contract). One document per poem div, urn = `urn:nabu:aspr:<Cameron-number>` — the div `xml:id` IS the canonical Cameron/DOE record id (A4.1 Beowulf, A32.1/A32.2 the Cædmon's Hymn dialect witnesses); title-slugs rejected (A43.5 vs A43.10 are BOTH "For Loss of Cattle"). Passages `<doc>:<line-ordinal>` (canonical, not synthetic — see Format). License verbatim from the TEI header itself: "Distributed by the University of Oxford under a Creative Commons Attribution-ShareAlike 3.0 Unported License" (`ota.bodleian.ox.ac.uk/repository/xmlui/handle/20.500.12024/3009`, bitstream fetched without auth; re-verified 2026-07-10). The only fully-open structured OE text source found — MCP-safe. Upstream effectively frozen (Last-Modified 2019-07-19) → `sync_policy: manual`. `health --remote` HEADs the bitstream (drift via the `.file-fetch.json` pin); license row honestly `unchecked` (no metadata endpoint — the license lives in-file). Fixture: 8-div trim incl. Beowulf 1–24. See `.docs/surveys/oe-survey.md` + backlog P12-2 findings. |
| 36 | **Bosworth-Toller Anglo-Saxon Dictionary — LINDAT data dump** (Tichý, Charles Univ. Prague) | The canonical OE dictionary (B-T + Supplement), complete entry set, lemma-keyed | SQL dump + CSV (`id;headword;body`, body in project XML — NOT TEI); entry `id` resolves to `bosworthtoller.com/<id>` | 3 | 4 | 4 | 5 (**CC BY 4.0** → `attribution`) | **Status: READY (P12-3, 2026-07-10)** — `BosworthToller` adapter shipped: the third dictionary-shelf occupant (`content_kind :dictionary`), first CSV dictionary (parser family `bosworth-csv`), FileFetch single-file path (DSpace bitstream `/content` URL — the repo moved to DSpace 7; old xmlui URLs serve the Angular shell, not files). CSV reality: all fields quoted, `;` separator, CRLF record ends, multi-line project-XML bodies, ids gap. Registry `bosworth-toller`, `enabled: false`, awaiting the owner-fired first real sync (~84 MB, one GET). `define --lang ang` + the conventions §9 ang fold (æ→ae, þ/ð→th) make it ASCII-typeable (`define aethele` → æðele); citations start empty (no OE crosswalk yet). License verbatim from the record metadata: dc.rights = "Creative Commons - Attribution 4.0 International (CC BY 4.0)" (hdl 11234/1-3532, "Data dump version 0.1", 2021). Record caveat verbatim: "The data is still being processed for accuracy … Not all entries have been checked and/or tagged" — the parser tolerates untagged bodies (fixture: 249/270 sense-less). Fallback: the older Germanic Lexicon Project digitization (effectively PD, superseded). See `.docs/surveys/oe-survey.md`. |
| 37 | **YCOE + YCOEP** (York-Toronto-Helsinki Parsed Corpora of OE Prose / Poetry) | ~1.5M words OE prose (Ælfric, Wulfstan, Bede, Boethius, Orosius, four WS Gospels, 5 Chronicle MSS, laws, homilies, medical) + 71,490 words poetry | **Penn-Helsinki labeled bracketing** (`.psd` parsed + `.pos` tagged) → **NEW parser family** | 4 | 5 | 2 | 2 (OTA "ACA — Academic Use": noncommercial, attribution, layered copyright) | **Status: SURVEYED (P11-1, 2026-07-09) — possible later, behind #34/#35.** OTA 2462 (YCOE, 295 files ~66.7 MB) / 2425 (YCOEP), downloadable after the OTA agreement. Terms verbatim: "Use and make personal copies … only for purposes of non-commercial research or teaching"; York homepage: "many of the texts in the corpus are subject to copyright restrictions" / annotations freely reproducible "in the course of non-commercial scholarly activity"; YCOEP: "available without fee for educational and research purposes, but it is not in the public domain." No redistribution grant on the text layer → posture `nc`…`research_private`, decide at ingest. Contains the **full four WS Gospels** (`cowsgosp.o3`, verse coords embedded in DOEC-style token ids) — the tetraevangelion path. Toronto e-text lineage overlaps ISWOC (distinct editions, never dedupe). Helsinki Corpus OE slice (OTA 1477) dominated by YCOE. See `.docs/surveys/oe-survey.md`. |
| 51 | **Coptic Scriptorium (CopticScriptorium/corpora)** | The literary Coptic canon with gold-to-automatic annotation: 77 corpora / ~2,390 docs / ~2.38M words — complete Sahidic + Bohairic NT/OT, Shenoute, Apophthegmata Patrum, Besa, hagiography, Pistis Sophia, documentary/magical papyri; lemma+POS+deprel, entities with Wikification, LANGUAGE-OF-ORIGIN loanword tags (8.6% of NT tokens — the language-contact layer), per-verse WEB/Brenton English, MS dates/places/repositories | **TreeTagger-SGML `.tt`** (upstream's "most complete representations"; overlapping span stacks, NOT XML → new bespoke family `coptic-tt`); TEI absent for the two biggest corpora; CoNLL-U deferred (v2 FEATS join); git, semiannual tagged releases + Zenodo | 4 | 5 | 3 | 3 (**mixed per document**: ~87% CC-BY(-SA) → attribution overrides; Sahidica NT "(c) J Warren Wells, academic use only" + BY-NC-SA quartet → source class **nc**, P10-4 inverted; 3 license-less docs skipped by rule) | **Status: READY (P17-1, 2026-07-13; `enabled: false` awaiting the owner-fired first sync — ~2.8 GB clone PINNED to release tag v6.2.0 via `GitFetch ref:`, never master).** Adapter `Nabu::Adapters::CopticScriptorium` + parser family `CopticTtParser` (three structural dialects fixture-verified, incl. the COLLAPSED one the survey's 8 samples missed; unknown span types fail loudly). One document per TT file; bible CHAPTER files merged to per-book documents (shared CTS work id; single-chapter Philemon edge case covered); in-repo TT zips read via `unzip` through `Nabu::Shell`, canonical never written outside fetch; `coptic-treebank`/`bohairic-treebank` duplicate collections excluded by rule; `sahidica.nt`'s 2-byte CoNLL-U placeholders never parsed. urn `urn:nabu:coptic-scriptorium:<cts-tail>:<citation>` (upstream-minted identity). Passage = verse/translation unit; text = DIPLOMATIC (strokes kept); `text_normalized` from the upstream **norm** layer at word grain through the ONE folding boundary (+ new conventions §9 `cop` fold: ⳿ U+2CFF deletes; strokes are Mn, already stripped). Gold+checked docs (366) feed `passage_lemmas` — **gold-lemma language #15** (`lemmas: :all` is the owner's include-automatic flip); loanword tags ride per-token + as per-passage `loans` code counts (a future `--loans` facet reads them without reparse); entities/identities per passage, people/places rosters unioned per document; MS dates (234 docs) + places → `document_axes` via the `CopticScriptoriumDates` extractor. Hub: **witness #14 sahidica NT (nc labels) + #15 Bohairic NT (attribution)**, 27 books each, urns verified in the tagged meta.json; OT witnesses deferred to v2 (upstream's own versification caveat — spot-check Psalms/Jeremiah first). v2 named: `--loans` facet + Coptic→Greek borrowing crosswalk, witness/identity links-journal producers, CoNLL-U FEATS join, Arabic translations (ANNIS-blocked upstream — though AP fixtures carry embedded `<arabic>` spans, ingested where present), CDO lexicon, sbl_greek collation. See `.docs/surveys/coptic-survey.md`, `test/fixtures/coptic-scriptorium/README.md`. |
| 52 | **LIV — Lexikon der indogermanischen Verben (Rix 2001) as LiLa LOD** (CIRCSE/LIV) | The reference PIE VERB inventory, Rix-school laryngeal notation: 305 reconstructed verbal roots → 426 stem-typed themes (present/aorist/perfect/causative/desiderative/essive… — a verbal-stem-type annotation axis NO kaikki shelf carries) → 385 Latin lexical entries / 340 writtenReps (suffio, pinso, parco, uireo…). Latin slice ONLY (print LIV lists 550 Latin word forms; the Germanic/Greek/etc. reflex columns are not included) | **Turtle** (657 KB, lemonEty/Ontolex) → new parser family `lila-ttl` — a censused-subset in-house triple reader (~160 lines; the rdf-turtle gem would drag the whole rdf family through the CLAUDE.md gem bar for two small regular files), `content_kind :dictionary` | 3 | 4 | 5 | 5 (**CC BY-SA 4.0 with publisher permission**) | **Status: READY (P18-6, 2026-07-14; `enabled: false` awaiting the owner-fired first sync — 657 KB GET).** License verbatim from the repo README: *“The publisher of the dictionary allowed us to model and publish the etymological relations between PIE roots, stems and Latin word forms contained in the data.”* + CC BY-SA 4.0 badge → `attribution`. `Liv` adapter: ONE dictionary (slug `liv`, language `ine-pro`) — one entry per etymon (headword = the laryngeal root, `{n}` homonym markers and leading homonym indices cleaned into key_raw; ~40-digit upstream etymon ids adopted verbatim as entry ids), Latin continuations as `dictionary_reflexes` rows (lang_code/language `lat`) joining gold lat through the §9 **u/v fold** (uireo ↔ vireo — the survey pin, fixture-tested). **Stem-type surface verdict:** entry BODY lines — `present stem *dʰu̯éh₂-/dʰuh₂- → pres suffio` — rendered by `define` with zero schema/query change (not gloss: the LOD ships relations, no meanings — nil gloss is honest; no new table for a one-source 426-row layer). Single-file `FileFetch` of the raw URL (no Last-Modified upstream — manual re-syncs refetch 657 KB unconditionally); completed publication → `sync_policy: manual`. Language-notes rider: `witness:liv` note on ine-pro, provenance `liv`, accreted idempotently at load. Credit Rix et al.; Boano/Passarotti/Mambrini/Ginevra (CLiC-it 2023). See `test/fixtures/liv/README.md`. |
| 53 | **de Vaan, Etymological Dictionary of Latin — LiLa LOD skeleton** (CIRCSE/EtymologicalDictionaryLatin) | The Leiden-school (IEED) etymology SKELETON of de Vaan 2008: 1,429 Latin headwords staged through **1,466 Proto-Italic + 1,394 PIE etymons** (2,653 inheritance edges: pie→pit 1,216 · pit→lat 1,410 · pie→lat 27 direct), linked to LiLa lemma URIs; dictionary ENTRIES omitted (Brill copyright) | **Turtle** (3.9 MB, lemonEty/Ontolex, Jena serialization with blank-node canonicalForms + U+2011 non-breaking hyphens) → same `lila-ttl` family, `content_kind :dictionary` | 3 | 4 | 4 | 3 (**CC BY-NC-SA 4.0** → `nc`) | **Status: READY (P18-6, 2026-07-14; `enabled: false` awaiting the owner-fired first sync — 3.9 MB GET).** License verbatim from the repo README: de Vaan’s EDL *“is copyrighted by Brill… The dictionary entries are not represented”*; repo LICENSE CC BY-NC-SA 4.0 → `nc` (the GRETIL/MW posture: local research + index, MCP default-excluded, never redistributed). `Edl` adapter: ONE source, **TWO dictionaries from one file** (the wiktionary-recon precedent) — `edl-ine-pro` (PIE etymons; reflexes = the PIt etymons they stage to, asterisk kept, + the 27 direct lat edges) and `edl-itc-pro` (PIt etymons; reflexes = Latin headwords) — so the existing shelf-visited `etym` walk runs the full Leiden chain lat → PIt → PIE (rōdō ← \*(w)rōde/o‑ ← \*Hreh₃d‑e/o‑) with zero new query code, and the PIt shelf stands **beside kaikki’s itc-pro as a provenance-distinct second witness** (the MW-beside-kaikki precedent; P18-3 audit: cross-entry same-word = two honest witnesses). U+2011 kept in display headwords, opened to ASCII “-” inside the fold so typed lookups land; `etyLinkType` all “inheritance” (censused) with a /borrow/i guard for future loan links. Single-file `FileFetch`; completed dataset → `sync_policy: manual`. Language-notes rider: `witness:edl` notes on itc-pro + lat, provenance `edl`. Cite Mambrini & Passarotti 2020. See `test/fixtures/edl/README.md`. |
| 56 | **StarLing / Tower of Babel — Indo-European etymological databases** (starlingdb.org, `IE.exe` package) | The Moscow-school Pokorny family, complete — five shelves since P23-0: **`starling-pokorny`** — Pokorny's *Indogermanisches Etymologisches Wörterbuch*, all **2,222 roots** with full Material/References/IEW-page apparatus (the G. Starostin-scanned, **Lubotsky-corrected** digitization — the same text dnghu re-badged, here under an actual grant); **`starling-piet`** — S. L. Nikolayev's Walde-Pokorny-based PIE database, **3,291 etymologies** (incl. one upstream NUMBER collision, #574 twice — disambiguated `574-b`, the P23-0 verdict below) in traditional laryngeal-free notation with per-branch reflex columns (HITT/IND/AVEST/IRAN/ARM/GREEK/SLAV/BALT/GERM/LAT/ITAL/CELT/ALB/TOKH) and the pokorny⇄piet crosslink preserved both ways; **`starling-vasmer`** (rus) — M. Vasmer's etymological dictionary of Russian, Trubachev edition, **18,239 entries** (Near/Further etymology + Trubachev's + editorial comments + IEW-style page cites; the roster's stand-in for a comparative Slavic base); **`starling-germet`** (gem-pro) — the Common Germanic database, **1,994 etymologies** with per-language columns GOT…HG; **`starling-baltet`** (bat-pro) — the Baltic database, **1,651 etymologies** (OLITH/LITH/LETT/OPRUS) | **dBase III + StarLing-encoded `.var` text** (font-shift doublebyte runs, `\`-markup) → new parser family **`starling-dbf`** (`StarlingDbfParser` + the table-driven `Nabu::StarlingText` decoder; byte meanings from the vendored `config/starling/unipro.lst`, never guessed — the corpus's ONE unmapped stray decodes U+FFFD, pinned), `content_kind :dictionary` | 2 | 4 | 4 | 4 (grant) | **Status: LIVE (P22-0 + P23-0; synced + flipped by the owner, live census 2026-07-17: 27,397 entries across the five shelves — the P23-0 fix landed piet whole).** **License = the written grant**, G. Starostin e-mail 2026-07-15: *"all etymological data are free for anybody to use for any purposes as long as the source is properly acknowledged"* → `attribution`, with the EXPRESS CONDITION that attribution name the specific compilers of each database (roster: <https://starlingdb.org/descrip.php?lan=en#bases>). **His caveat rides verbatim** (the Larth treatment): the databases are *"individual reconstructions with the subjective input of their original creators, and do not always represent the most up-to-date, or the most 'consensus-approved' versions"*. **Per-base compiler credit (in `MANIFEST.license` — renders on every define/etym/MCP surface):** Pokorny base (in-package `pokorny.inf` DBINFO): *"scanned and recognized by George Starostin (Moscow), who has also added the English meanings. The database was further refurnished and corrected by A. Lubotsky."*; PIE base (`piet.inf` DBINFO + roster item 3): *"compiled on the basis of Walde-Pokorny's dictionary by S. L. Nikolayev"*, *"Hittite and Tokharian reflexes … added by S. Starostin from the dictionaries of Friedrich, Tischler and Adams"* (roster: "Compiled by Sergei Nikolayev … with Anatolian (Hittite) and Tocharian material added in by S. Nikolayev and S. Starostin"); Vasmer base (`vasmer.inf` is BLANK — the roster's actual words carry the credit): *"scanned, OCR'd, and database-converted versions of M. Vasmer's etymological dictionary of Russian (currently serving as a substitute for the comparative Slavic database)"*; Germanic base (`germet.inf` DBINFO): *"The Common Germanic database, compiled by S. Nikolayev and subordinate to the Common Indo-European database."*; Baltic base (`baltet.inf` DBINFO): *"The Baltic database, compiled by S. Nikolayev and subordinate to the Proto-Indo-European database."*. pokorny/piet `ine-pro`; vasmer `rus`; germet `gem-pro` (unifies with the wiktionary-recon Proto-Germanic shelf); baltet `bat-pro` (minted by the family-code+`-pro` convention — Wiktionary reconstructs Balto-Slavic, not Proto-Baltic; journaled). **Reflex verdict (censused, journaled P22-0):** single-language attested columns HITT/IND/AVEST/ARM/LAT/ALB mint ONE `dictionary_reflexes` row per cell — the leading citation form only, clean-token gated (~4.4k rows projected: LAT 1,386 · IND 1,335 · AVEST 652 · ARM 486 · HITT 323 · ALB 230); GREEK (Latin transcription), SLAV/BALT/GERM (Nikolayev-notation branch protoforms) and mixed IRAN/ITAL/CELT/TOKH stay body-only, every column verbatim in the body. **P23-0 reflex verdicts (censused on the full bases):** germet mints from 19 of 21 single-language columns — **14,627 rows** (GOT joins the **got** gold, OENGL the **ang** gold — attested counts live via ReflexViews; the rest speak the Wiktionary codes the kaikki crosswalk speaks: non/no/gmq-osw/sv/gmq-oda/da/enm/en/ofs/osx/dum/nl/gml/nds/goh/gmh/de), with a censused STOP_TOKENS gate for bare dialect-label leads (CrimGot/NIsl/OGutn/OWFris… — zero piet/pokorny drift, measured); EASTFRIS + OLFRANK stay body-only (variety-ambiguous label mixes). baltet mints **3,091 rows** (olt/lt/lv/prg). vasmer mints none — prose fields, body-only. **Data-defect census (whole package):** NUMBER collisions — piet ×1 (#574 twice, the second sitting where the vacant 1574 belongs: the owner's 2026-07-16 whole-file quarantine), baltet ×6 (76/95/248/689/1049/1394 — exactly piet's six dangling BALTNUM links; there the INTERLOPER comes first in file order), pokorny/vasmer/germet ×0 → first occurrence keeps the plain entry id, repeats mint a stable `-b` file-order suffix + an honest body note, never renumbered; headword-less records — piet 6 (content-bearing Iranian stubs the live CGI cannot serve), germet 6 / baltet 7 (empty numbered slots), kept under the mechanical `#NUMBER` placeholder so crosslinks at those numbers resolve. piet's SLAVNUM/BALTNUM/GERMNUM body lines now name LIVE entry ids (censused: GERMNUM 1,965/1,965, SLAVNUM 1,233/1,233, BALTNUM 1,626/1,632); body-line → clickable-link resolution needs a crosslink-rows lane (the citations pattern) — journaled follow-up, not built. vasmer's OCS runs decode through the second vendored table **`config/starling/chslav.lst`** (the official 3.9.0 `[Chslav font]` conversion; 19,229 of 19,257 otherwise-unmapped pairs; 28 upstream strays → honest U+FFFD). Language-notes rider: `witness:starling` on ine-pro, rus, gem-pro, bat-pro. ZipFetch (Last-Modified detection; hand-curated 2005-era package). See `test/fixtures/starling/README.md` + `config/starling/README.md`. |
| 57 | **Pleteršnik, Slovensko-nemški slovar 1894–95** (ZRC SAZU; CLARIN.SI hdl 11356/1114) | THE reference dictionary of older Slovenian: 103,185 entries (counted = the description exactly) — unaccented headword `<ge>` (matches goo300k's modernized gold lemmas), accented form with **Slovenian tonemes** (`abecę̑da`, `ábəł`), POS, **German glosses**, per-sense source-authority sigla (Cig., Jan., Levst.), dialect/region tags (vzhŠt., BlKr., Rez.), `<pi>` etymology zones; covers 19th-c. standard AND 16th-c.-onward lexis | flat ZRC SAZU dictionary XML (NOT TEI; one `<rc geslo-id>` per line, XSD shipped) → new parser family **`zrc-xml`**, `content_kind :dictionary`; one 4.8 MB zip (25 MB XML) | 3 | 5 | 4 | 5 (**CC BY 4.0** → `attribution`) | **Status: LIVE (P23-2; `sl-lexica` source synced + flipped by the owner, live census 2026-07-17: 139,405 entries across the three shelves).** License verbatim from the DSpace record (verified at fetch time): dc.rights = "Creative Commons - Attribution 4.0 International (CC BY 4.0)", label PUB. Dictionary slug `pletersnik`, language `sl` (headwords are modernized orthography — census verdict journaled in backlog P23-2). Display headword = the accented `<oi>`; `headword_folded` from the unaccented `<ge>` (folding the toneme form would strand ə/ł spellings), so `define abeceda` and `define abecę̑da` both land — the gold-lemma→dictionary loop closed for sl (goo300k lemma hits carry these German glosses). Journaled, not wired: dialect/geo tags as a facet, `<ov>` authority sigla as an attestation apparatus, `dodatek` flag. See `test/fixtures/sl-lexica/README.md`. |
| 58 | **Slovar jezika Janeza Svetokriškega (JSV)** (Snoj, ZRC SAZU; CLARIN.SI hdl 11356/1092) | Baroque Slovenian: the lexis of the 233 sermons of *Sacrum promptuarium* (1691–1707) — 8,461 entries counted (the description says 8,540; delta reported honestly): modernized headword, grammar, modern-Slovenian gloss, **verbatim Bohorič-orthography attestation quotes** with volume/page citations (`(I/1, 207)`), **loanword etymologies** (`← it. a … < lat. ad`) | same `zrc-xml` family / `sl-lexica` source (one 1.8 MB zip) | 2 | 4 | 4 | 5 (**CC BY 4.0** → `attribution`) | **Status: LIVE (P23-2; synced with the sl-lexica source 2026-07-17)** — dictionary slug `jsv`, language `sl`. Every `<ct>` mints an UNRESOLVED DictionaryCitation (urn_raw/label verbatim, `cts_work` nil — no urn invented; `citation` = the parsed `I/1.207` vol/page pair, the `s.` and-following suffix tolerated, upstream typos parse to nil): resolution against an IMP *Sacrum promptuarium* crosswalk is journaled future work. Loanword `<op>` etymologies ride the body (a dictionary-native language-contact layer for a future P15-3 pass). |
| 59 | **Besedje slovenskega knjižnega jezika 16. stoletja (besedje16)** (Ahačič et al., ZRC SAZU; CLARIN.SI hdl 11356/1127) | The complete word inventory of **1550–1603 Slovenian print**: 27,759 entries with POS and **per-word attestation sigla of the editions** (TA 1550, TT 1557, DB 1584 = Dalmatin's Biblia — the very document goo300k/IMP hold as `zrc_00001-1584`), earliest-attestation data at word grain | same `zrc-xml` family / `sl-lexica` source (one 0.4 MB zip; CRLF file — preserved in fixtures) | 2 | 4 | 4 | 5 (**CC BY 4.0** → `attribution`) | **Status: LIVE (P23-2; synced with the sl-lexica source 2026-07-17)** — dictionary slug `besedje16`, language `sl` with the period carried in the title + the sl language note (a period subtag would fracture the define/gloss joins; census verdict journaled). Sigla ride the body verbatim; the mechanical sigla→goo300k/IMP document crosswalk and the `zvezdica`/`hom` attributes are journaled future layers. The Franček crosswalk deposit (11356/1472) was NOT ingested — outside the packet's three-artifact scope, still on the survey's rider list. |
| 54 | **Language dossiers — the local-language shelf** (canonical/local-language, P19-1) | Nabu's OWN canonical memory: one Markdown + YAML-front-matter dossier per language code — the owner's curated name/family/context prose (rehomed from `config/languages.yml` + the ledger's `language_notes`, both retired by the canonical-memory migration) plus programmatic accretion sections with provenance headers (IE-CoR variety notes, LIV/EDL stage witnesses) | Markdown + YAML front matter; parser family `language-dossier`, `content_kind :language` → `language_records` (derived, migration 014) | 1 | 4 | 5 | 5 (owner-authored, class `open`) | **Status: LIVE-LOCAL (P19-1, 2026-07-14; `enabled: true` by default — argued in sources.yml: no network, no license risk, rebuild replays regardless).** The FIRST `sync_policy: local` source (architecture §16): no upstream ever; `fetch` = `LocalFetch` re-scan (per-file sha256 ledger pins keyed `local:<file>`; un-atticked disappearance reads LOUD at health; deliberate retire = move into `.attic/`); drift probe short-circuits to `local`; writes reach canonical ONLY through `Nabu::LanguageShelf` (the local shelf's Adapter#fetch analogue). Populated by the owner-fired one-shot `nabu language --export-dossiers` + `bin/nabu sync local-language`; `nabu language CODE` reads the merged derived view unchanged. |
| 55 | **Local library — the owner's own shelf** (canonical/local-library, P19-4) | PDFs, scans and scholarly articles the owner acquires: one `<collection>/` dir per drop, `manifest.yml` per collection as the SOURCE OF RECORD (file/title/creator/year/languages/provenance/license_class/tags/related — a YAML list shaped for mechanical `nabu ingest` appends). PDFs with a text layer → PAGE-GRAIN passages (`…:p12`, mutool through `Nabu::Shell`); textless scans/images → metadata-only documents (`text_layer: none`, queued for the HTR era — improvements §3.4 — never quarantined for being scans); txt/md → paragraph passages. Manifest `related:` urns → kind=reference edges in the links journal, so `nabu links` shows an article beside the passages it discusses | Owner files + YAML collection manifests; parser family `library-manifest`, `content_kind :passages` (article-ness is document metadata `kind: article`, not a loader routing) | n/a | 4 | 5 | **Per-item, DEFAULT `research_private`** (MCP default-excluded, never redistributed — acquired scholarly PDFs are mostly copyrighted; an entry's explicit more-open class is honored as a per-document `license_override`) | **Status: LIVE-LOCAL (P19-4, 2026-07-14; `enabled: true` by default — the P19-1 argument verbatim).** The SECOND `sync_policy: local` shelf (architecture §16): `fetch` = `LocalFetch` re-scan (per-file sha pins, strays and manifests included); manifested-but-missing = the vanished/attic story (pin lingers, health shouts; atticked = retired, passages persist); file-present-but-unmanifested = UNRECOGNIZED census (awaiting ingest). Full conformance suite (unlike the dossier shelf, this one mints documents+passages; metadata-only documents pass via the marker-driven hook). Reference edges refresh at every sync (producer `library`, superseding). Populated by hand until `nabu ingest` (next packet) becomes the sanctioned front door. |
| 61 | **Owner notes — the local-notes shelf** (canonical/local-notes, P24-1) | Nabu's THIRD canonical-memory shelf: the owner's curatorial notes keyed by ANY urn the corpus knows (documents, passages, ranges, dictionary entries — scholia of one's own), one YAML list of note records (`urn`/`note`/`added`/`tags`) per `<topic>.yml` file, grouping at the owner's whim (default topic `notes`) | YAML note lists; parser family `urn-notes`, `content_kind :notes` → `urn_notes` (derived, migration 015) | 1 | 4 | 5 | 5 (owner-authored, class `open`; render/MCP withholding follows each note's TARGET document) | **Status: LIVE-LOCAL (P24-1, 2026-07-16; `enabled: true` by default — the P19-1 argument verbatim).** The THIRD `sync_policy: local` source (architecture §16): no upstream ever; `fetch` = `LocalFetch` re-scan (per-file sha256 ledger pins keyed `local:<file>`; un-atticked disappearance reads LOUD at health; deliberate retire = move into `.attic/`); drift probe short-circuits to `local`; writes reach canonical ONLY through `Nabu::NoteShelf` (`nabu note URN [TEXT]` — the urn must RESOLVE in the catalog; `--force` records a deliberately dangling note on planned material). Renders on `show`/`define`/`links` footers and `nabu note --list`; MCP serves notes BY DEFAULT on nabu_show/nabu_define, withheld with a withheld target. |
| 62 | **Source dossiers — the local-source shelf** (canonical/local-source, P24-0) | Nabu's canonical memory at the SOURCE grain: one Markdown + YAML-front-matter dossier per registered source — the owner's curated `description` (the load-bearing 1–3 sentence content description, served on `nabu list` cards/census and MCP `nabu_status`), `themes`, `key_works` urns, free prose, plus provenance-headed accretion sections | Markdown + YAML front matter; parser family `source-dossier`, `content_kind :source` → `source_records` (derived, migration 015) | 1 | 4 | 5 | 5 (owner-authored, class `open`) | **Status: LIVE-LOCAL (P24-0, 2026-07-16; `enabled: true` by default — the P19-1 argument verbatim: no network, no license risk, rebuild replays regardless).** The THIRD `sync_policy: local` source (architecture §16): no upstream ever; `fetch` = `LocalFetch` re-scan (per-file sha256 ledger pins keyed `local:<file>`); drift probe short-circuits to `local`; writes reach canonical ONLY through `Nabu::SourceShelf` (the third sanctioned gateway). Populated by the owner-fired one-shot `nabu list --export-source-dossiers` (descriptions seeded from docs/library.md + sources.yml prose; honest stubs where none) + `bin/nabu sync local-source`; per-source scaffolds via `nabu ingest --shelf source SLUG`; gate-checked (`rake site:check` flags dossier-vs-library.md mention drift, never generates). |
| 60 | **Damaskini — Annotated Corpus of Pre-Standardized Balkan Slavic Literature 1.1** (CLARIN.SI hdl 11356/1441) | 23 gold-annotated samples of "damaskini" and other Balkan Slavic manuscripts/prints, **15th–19th c.**, 6,036 sentences / 53,257 tokens on the Church-Slavonic→Bulgarian continuum; ~10 independent witnesses of Euthymius of Tarnovo's *Life of St. Petka*. Gold lemma + custom MULTEXT-East **msd-bg-dam** MSD (XPOS) + UD deps (Level-2 validated) + `# text_en` on 100% of sentences | ONE corpus-wide **CoNLL-U** file (`conllu` family) + per-document **TSV** (3 orthographic layers, folio anchors, headers) + txt + 2 PDFs; two zip bitstreams, no auth | 2 | 4 | 4 | 5 (**CC BY-SA 4.0**) | **Status: LIVE (P23-1; synced + flipped by the owner, live census 2026-07-17: 46 docs / 12,072 passages — the 23 witnesses + their `-en` siblings).** License verbatim, the deposit record `dc.rights`: "Creative Commons - Attribution-ShareAlike 4.0 International (CC BY-SA 4.0)", rights URI creativecommons.org/licenses/by-sa/4.0/, access **PUB** → `attribution`, MCP-safe (v1.0 = hdl 11356/1368 was GPL-3, superseded; 1.1's grant governs). The clarin-si-survey (P17-6) pick #1 — the first openly licensed machine-readable slice of the Bulgarian/Macedonian historical axis (survey II: blocked everywhere else); zero overlap with TOROT/PROIEL/CCMH (their OCS canon vs this 15th–19th-c. descendant line). **Adapter `Damaskini`, `conllu` family reuse** (one doc per `# newdoc id`; new optional `citation:`/`metadata:` hooks on `ConlluParser`, UD callers byte-identical): urn `urn:nabu:damaskini:<newdoc-id, downcased>`, passage citation = the numeric tail of upstream's CORPUS-continuous sent_id (veles starts at 5601 — upstream's own numbers, frozen). Surface text = the corpus's mixed Latin/Cyrillic "diplomatic" transliteration (ъ 8,030×, ѳ ѯ ѵ ѱ ћ џ ь ꙫ; 100% NFC). **Language (honest verdict):** deposit tagged `bul, mkd` COLLECTIVELY, no per-doc machine tag; the philological PDF's own Norm classification maps → **chu** (veles, vukovic, kievski — Church Slavonic norm) / **bul** (20; fn.7: "Bulgarian" used "for historical reasons - the included sources do not use 'Macedonian' or 'Serbian'"); Norm + dialectal Origin (Macedonia/Rhodopes/Serbia-Torlak/West/East Bulgaria) ride as `document_facets` (norm/origin). Gold lemmas → `passage_lemmas`; TSV headers (place with honest "?", dates 1536–1860 incl. decades/ranges/centuries/"19th (post 1817)", scribes) → doc metadata + **`AxisBuilder::DamaskiniDates`** rows; `translations: true` mints `-en` siblings (same CC BY-SA, `--parallel` verse pairs via the new DAMASKINI_DOCUMENT work pattern). TSV TOKEN layers (accented\|Cyrillic\|diplomatic collation, folio, chunk, cross-text `ref` — the St.-Petka collation case) are **phase-2** (backlog P23-1: column layouts vary 15–20 cols, 3 files lack the cyrillic column, 5 files off by 1–3 sentences vs the CoNLL-U). See `.docs/surveys/clarin-si-survey.md` §1, `test/fixtures/damaskini/README.md`. |
| 63 | **CorPH — Corpus PalaeoHibernicum** (ERC ChronHib, Maynooth) | 78 deep-annotated Early Irish texts, **7th–10th c.**: Annals of Ulster, Vita Columbae, Blathmac, the Milan (~40k tokens) / St Gall Priscian / Würzburg minor gloss corpora (it absorbed the earlier standalone Milan and St Gall digital editions), Armagh, law, poetry, computus — 17,944 sentences / 136,559 gold-lemmatized tokens (census 2026-07-17), with per-lemma dil.ie headword keys, cited etymologies, mutation/onomastic/problematic annotation | ONE MySQL dump `chronhibdev_2020.sql` (39,102,512 B) in the GitHub repo `chronhib-MU/Chronhib-Website` — new **`corph-sql`** parser family (streaming INSERT walker), ordinary GitFetch, PINNED to commit `e7ef75d5f9a6` (dormant repo, 2021-05-11; fetch verifies the pin and stops on drift) | 2 | 5 | 4 | 4 (**MIT**) | **Status: LIVE (P25-0; synced + flipped by the owner 2026-07-17 — registry flip with sign-off, quarantine-noise hotfix tracked separately; live census: 76 docs / 17,942 passages).** License verbatim, the repo LICENSE (covers the repo including the dump): "MIT License / Copyright (c) 2020 [Chronologicon Hibernicum]" → `attribution`. **CODECS trap:** the CorPH site publishes no license of its own, and the CC BY-SA 3.0 footer seen on CODECS is CODECS's OWN site license, NOT CorPH's — never cite it for this source. THE FIRST CELTIC SOURCE and the **first sga GOLD lemmas** (the axis keystone: lights cel-pro ReflexViews attested counts, piet's CELT column context, IE-CoR's sga variety). Document per TEXT row (`urn:nabu:corph:<Text_ID>`), passage per SENTENCES text unit, MORPHOLOGY tokens in the P7-5 `tokens`/`lemma` contract → `passage_lemmas` (upstream homonym indices "macc 1" split lemma+homonym); LEMMATA joins per-token language + dil.ie ids ("dil"). **Language honesty (censused):** 80.5% Early Irish / 12.4% Latin / rarities (Old English, Old Norse, Pictish — unmappable stay verbatim `lang_source`); language lives per TOKEN, per PASSAGE (majority — the gold-index grain; 71 sga + 4 lat documents), per document. **Deep extraction:** TEXT Date → `AxisBuilder::CorphDates` rows (73/78 parse honestly — the ChronHib "date range … used" phrase incl. "(for text)" preference + the "Text: N-M / N X M" fallback; 5 honest residues incl. the Annals' annalistic spread); DIL headword ids → links-journal kind=reference edges `urn:nabu:dict:edil:<id>` (producer `corph`, ~12,300 document-grain pairs projected — the eDIL bridge before eDIL itself unblocks); Etymology column (cited PC/PIE reconstructions, 4,562 rows) journaled as the crosswalk follow-up. Honest residues: TEXT 0067/0068 have no sentences (skipped by rule); one empty and one orphan (Text_ID "6") SENTENCES row (censused). See `test/fixtures/corph/README.md`. |
| 64 | **RIIG — Recueil informatisé des inscriptions gauloises** (ANR RIIG, Ausonius/Bordeaux; riig.huma-num.fr) | **428 Gaulish inscriptions** — the Continental Celtic corpus: Gallo-Greek (`xtg-Grek`, 400) + Gallo-Latin (`xtg-Latn`, ~20) + bilinguals/indeterminates; per-record editorial READINGS (`seg` per editor, cert/resp), `<w>`-tokenized with **msd/pos/type morphosyntax**, RIG print-corpus concordance, TM ids, `origDate` (signed BCE/CE years, cert+evidence) + WGS84 findspots, French apparatus + translations; records carry DOIs (10.21412/petrae_riig_*) | EpiDoc TEI per record at the stable pattern `documents/data/documents/RIIG/<ID>.xml` (~6 MB total); corpus map GeoJSON embedded in corpus.html (the crawl seed) | 3 | 5 | 4 | 5 (**CC BY 4.0 in-file**) | **Status: LIVE (P25-1; crawled + flipped by the owner 2026-07-17; live census: 477 docs / 1,323 passages — the 428 records + 76 `-fr` siblings — a sibling ref exists iff the parser's own prose extraction finds paragraphs (P25-4 retired the byte peek; +18 previously-missed siblings minted) — the in-file grant GOVERNS (house doctrine, the Freising ruling) → **`attribution`**; the project page's *"L'ensemble de cette documentation est fournie en Open Access, sous la licence CC BY-NC-ND 4.0"* covers the site documentation only (never fetched; facsimile images separately rights-encumbered, never fetched). **Adapter `Riig`, NEW family `riig-epidoc`** (DOM — 428 tiny files, the freising precedent; `CelticLeiden` shared Leiden policy with ogham): passage = line within a reading (`urn:nabu:riig:<id>:<seg-id>:<lb n>`), EVERY reading mints (no marked preferred one — apparatus/translations point at readings by id); choice→reg, word-internal pretty-print whitespace stripped (`<space/>` = real division), gap→`[…]`, del→`⟦⟧`, surplus→`{}`, supplied/unclear counted; msd layer → per-line `words` annotations. Fetch = FileFetch corpus map + polite resumable record crawl (the ORACC html-en shape). `origDate`/findspot → **`AxisBuilder::RiigDates`** (signed years, Trismegistos refs; WGS84 geo verbatim in metadata); RIG concordance → kind=reference links edges (`rig:G593`, producer `riig`); French translation divs → `-fr` siblings (registry `translations: true`; per-file peek — coverage partial). **Quarantine floor (P25-4 census): 9** — 7 broken base records (bdr-04-05 lb@n, bdr-17-01 bad language tag, 5 × no-citable-text) + their 2 `-fr` riders. See `test/fixtures/riig/README.md`. |
| 65 | **OG(H)AM — Ogham in 3D v2.0** (DIAS / Maynooth; ogham.celt.dias.ie, GitHub lguariento/og-h-am) | **~500 ogham stones** — Primitive Irish (`pgl`) in **REAL Ogham codepoints** (ᚑᚌᚐᚋ, verbatim NFC), plus Pictish (`xpi`), Old/Middle Irish, Latin companion inscriptions, one runic stone; PARALLEL edition layers per record (ogham / transliteration / roman), forfeda + letter-variant glyphs via the repo charDecl, `nymRef` normalized name forms, **word-level dil.ie (eDIL) links**, logainm.ie place ids, CIIC/CISP/SMR concordances, English translations, 3D models | EpiDoc XML, one git repo (`XML/<REGION>/<ID>.xml`, 504 records + charDecl) | 3 | 4 | 4 | 3 (**conflict — nc pending**) | **Status: LIVE (P25-1; synced + flipped by the owner 2026-07-17, still `nc` awaiting the license reply; live census: 834 docs / 1,053 passages across the stone×layer documents).** License CONFLICT, both verbatim: site about-page *"the XML files… are freely accessible and downloadable under a CC-BY-NC-SA License…"* vs EVERY sampled record's `<licence>` *"Creative Commons Attribution 4.0 International License"* (target …/by/4.0/); repo has NO LICENSE file. The grants contradict rather than layer, so the RESTRICTIVE reading holds → **`nc`** (MCP default-excluded, never redistributed) PENDING the drafted clarification email (unlock registry #14); relabel-on-reply. **Adapter `Ogham`, NEW family `ogham-epidoc`** (DOM + charDecl glyph table, the freising glyph-map precedent): Document = (stone × layer), the Freising sibling design — ogham layer = bare urn (`urn:nabu:ogham:i-may-010`), transliteration/roman/… = `-translit`/`-roman` siblings with IDENTICAL line suffixes (suffix-equality `--parallel` alignment; script honesty: the transliteration is a parallel rendering, never the Ogham text's replacement; a false `-Ogam` subtag on a Latin-script layer is shed, `-Latn` never invented). Glyphs resolve per layer (ogham vs diplomatic mapping, `@type` override); choice→corr (either order); no-`<lb>` layers fall back to one `:text` passage (the EDH P23-3c precedent); a DECLARED layer with no citable text mints no ref (skip-by-rule, censused — `OghamEpidocParser#layer_census`, the parser's own extraction; P25-4 retired the byte peek)). dil.ie refs → kind=reference links edges (`https://dil.ie/<id>`, producer `ogham` — the same eDIL id space corph will bridge, coordinated by producer, no code coupling); logainm/geo/idnos/translation → primary-document metadata. GitFetch, `sync_policy: manual` (live project). **Quarantine floor (P25-4 census): 5** — W-PEM-006/012 transliteration lb@n + 3 charDecl glyph-drift refs (i-ker-054-roman, s-abd-003 ×2). See `test/fixtures/ogham/README.md`. |

## Tier 3 — restricted, awkward, or specialized (adapter only if research demands)

| # | Source | Content | Blocker |
|---|--------|---------|---------|
| 23 | **PHI Latin Texts / Greek Inscriptions** | Canonical Latin corpus + epigraphy | No bulk download; terms prohibit systematic copying. Browse-only companion; see unlockables. |
| 24 | **TLG** | Most complete Greek corpus in existence | Subscription, no export. The single biggest gap in any open pipeline. See unlockables. |
| 25 | **Trismegistos** | Metadata/identifiers for papyri & inscriptions | Metadata-only, access tiers. Valuable as an ID crosswalk, not as text. |
| 26 | **ctext.org** | Classical Chinese | API with tight quotas, restrictive terms. Kanripo (#16) is the open substitute. |
| 27 | **CAL (Comprehensive Aramaic Lexicon)** | Aramaic corpus + lexicon | Web-only, restrictive. Request-based (see unlockables). |
| 28 | **Beta maṣāḥǝft** | Ethiopic (Gǝʿǝz) manuscripts | TEI on GitHub actually — promote to Tier 2 if Ethiopic enters scope. |
| 29 | **Internet Archive / HathiTrust PD scans** | The infinite long tail: every out-of-copyright edition | Not a text corpus — a scan firehose. Served by the ad-hoc pipeline plus a thin "fetch by identifier" adapter (IA has clean APIs; HathiTrust PD downloads need an account/API key). |
| 31 | **Russian National Corpus — historical corpora** (Old/Middle Russian, Birchbark) | `orv`/`chu` spanning 11th–18th c., deeply annotated | **SURVEYED-BLOCKED (P9-6).** Query-only web UI (`ruscorpora.ru`); stated "texts cannot be distributed" → not bulk-ingestable. **The ingestable slice is its `CC BY-SA 4.0` UD releases** (Birchbark, Middle-Russian RNC) — survey pick #1, reuses the `ud` adapter, zero new family. Bulk corpus unblock: written permission (`research_private`). |
| 32 | **"Манускриптъ" / Manuscript (manuscripts.ru, Udmurt State Univ.)** | 130+ transcribed Slavonic/Russian mss, 10th–15th c., >3.5M word occurrences (Gospels, menaia, chronicles) | **SURVEYED-BLOCKED (P9-6).** Full-text retrieval system; no located bulk export or license grant. Unblock: write to the Izhevsk team for a data grant (`research_private` after owner contact) — high potential value if it lands. **P13-1 re-check (2026-07-11): UNCHANGED, arguably degraded** — `manuscripts.ru` redirects to `io.udsu.ru` with a host-mismatched TLS cert, `manuscript.udsu.ru` no longer resolves; no export/API/license appeared (live app not renderable this pass — inferred from reachability + literature). |
| 33 | **Sreznevsky, *Materialy dlya slovarya drevnerusskogo yazyka*** | Old Russian historical dictionary, citable attestations from ~2700 sources | **SURVEYED-BLOCKED (P9-6).** Only page scans (HathiTrust, Presidential Library); no machine-readable TEI located → not ingestable as text. Unblock: HTR/OCR on the PD scans (docs/03 reconstruction strategy). **P13-1 re-verify (2026-07-11): CONFIRMED, still no machine-readable edition** — `oldrusdict.ru` ("40,000+ entries") is a query-only web UI with no export and no stated terms; GitHub search finds no structured transcription; NOT in the GORAZD hub (#47). |
| 38 | **DOEC — Dictionary of Old English Corpus** (Univ. of Toronto) | The complete surviving OE record: ~3M OE words, ~3,000 texts, TEI-P5 — the ancestor e-text of YCOE/ISWOC | **SURVEYED-BLOCKED (P11-1).** Subscription search product ($75/yr indiv., $200/yr inst., 20 free logins/yr); terms verbatim (§3, webcorpussub.pdf): "Recompiling, copying, publication, or republication of the data, or any portion thereof … may be done only with specific written permission from the Dictionary of Old English project"; library terms prohibit TDM. Unblock: written permission (`research_private`), OR verify the **2000 DOEC release on OTA (record 2488, ~26 MB SGML/TEI, "Academic Use" noncommercial)** — a local-research path, still no redistribution. See `.docs/surveys/oe-survey.md`. |
| 39 | **OE web-app editions: Old English Poetry in Facsimile · Electronic Beowulf · Jebson's Anglo-Saxon Chronicle · The Digital Ælfric** | OEPF ~350 works/~26k lines transcribed+translated; eBeowulf facsimile+transcription; Jebson ASC MSS A–E; Digital Ælfric 8 homilies/27 MSS | **SURVEYED-BLOCKED (P11-1).** All web-apps with no data download and no reuse grant: OEPF "open-access" = access not a license (Digital Mappa app; unblock: ask Foys/UW for a data grant); eBeowulf "© Kevin Kiernan and the British Library"; Jebson verbatim "Copyright © 1996-2006, Tony Jebson … all rights reserved" (XHTML only, TEI-P4 source unpublished; unblock: email); Digital Ælfric commercial. ASPR #35 covers the poetry text layer; ISWOC #34 covers ASC/Ælfric samples. See `.docs/surveys/oe-survey.md`. |
| 44 | **Rahlfs LXX (CATSS/CCAT lineage)** | The Septuagint in Rahlfs's 1935 edition — every machine-readable copy descends from the TLG→CATSS morphological database | **SURVEYED-BLOCKED (P11-5, 2026-07-09).** The 1935 TEXT is PD by age (Rahlfs d. 1935), but the machine-readable lineage is encumbered: the CCAT user declaration requires verbatim "Not to use or make available these materials for commercial purposes without first obtaining the written consent of the owners/encoders" and "To control access to these materials and require any other party to whom the recipient supplies any portion of this material to observe these conditions" — a registration-gated no-uncontrolled-redistribution term below every acceptable class. `eliranwong/LXX-Rahlfs-1935` relabels it CC-BY-NC-SA in README prose but ships NO license file and itself requires the CCAT declaration; `CenterBLC/LXX`'s MIT covers only its Text-Fabric conversion (`@Editors=CCAT` headers); **Rahlfs-Hanhart 2006 is © Deutsche Bibelgesellschaft**; STEPBible (CC BY 4.0) ships NO Greek OT as of 2026-06 (TAGNT + Hebrew TAHOT only, verified in-tree). **The open LXX is Swete 1909 — and it is already in the catalog** (First1KGreek tlg0527, #2; the standalone `nathans/lxx-swete` CC-BY-SA repo is itself derived from tlg0527 — ingesting it would duplicate the edition). Unblock paths, UPDATED 2026-07-15: a direct permission request was **DECLINED by the rights holder** (E. Tov, by email — CATSS is now part of the commercial Accordance Bible software, OakTree; such requests are routinely turned down). The sign-the-CCAT-declaration route is accordingly DOUBTFUL: the rights holder locates the database's licensing with Accordance, so do not sign-and-ingest without revisiting. Remaining honest paths: Swete 1909 (already held) or STEPBible's planned TAGOT. |
| 47 | **GORAZD — Old Church Slavonic Digital Hub** (gorazd.org, Institute of Slavonic Studies, Czech Academy) | The digitized Prague **SJS** (*Slovník jazyka staroslověnského*, the most comprehensive OCS dictionary in existence, ~33k entries) + Cejtlin *Staroslavjanskij slovar'* + Greek–OCS index + card archive. **NB: Miklosich and Sreznevsky are NOT in the hub** (packet lead corrected) | **SURVEYED-BLOCKED (P13-1, 2026-07-11).** Query-only web UI (Gulliver); no download, no API, and **no content license anywhere** — only GDPR/cookie text; the advertised GNU GPL covers the *software tools*, not the data. No LINDAT/CLARIAH-CZ deposit (the ASEP CC0 covers catalog metadata only). The unlicensed GitHub scrape `utajum/old-church-slavonic-dictionary` (33,036 records, GORAZD ids in the JSON) proves the data is structured (per-entry Greek/Latin equivalents + 5-language glosses) but inherits the block — never ingest it without the grant. Unblock: data-sharing agreement with the Slovanský ústav AV ČR — the highest-value Slavic dictionary unblock there is. See `.docs/surveys/slavic-survey-2.md`. |
| 48 | **Miklosich, *Lexicon palaeoslovenico-graeco-latinum* — BCDH/ELEXIS TEI edition** | The 1862–65 OCS–Greek–Latin lexicon (PD by age), **41,338 entries**, converted to TEI (Lex-0) by the Belgrade Center for Digital Humanities (2022) | **SURVEYED-BLOCKED (P13-1, 2026-07-11) — the nearest dictionary-shelf prize.** CLARIN.si record 11356/1666 is **metadata-only**: 0 downloadable files, no `dc:rights` (verified on the record page; REST `"bitstreams":[]`). If released it would drop into the **existing TEI dictionary adapter family** — no new format work. Unblock: email BCDH/ELEXIS (or repo-help@clarin.si) to complete the deposit + confirm license. PD scans (archive.org, MDZ) remain the OCR fallback. See `.docs/surveys/slavic-survey-2.md`. |
| 49 | **South Slavic Church Slavonic editions: Croatian (Staroslavenski institut) · Serbian (SANU) · Bulgarian (Cyrillomethodiana/histdict, Sofia) · DIACU (CNR)** | Croatian: RCJHR dictionary (letters A–I) + 60-source Glagolitic corpus; Serbian: 12th–18th c. corpus, ~450k-word medieval slice, morph-annotated; Bulgarian: ~147 texts 10th–18th c. + *Starobălgarski rečnik*; DIACU: 652-doc diachronic ChSl JSON (22.8 MB) | **SURVEYED-BLOCKED (P13-1, 2026-07-11) — no open machine-readable South Slavic ChSl edition exists.** Croatian: dictionary is **scanned-page PDFs**, corpus unpublished, no license (unblock: write info@stin.hr). Serbian: internal to the SANU dictionary project, no release (unblock: contact the Institute for the Serbian Language). Bulgarian: web-UI only, footer verbatim "© Софийски университет „Св. Климент Охридски" 2011-2022" — no grant (unblock: ask the Sofia team). DIACU (`MariaCassese/DIACU`): clean JSON but **no LICENSE file** (GitHub `license: null`) and the OCS core is re-packaged TOROT/PROIEL already held (sampled Zographensis record sources `torottreebank/treebank-releases`) — only the New-ChSl/Ruthenian slices are novel (unblock: ask the author; low value net of overlap). Adjacent but open: **CroALa** — Croatian **Latin**, ~5.6M words TEI, CC-BY, GitHub+Zenodo — noted for a future Latin-breadth phase. See `.docs/surveys/slavic-survey-2.md`. |

## Ranking synthesis

**By effort-to-value (build order):** 1 → 2 → 4 → 3/5 → 10 → 7 → 8 → 11 → 6 → 9 → 15 → 14 → 18/19 → 13.

Rationale: adapters 1–2 share one EpiDoc/CTS implementation and immediately deliver the classical backbone; 3–5 share the PROIEL/CoNLL-U implementation and deliver the annotated IE layer; 10 reuses EpiDoc. That's three parser families covering ten sources. TITUS (#13) is deliberately late despite top-tier content: it's the highest-friction integration and should be attempted only once the normalization layer is mature enough to absorb its encoding chaos.

**By content criticality for the Slavic/IE research axis:** 3, 5, 19, 18, 13, 4 — these are the corpora whose absence would actually block work.

**By licensing cleanliness (safe to ever share/publish derivatives):** 1, 2, 7, 8, 9, 10, 15, 16 (CC with attribution) > 3, 5 (NC clause) > 6, 14, 19 (research-use, keep private) > 13, 23–27 (private use only, segregate).

**Format families → shared parser components:**
- *EpiDoc/CTS TEI:* Perseus, First1KGreek, Papyri.info
- *PROIEL XML:* PROIEL, TOROT
- *CoNLL-U:* UD, DCS(convertible), AGDT
- *ATF/JSON:* ORACC, CDLI
- *Bespoke:* GRETIL, TITUS, Sefaria JSON, ETCSL, TLA, kanripo, CCMH
- *IIIF image feeds (→ HTR):* e-codices, ADA, DigiVatLib (see unlockables)
